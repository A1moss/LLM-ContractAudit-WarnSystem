# A24 基于大模型的企业合同智能审核与风险预警系统

# 详细设计文档

> 版本：v4.0（对齐实现：证据-裁决解耦 + 建议层）
> 日期：2026年9月
> 团队：海底汪汪队

---

## 目录

1. [项目概述与商业价值](#1)
2. [核心方案一：合同智能分类与要素抽取](#2)
3. [核心方案二：证据驱动的确定性风险识别](#3)
4. [核心方案三：标准条款语义比对与缺失检测](#4)
5. [核心方案四：用户反馈闭环与审核报告生成](#5)
6. [系统架构总览](#6)
7. [创新亮点总结](#7)
8. [开发历程与里程碑](#8)
9. [数据库设计](#9)
10. [API接口设计](#10)
11. [部署与运维](#11)
12. [安全与企业适配](#12)
13. [V2.0 未来规划](#13)
14. [附录A：项目目录结构](#14)
15. [附录B：技术风险与应对](#15)
16. [附录C：鼓励创新方向（赛题第7节）](#16)
17. [附录D：量化实验指标与验证方案](#17)
18. [附录E：数据集与知识库](#18)
19. [附录F：赛题提交材料对应体系](#19)

---

# 第一部分：方案设计篇

---

## <a id="1"></a>一、项目概述与商业价值

### 1.1 项目定位

**SmartContract Auditor** 是一款面向企业法务与风控部门的 AI 原生合同智能审核平台，深度适配网新恒天等 IT 服务与金融科技企业的合同管理场景。它以"人机协同审核"为核心理念，融合大语言模型的语义理解能力、证据-裁决解耦的确定性推理机制以及专业法务知识库，实现从"合同上传 → 智能审核 → 风险预警 → 报告生成"的全流程闭环。

### 1.2 赛题要求覆盖与达标方案

> 比赛文档的第一要务：先证明基础指标达标，再展示创新加分。以下逐项对照赛题强制要求，说明本系统的达标方案和对应的量化指标验证位置。

| 赛题要求 | 达标方案 | 实测指标 | 验证 |
|---------|---------|:------:|------|
| 合同类型分类（≥5 种，准确率 ≥85%）| RAG 检索范本 + LLM 推理（11 类 + 服务外包标签）| **98.6%**（145/147）| 附录 D.2 |
| 关键要素抽取（F1 ≥ 80%）| JSON Schema 约束 + Few-shot Prompting + 分块并行 | **90.7%**（披露样本加权综合 F1）| 附录 D.2 |
| 风险条款识别（≥10 类，召回率 ≥75%）| 证据-裁决解耦：LLM 抽事实 + 代码硬阈值裁决（13 类）| **召回率 80.1% / 精准率 76.6%** | 附录 D.2 |
| 标准条款比对与缺失检测 | 结构化过滤 + LLM 逐条比对 + 图分析跨条款风险 | 覆盖率 / 缺失清单 | 第四章 |
| 自动生成结构化审核报告 | AI 生成摘要 + ECharts 风险热力图 + 建议层 | 六章结构报告 | 第五章 |
| Web 可视化界面 | Vue 3 SPA + pdf.js + ECharts 热力图 | 五个核心页面 | 第六章 |
| 用户反馈标注 | 交互式标注（确认/修正/补充）+ 结构化存储 | 闭环机制 | 第五章 |

### 1.3 功能范围与交付优先级

系统明确区分三个交付优先级，确保 3 周开发周期内核心功能可完整演示：

**P0（必须交付 — 赛题强制要求 + 演示闭环基础）：**

合同上传与多格式文档解析（含 OCR）、合同类型分类（11 类法理 + 服务外包标签）、关键要素结构化抽取、规则召回 + 证据抽取 + 确定性裁决 + 建议层（13 类风险）、标准条款比对与缺失检测、审核报告生成（含 ECharts 风险热力图）、用户反馈标注、Web 可视化界面。

**P1（冲刺交付 — 差异化加分项）：**

多轮对话式合同修改（RCBSF 多智能体）、跨条款关联风险分析（图分析）、JWT 认证 + 角色区分、Docker Compose 一键部署。

**P2（未来规划 — 答辩 PPT 标注"V2.0 规划中"）：**

自定义审查规则引擎、合同版本差异对比、企业知识库管理、RBAC 三级权限体系、完整操作审计日志、数据加密体系、印章/签名区域定位。详见第十三章"未来规划"。

### 1.4 评分要点对应与核心指标达成

为方便评委按评分维度快速定位，以下表格逐项对应初赛评分标准：

| 评分维度 | 本系统对应章节 | 关键内容 |
|---------|-------------|---------|
| 需求分析 | 第一章（1.1-1.5）| 商业价值故事 + 赛题覆盖矩阵 + 功能优先级矩阵 |
| 方案设计 | 第二至五章 | 四个核心技术方案的问题分析 → 方案设计 → 创新 → 指标 |
| 技术创新 | 第三章 + 第七章 | 证据-裁决解耦 + 图分析条款比对 + 建议层 grounding |
| 工程可行性 | 第九至十二章（工程实现篇）| 数据库设计 + API 设计 + Docker 部署 + 降级方案 |
| 商业价值 | 第一章（1.5）| 量化价值对比（审核时间 80%↓、风险遗漏 5%↓） |
| 演示效果 | 第八章（开发历程）| 里程碑 + MVP 交付闭环 |

**核心指标达成总览（实测，对标赛题强制要求）：**

| 赛题强制指标 | 赛题要求 | 实测值 | 状态 |
|------------|:------:|:--------:|:---:|
| 合同分类准确率 | ≥ 85% | **98.6%**（145/147）| ✅ 超额 13.6pp |
| 要素抽取 F1 | ≥ 80% | **90.7%**（披露样本加权综合 F1）| ✅ 超额 10.7pp |
| 风险识别召回率 | ≥ 75% | **80.1%** | ✅ |
| 风险识别精准率 | ≥ 75% | **76.6%** | ✅ |
| 风险类型覆盖 | ≥ 10 类 | 13 类（R01-R13）| ✅ 超额 |

### 1.5 一句话价值主张

> **"把法务从逐条比对中解放出来，5 分钟完成一份百页合同的全面审核——不只是找风险，更是沉淀企业法务知识资产。"**

### 1.6 商业价值故事

网新恒天（Insigma Hengtian）作为国内领先的 IT 服务与金融科技企业，法务部每天面对大量技术服务合同、软件开发外包合同、SaaS 服务协议和金融客户合作协议。李律师是法务部的主办律师，每份合同需要逐条审核几十页的条款——不仅关注常规的违约金、保密、知识产权，还需要特别关注 IT 服务行业的特有风险：**SLA 服务等级条款是否合理、数据导出与销毁条款是否完备、知识产权交付物归属是否清晰、源代码托管是否约定**。

**一份典型的技术外包合同完整审核通常需要 2-4 小时。** 更棘手的是，李律师休年假时，同事接手完全不熟悉他之前与客户的特殊约定——法务知识沉淀在个人脑中，人走了经验就没了。

**引入 SmartContract Auditor 后：**

- 李律师将百页技术外包合同一键上传，系统在 30 秒内完成全文解析、类型识别、要素提取
- AI 自动标注了 15 处风险条款（其中 3 处高风险、7 处中风险、5 处低风险），涵盖常规风险（违约金、保密）和行业特有风险（SLA 罚则过高、数据导出条款缺失、IP 归属模糊），每处附带原文定位、风险等级、修改建议和参考条款
- 系统的证据-裁决引擎先让 LLM 抽取条款客观事实，再由确定性规则按法条阈值裁决，每条风险都附带「事实证据 + 裁决依据 + 法条 + 修改建议」，可逐条追溯
- 系统自动比对网新恒天的标准合同模板，发现缺失了"不可抗力条款""数据隐私保护条款"和"源代码托管条款"，并给出了补全建议
- 李律师用 5 分钟审核了所有 AI 标注，修正了 2 处误报，补充了 1 处 AI 未发现的行业特定风险（金融客户的监管合规要求）
- 点击"生成报告"，一份结构化的审核报告（含风险分布热力图、条款对比表、具体修改建议）导出为 PDF，发给业务部门
- 上周的审核记录、风险标注、修改决定全部存档在系统中，新同事可以直接搜索历史案例

**量化价值对比：**

| 指标 | 传统模式 | AI 辅助模式 |
|------|---------|-----------|
| 单份合同审核时间 | 2-4 小时 | 5-15 分钟 |
| 风险遗漏率 | ~15%（人工疲劳） | <5%（AI 全覆盖） |
| 法务知识可传承性 | 依赖个人 | 系统沉淀，可搜索 |
| 跨合同关联风险发现 | 几乎不可能 | 自动关联提醒 |
| API 调用成本 | — | 预期 < 0.5 元/份（精准模式） |

### 1.7 本文档阅读指引

本文档分为三部分：

- **方案设计篇（第一至第八章）：** 围绕四个核心技术问题展开，遵循"问题分析 → 方案设计 → 创新亮点 → 预期指标"的叙事逻辑。评委和答辩老师可重点阅读。其中 1.2 节为赛题达标总览，1.3 节为功能优先级矩阵。
- **工程实现篇（第九至第十二章）：** 数据库设计、API 接口、部署方案、安全方案等工程落地细节。证明方案的可行性和完整性。
- **附录（A-E）与未来规划（第十三章）：** 目录结构、风险应对、创新方向、实验指标、数据集方案；V2.0 规划功能清单。作为支撑材料，答辩时可选择性引用。

---

## <a id="2"></a>二、核心方案一：合同智能分类与要素抽取

> **赛题指标：** 合同类型分类 ≥ 5 种，准确率 ≥ 85%；关键要素抽取 F1 ≥ 80%

### 2.1 问题分析

合同审核的第一步是"搞清楚这是什么合同、里面有什么"。传统做法是法务人员手工翻阅，判断合同类型、提取甲乙双方、金额、日期等关键信息——这类机械性工作占审核总时间的 30% 以上。

**技术挑战：** 合同类型多样（11 类法理类型 + 服务外包业务标签），表述方式差异大（"买方/卖方""甲方/乙方""需方/供方"等称谓变体）；且长合同的关键类型信号常出现在中后段，仅截取开头会因「中间信息丢失」导致误判。

### 2.2 法理分类：检索增强 + LLM 推理（11 类 + 服务外包业务标签）

系统采用「检索增强 + 大模型推理」的分类方案，不做本地小模型微调，输出两个正交维度：

- **法理分类（11 类，单一互斥）：** 买卖合同、租赁合同、承揽合同、建设工程合同、技术合同、委托合同、物业服务合同、中介合同、保密协议、无名合同、劳动合同——按合同核心标的的实质法律关系判定（劳务派遣归入劳动合同）。
- **服务外包业务标签（is_outsourcing，可叠加）：** 独立于法理分类的第二判断——该合同是否属「服务外包」业务。一个合同可以法理上是承揽/技术/委托，同时业务上是服务外包，两者不冲突。

**RAG 少样本分类（检索增强 + LLM 推理）：**

1. **RAG 少样本检索：** 在合同范本向量库中检索 top-K 篇与待分类合同最相似的已标注范本，作为「少样本示例」注入 LLM 上下文（借鉴 KATE 的 kNN 检索少样本示例选择、kNN-LM / REALM 的检索增强思想）；检索无结果时降级为纯 LLM 零样本。
2. **LLM 法理推理：** LLM 内置完整 11 类法理定义（含「劳务派遣归入劳动合同」等类别归属规则），参照检索示例做最终法律判断——示例只是 few-shot evidence，最终以正文实质内容 + 分类 ontology 为准。

**效果（实测，对应赛题指标 1）：** 147 份分类正式测试集（第一批 84 + 第二批 55 + 人工构造困难样本 8）上分类准确率 **98.6%**（145/147），远超赛题 ≥ 85% 门槛。分类体系唯一真源维护于 `ai/taxonomy.py`，分类器、条款比对、前端下拉均从该处读取，避免多处硬编码不一致。（历史冻结口径：97.62%，82/84，84 份原测试集——保留作版本对照。）

### 2.3 关键要素结构化抽取

分类完成后，系统从合同中抽取结构化关键要素。采用 JSON Schema 约束 + Few-shot Prompting 方案：在 Prompt 中预定义 JSON Schema（parties / amount / performance_period / dispute_resolution 等字段），配合标注示例引导 LLM 输出规范 JSON；后端做格式校验与容错。长合同分块并行抽取后按「非空优先」合并，避免尾部要素丢失。

**抽取字段：** 双方（甲方/乙方，含买卖/委托/承揽等多种称谓归一化）、合同金额（数值 + 币种，区分含税/不含税）、签署日期、履行期限（起止日期）、争议解决方式、适用法律。

**效果（实测，对应赛题指标 2）：** 84 份测试集上要素抽取披露样本加权综合 F1 **90.7%**——双方 95.2%、金额 84.6%、期限 85.9%、争议解决 96.3%，远超赛题 ≥ 80% 门槛。要素抽取结果结构化存入 contracts 表的 extracted_elements 字段（JSON 格式，详见第九章数据库设计）。

### 2.4 文档解析支撑

分类和抽取的前提是高质量的文本提取。系统支持三种格式的解析：

| 格式 | 处理方案 | 核心库 |
|------|---------|--------|
| .docx | 直接提取文本 + 保留段落结构（标题/条款/表格/签名区）| python-docx |
| .pdf（文本型）| 直接提取文本 + 表格识别 | pdfplumber |
| .pdf（扫描型）/ 图片 | OCR → 版面分析 → 文本提取 | PaddleOCR |

**扫描质量预检与容错（P0 简化版）：** P0 阶段的核心目标是"可提取文本即可"——PaddleOCR 对清晰扫描件的识别准确率已足够支撑后续分类和审核。上传扫描件/图片时，系统执行基础质量预检（检测 DPI < 150 时提示可能影响识别效果），OCR 识别结果中置信度 < 0.6 的区域在风险标注时附带"OCR 识别质量低，建议人工核对原文"提示。高级质量检查（倾斜检测、缺页告警、法律术语字典纠错）和置信度分级标注体系列入 V2.0 优化。

解析过程中保留段落层级结构（章-条-款-项），这是后续风险定位和原文高亮的技术基础——每一条 AI 标注都能精确追溯到原文的页码和行号。

---

## <a id="3"></a>三、核心方案二：证据驱动的确定性风险识别（LLM 抽事实 + 规则裁决）

> **赛题指标：** 风险识别 ≥ 10 类，召回率 ≥ 75%

### 3.1 问题分析

风险识别是合同审核的核心，也是最容易「翻车」的环节。直接让 LLM 判定风险有两个致命问题：**幻觉**（标注出原文根本不存在的风险条款）和**不一致**（同一合同两次审核结论漂移）。规则引擎可复现但覆盖不了模糊语义，纯 LLM 覆盖面广但不可控。

**我们的方案：把「有没有风险」和「怎么改」彻底解耦。** LLM 只负责抽取**客观事实证据**（不判断风险、不下法律结论），风险判定完全交给 Python 的**确定性规则裁决**（硬阈值，可复现）。这一「证据-裁决解耦」架构是 A24 的核心技术壁垒——LLM 擅长语义理解但输出不稳定，代码裁决稳定可复现但需要结构化输入，两者取长补短、各司其职。

### 3.2 三层流水线总览

| 层 | 模块 | 职责 | 是否用 LLM |
|----|------|------|:---:|
| 第一层：规则召回 | `rule_engine.py` | 确定性正则召回 R01–R13，`fast` 模式基线、`precise` 模式兜底 | 否 |
| 第二层：证据抽取 + 确定性裁决 | `evidence_extractor.py` → `evidence_adjudicator.py` | LLM 只抽 13 类条款事实证据 → Python 按 v1.2.4 口径硬阈值裁决 R01–R12 | 抽取用 LLM，裁决纯代码 |
| 第三层：建议层 | `recommendation_engine.py` | 按法条编号精确取条文 + LLM 生成修改建议 + grounding 校验 | 是（只负责「怎么改」） |

**13 类风险清单与法理依据（v1.2.4 口径）：**

| 编号 | 风险类型 | 裁决依据（硬阈值，代码确定性判断） | 法条 |
|:---:|---------|----------------------------------|------|
| R01 | 违约金过高 | 违约金日利率 ≥ 5‰ | 民法典 585 |
| R02 | 无限责任 | 赔偿范围覆盖「全部损失/一切责任/预期利益或间接损失」，无上限 | 民法典 584 |
| R03 | 单方解约权 | 一方可随时无理由解约且无补偿（含委托/承揽任意解除权） | 民法典 562/563/933/787 |
| R04 | 管辖不利 | 管辖法院/仲裁地明确偏向对方（我方=甲方） | 民诉法 35 |
| R05 | 保密期不合理 | 合同约定的保密义务永久/无限期 | 民法典 501 |
| R06 | IP 权益失衡 | 无对价/明显不对称的成果利益剥夺（有对价买断不标） | 民法典 859 |
| R07 | 付款义务失衡 | 付款与服务贡献明显脱钩（预付款/尾款比例失衡） | 民法典 525/526 |
| R08 | 验收机制缺失 | 缺乏足以客观判断交付是否合格的验收机制（仅交付型合同） | 民法典 621-623/845 |
| R09 | 不可抗力缺失 | 交付型合同无不可抗力条款（非交付型不标） | 民法典 180/590 |
| R10 | 竞业限制过宽 | 竞业期限 ≥5 年且范围全国/主营全禁（商业竞业） | 劳动合同法 23/24 |
| R11 | 自动续约陷阱 | 期满沉默即自动续约、无书面退出机制 | 民法典 734 |
| R12 | 数据隐私不当 | 数据/个人信息共享转让无授权边界 | 个保法 23 |
| R13 | 名实不符 | 名义类型与实质法律关系不一致（单列示警，不进 F1） | 劳动合同法 66 |

> **行业适配说明：** 以上风险类型覆盖 IT 服务与金融科技行业的高频风险场景——SLA 罚则（R01/R07）、数据导出与销毁（R12）、知识产权交付物归属（R06）、源代码托管义务（R08）等，直接适配网新恒天的业务场景。

每条风险裁决结果结构化存入 audit_records 表，保留 detection_method、证据链（evidence）与建议层（recommendation）字段。

### 3.3 第二层第一步：事实证据抽取（LLM 只抽事实，不做判断）

规则引擎能覆盖确定性风险，但面对模糊条款（如「合理的违约金」是多少、「全部损失」是否含间接损失），需要语义理解能力。关键是：**语义理解只用于抽取事实，不用来下结论。**

证据抽取层用一套冻结的 JSON Schema，让 LLM 从合同原文中抽取 13 类条款的**客观事实**：

| 证据字段 | 抽取内容（只抽事实，不判风险） |
|---------|------------------------------|
| R01_违约金 | 违约金比例、单位（日/月/一次性）、计算基数、责任方 |
| R02_责任 | 赔偿范围（实际损失/直接损失/全部损失/含预期利益或间接损失）、是否有上限 |
| R03_单方权利 | 解约/中止/变更权在谁、是否任意、是否无补偿 |
| R04_管辖 | 争议方式（诉讼/仲裁）、约定地点、偏向哪方 |
| R05_保密 | 保密期限（永久/无限/不因终止而终止）、是否法定义务 |
| R06_IP | 成果归属、是否有对价、失衡表述 |
| R07_付款 | 预付款/尾款比例、是否有里程碑 |
| R08_验收 | 是否有客观验收依据及原文 |
| R09_不可抗力 | 是否有不可抗力条款及原文 |
| R10_竞业 | 竞业期限、范围、是否全国/主营全禁 |
| R11_续约 | 续约模式（沉默自动续/主动续签/固定期限）、是否有退出通道 |
| R12_数据 | 是否有个人信息/敏感数据、是否有授权边界 |

**抽取纪律（Prompt 硬约束）：** 找不到填 null（数字）/false（布尔）/""（文本），绝不臆测；比例用小数（5‰=0.005）；**绝对不下「是否构成风险」的结论、不输出 risk/is_risk 字段**。

**长文本处理：** 证据抽取按 ~20000 字符分块，块内并行调用；「补全风险条款」段落独立切出、与正文并行抽取，合并时补全条款字段级覆盖正文（中性值不覆盖，避免误杀写在正文里的风险）。

### 3.4 第二层第二步：确定性裁决（Python 硬阈值，v1.2.4 口径）

证据抽取完成后，**风险判定完全由 Python 代码完成，LLM 不再参与**。裁决器 `evidence_adjudicator.py` 按 v1.2.4 标注口径的硬阈值逐类裁决（见 3.2 表），例如：

- R01：证据中的违约金日利率 ≥ 5‰ 才触发（3‰/日即使年化 109.5% 也不触发）；
- R02：赔偿范围 scope = 「全部损失」或「含预期利益或间接损失」才触发（「实际损失/直接损失」是有边界赔偿，不触发）；
- R08：只有当合同缺乏「足以客观判断交付是否合格」的验收机制才触发（仅有「验收合格后付款」这类结果性表述而无标准/程序/责任主体的，判缺失）；
- R10：商业竞业期限 ≥5 年 **且** 范围全国/主营全禁才触发（3 年同类排他不触发）；
- R11：沉默即自动续约且无书面退出机制才触发。

**为什么这一解耦能同时提升精准率与召回率（v4→v6.4 实测）：**

| 版本 | 方案 | 精准率 | 召回率 | F1 |
|------|------|:---:|:---:|:---:|
| v4 | LLM 泛化 Prompt 直接判风险 | 24.0% | 97.4% | 38.0% |
| v5 | 阈值写进提示词（严格 Prompt）| 25.9% | 73.5% | 38.3% |
| **v6.4** | **LLM 抽事实 + 代码硬阈值裁决** | **76.6%** | **80.1%** | **78.3%** |

v4 让 LLM 直接判风险，过报 466 条（精准率仅 24%）；v5 把阈值写进 Prompt，召回率反被误杀到 73.5%。教训很明确：**LLM 擅长布尔/数值判断，复杂枚举/嵌套规则交给代码裁决**。v6.4 把裁决权收回代码后，精准率从 24% 拉到 76.6%，同时召回率守住 80.1%。

### 3.5 第三层：建议层（风险处置，v6.5）

风险裁决层（冻结）只负责「有没有风险」，建议层负责「怎么改」，两者彻底解耦、绝不反向影响 R01–R12 判定：

1. **法条精确检索：** 按 RULE_LAWS 中该风险的法条编号**精确取条文**（`laws.json` 精编库），不做语义检索——避免「R11 自动续约」漂移到「劳动合同无固定期限」这类语义漂移。
2. **LLM 生成建议：** 基于裁决结论 + 事实证据 + 法条，批量生成三段内容——risk_description（为什么标出）、suggestion（修改方向）、example（示例条款）。
3. **Grounding 校验：** 拦截建议中出现的任何**证据/法条里没有的数字、比例、金额、期限、法条编号**（含中文数字），防止 LLM 编造（如自创「百分之十」）。
4. **空建议兜底：** LLM 部分失败时回退到护栏方向（RISK_GUARDRAILS），保证每条风险都有可执行的处置方向。

**建议层护栏示例：** R01 方向「重新评估违约责任与可能损失是否匹配」，forbidden「不得自行新增具体比例、金额、期限」；R04 方向「协商将管辖调整为我方所在地或中立地」，forbidden「不得自行新增具体法院名称、地域」。

**审核模式：** 系统提供两个审核模式——`fast`（纯规则引擎，秒级出结果，作为确定性基线）与 `precise`（规则 + 证据抽取 + 确定性裁决 + 建议层，主口径，实测指标达标）。引擎默认 `precise`，前端与后端三处统一，避免误用 `fast` 输出纯规则结果。

**LLM 调用一致性保障：** 所有审核相关的 LLM 调用（分类、要素抽取、证据抽取、建议生成）统一 temperature=0，System Prompt 冻结为代码常量（不随用户输入动态拼接，避免 Prompt 注入），输出经 JSON 提取与字段校验，确保同一份合同不同时间审核结论稳定可复现。

### 3.6 误报抑制与精准率保障

赛题要求风险识别「召回率与精准率均不低于 75%」。精准率的核心保障不是事后过滤，而是**把裁决权收回代码**——v6.4 的证据-裁决解耦本身就把误报从 v4 的 466 条压到 37 条。在此之上还有三重保障：

1. **确定性裁决的硬阈值（根本保障）：** 裁决器按 v1.2.4 口径的严格阈值判定，模棱两可的不标（如 3‰/日的违约金不触发 R01、3 年同类竞业排他不触发 R10、有对价买断的 IP 归属不触发 R06），从源头杜绝「LLM 自由发挥」造成的过报。
2. **跨来源置信度融合：** 规则引擎 / LLM 证据裁决独立检出同一风险时交叉验证，上调置信度；仅单来源弱信号的风险置信度较低。
3. **R13 名实不符单列示警：** 名实不符（如名为服务外包实为劳务派遣）属定性判断，规则信号示警、单列报告、不进严格 F1，避免「硬判」引入误报。
4. **建议层 grounding 校验：** 拦截建议中编造的数字/比例/法条，保证修改建议不「信口开河」。

**实测精准率：** 84 份测试集上风险精准率 **76.6%**（TP 121 / FP 37 / FN 30），召回率 **80.1%**，F1 78.3%，精准率与召回率双过 75% 门槛。

### 3.7 审核结果的数字化沉淀

每条风险审核记录以结构化形式存入 `audit_records` 表，包含：风险类型（R01-R13）、风险等级、原文片段 + 位置（clause_position，供前端高亮定位）、判定理由 + 法条引用、修改建议、检测方法（rule/evidence）、**证据链（evidence，LLM 抽取的原始事实）**、**建议层（recommendation：风险说明/修改建议/修改示例/法条依据/grounding 校验结果）**。这使得每条 AI 判断都是可追溯、可审计的——评委和用户可以清楚看到「这条风险的事实证据是什么、按哪条硬阈值裁决、建议依据哪条法条」。完整表结构见第九章。

### 3.8 实测指标与验证方法

| 指标 | 赛题要求 | 实测值 | 验证方法 |
|------|:-----:|:--------:|---------|
| 法理分类准确率 | ≥ 85% | **98.6%**（145/147）| 147 份分类测试集正确分类数 / 总数 |
| 要素抽取 F1 | ≥ 80% | **90.7%**（披露样本加权综合 F1）| 值级严格比对（金额 10% 容差、期限年份交集）|
| 风险识别召回率 | ≥ 75% | **80.1%**（TP 121 / FN 30）| 条款级严格比对（TP = 预测 ∩ gold）|
| 风险识别精准率 | ≥ 75% | **76.6%**（TP 121 / FP 37）| 条款级严格比对（FP = 预测 − gold）|
| 风险类型覆盖 | ≥ 10 类 | 13 类（R01-R13）| 枚举检查 |

逐类型实测（84 份测试集 / 152 条 gold）：

| 类型 | TP/FP/FN | 精准率 | 召回率 | 类型 | TP/FP/FN | 精准率 | 召回率 |
|------|:---:|:---:|:---:|------|:---:|:---:|:---:|
| R01 违约金过高 | 20/0/1 | 100% | 95% | R07 付款失衡 | 0/5/6 | 0% | 0% |
| R02 无限责任 | 15/6/0 | 71% | 100% | R08 验收缺失 | 15/6/10 | 71% | 60% |
| R03 单方解约 | 11/0/1 | 100% | 92% | R09 不可抗力缺失 | 19/1/9 | 95% | 68% |
| R04 管辖不利 | 8/0/0 | 100% | 100% | R10 竞业过宽 | 7/0/0 | 100% | 100% |
| R05 保密期不合理 | 0/10/0 | 0% | 0% | R11 自动续约 | 16/0/0 | 100% | 100% |
| R06 IP 失衡 | 4/7/3 | 36% | 57% | R12 数据隐私 | 6/2/0 | 75% | 100% |

完整实验方案、测试集构成、复现脚本见附录 D。

### 3.9 数据与知识库构成

知识库与测试集均为团队自主构建，已全部落地（非规划）：

**知识库（`backend/ai/knowledge/`，JSON 格式）：**

| 文件 | 内容 | 规模 |
|------|------|:---:|
| `laws.json` | 《民法典》合同编 + 司法解释 + 劳动法/个保法等精编条文，建议层按法条编号精确取用 | 161 条 |
| `risk_cases.json` | 12 类风险的典型案例（合同片段 + 类型 + 等级 + 判定理由 + 参考法条）| ~80 条 |
| `standard_clauses.json` | 11 类合同的标准条款，带 type/priority/depends_on/conflict_with 图论字段 | 结构化 |

**测试集（`backend/evaluate/realtest.json`）：** 84 份合同——32 份真实已签署政府采购合同 + 47 份上市公司公告披露版合同 + 5 份人工构造（4 劳动合同 + 1 保密协议），共 152 条 gold 风险标注（组员背对背标注 + AI 辅助法理审核裁定，口径 v1.2.4）。测试集构成与字段结构详见附录 D。

**知识库冷启动说明：** 建议层的法条检索按「法条编号精确取条文」，不依赖运行时积累的历史数据，首次部署即可完整工作；用户反馈积累的历史审核记录作为后续迭代的补充数据，属加分项而非必需项。

---

## <a id="4"></a>四、核心方案三：标准条款语义比对与缺失检测

### 4.1 问题分析

企业法务通常有一套标准合同模板，逐条比对"对方版本和我们的标准版本差在哪里"是审核中最耗时的工作。传统方案基于关键词匹配，容易出现"标题匹配但内容完全偏离"的漏判。

**技术挑战：** 不同合同的条款标题表述各异（"违约责任""违约条款""违约处理"），条款顺序可能重排，内容可能拆分合并。纯关键词匹配无法应对这种表述多样性。

### 4.2 结构化过滤 + LLM 逐条比对 + 图分析

系统采用「结构化过滤 + LLM 逐条比对」策略（借鉴 PAKTON 的结构化检索思想——按合同类型精确过滤，而非拿类型名做语义检索）：

**第 1 步 — 按类型结构过滤：** 标准条款库自带 `type` 字段，按合同类型精确匹配（含别名映射，如承揽/委托经别名命中服务外包条款）；合同含服务外包业务标签时额外叠加服务外包专属条款（SLA/知识产权/源代码/人员独立性）。类型名不匹配时回退到全部条款，避免比对空白。

**第 2 步 — LLM 逐条比对：** 将标准条款（标题 + 优先级 + 内容摘要）与合同全文送入 LLM，逐条判定覆盖状态——`covered`（已覆盖）/`partial`（部分覆盖，有偏离）/`missing`（缺失），并输出匹配原文、偏离说明、补全建议、风险说明与相关法条。长合同分块并行比对，同一标准条款取覆盖状态最优的一条合并。

**第 3 步 — 跨条款关联风险（图分析）：** 标准条款库带 `depends_on`（前置依赖）与 `conflict_with`（互斥冲突）图论字段，据此做图分析，检测「前置条款缺失导致下游条款失效」与「互斥条款同时存在」两类单条比对发现不了的联动风险（借鉴 GRAPH-GRPO-LEX 的合同图建模与 LegalGraphRAG 的图检索增强思想）。

### 4.3 缺失检测与补全建议

- **缺失检测：** 标准模板中 status=missing 的条款自动列入缺失清单，priority=required 的缺失单独汇总为关键缺失（如不可抗力、验收标准、数据保护）。
- **偏离分析：** status=partial 的条款生成并排对比视图，差异部分高亮标注。
- **补全建议：** 对缺失条款，从模板库调取标准文本，结合合同上下文送入 LLM 生成定制化的补全建议。不仅告诉用户「少了什么」，还直接给可用的文本。

### 4.4 条款模板的可配置化设计

赛题要求「支持加载企业标准条款模板（用户可配置）」。系统预置 11 类法理合同的标准条款模板，其中采购/技术/服务外包类模板已适配网新恒天 IT 服务业务场景（嵌入 SLA 条款、数据安全条款、IP 归属条款等行业特有条款标准文本）：

**条款分级配置：** 每条标准条款标注「强制必选」（priority=required，缺失标高风险）、「推荐可选」（recommended，缺失标中风险）、「禁止出现」（prohibited，出现标高风险），属性在标准条款 JSON 中存储，默认可按企业需求调整。

**图论字段配置：** 每条条款带 `depends_on`（前置依赖）与 `conflict_with`（互斥冲突）字段，支撑 4.2 的跨条款关联风险分析。

**模板版本管理：** 每次模板修改递增版本号，历史版本保留；已审核合同记录审核时的模板版本，支持「使用新模板重新审核」的主动提示，避免自动重审导致的审核结果漂移。

---

## <a id="5"></a>五、核心方案四：用户反馈闭环与审核报告生成

### 5.1 用户反馈闭环（P0）

法务审核 AI 结果后，可对每条风险执行三种操作：
- **确认正确：** 作为正例存入 feedback_log 表，增强对该判断模式的信心
- **标记错误：** 选择错误类型（误报/漏报/等级不准），提交修正意见
- **手动补充：** 添加 AI 未检测到的风险

反馈不立即生效（法律审核要求结果稳定可追溯），而是以结构化形式存入 feedback_log 表，积累 ≥ 50 条后由管理员导出，用于后续模型迭代（更新 Prompt 示例、调整规则阈值、补充风险词典、更新 ChromaDB 向量）。每次更新记录版本号，支持回滚。

> **设计说明：** 反馈数据的用途是"结构化存储，支持导出用于后续模型迭代"——P0 阶段不实现自动触发模型更新，而是提供管理后台的数据导出功能（CSV/JSON），由团队在下一轮模型优化中手动使用。这一定位避免了过度承诺实时自适应能力，同时保留了反馈闭环的完整性。

> **注：** 自定义审查规则、合同版本差异对比、企业知识库管理等进阶功能属于 P2 未来规划，详见第十三章。

### 5.2 审核报告生成（P0）

审核完成后自动生成六章结构化报告：

| 章节 | 内容 | 亮点 |
|------|------|------|
| 一、合同基本信息 | 编号、类型、双方、金额、日期 | 自动提取，无需手动填写 |
| 二、风险评估总览 | 风险热力图 + 高/中/低分布 + 综合评分（0-100）| 30 秒了解全局风险 |
| 三、风险条款逐项分析 | 每条风险附原文定位 + 等级 + 修改建议 + 审核路径 | AI 判断可追溯 |
| 四、条款完整性检查 | 缺失条款清单 + 偏离条款对比 + 补全建议 | 从诊断到治疗闭环 |
| 五、修改建议汇总 | 按优先级排列的操作清单 + 可直接复用的参考条款 | 可执行、可落地 |
| 六、附录 | 审核时间线 + 标准说明 + 审核日志 | 透明可审计 |

报告输出格式：HTML（在线预览）+ PDF（下载）+ Markdown（内部归档）。其中风险热力图将合同每一页的风险密度映射为颜色（绿 → 黄 → 橙 → 红），是 A24 在产品体验层面区别于命令行/纯文本输出的核心优势。

---

## <a id="6"></a>六、系统架构总览

### 6.1 四层技术架构

系统采用经典四层架构：

**第一层 — 前端展示层（Vue 3 + Element Plus）：** 合同上传、审核看板、风险详情、报告展示、反馈标注五个核心页面。ECharts 渲染风险热力图，合同原文用 pdf.js 渲染，风险条目点击跳转原文定位。

**第二层 — 业务后端层（Python FastAPI）：** 文件服务、合同管理、审核调度（后台任务 `_run_audit`）、模板管理、统计服务。JWT 认证 + RBAC 权限控制。

**第三层 — AI 核心引擎层（同进程 Python 模块）：** 审核流水线（解析 → 分类 → 要素抽取 → 规则召回 → 证据抽取 → 确定性裁决 → 建议层 → 条款比对 → 报告）。AI 模块作为普通 Python 包被业务层直接调用，不走 HTTP，省去序列化开销。

**第四层 — 数据持久层：** MySQL 8.0 / SQLite（结构化业务数据）+ ChromaDB（向量化知识库）+ 本地文件系统（原始合同 + 导出报告）。

（完整架构全景图见答辩 PPT）

### 6.2 技术栈选型

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 前端 | Vue 3 + Element Plus + ECharts + pdf.js | 响应式开发、中文文档完善、图表组件丰富 |
| 业务后端 | Python FastAPI + SQLAlchemy | 赛题指定技术栈；与 AI 逻辑同语言，无需服务间通信 |
| 大模型 | DeepSeek（deepseek-chat，temperature=0）| 中文法律文本理解好，temperature=0 保证可复现 |
| 向量数据库 | ChromaDB | 轻量部署，本地嵌入式运行 |
| OCR | PaddleOCR | 中文识别效果好，开源免费 |
| 文档解析 | python-docx + pdfplumber | Python 生态完善 |
| 部署 | Docker Compose（FastAPI + MySQL + ChromaDB）| 一键启动，评委 15 分钟可复现 |

### 6.3 架构关键决策：全栈 Python

赛题明确要求后端使用"Python + FastAPI/Django"，这是基础合规要求。全 Python 方案还带来额外好处：AI 逻辑（文档解析、分类、审核）和业务逻辑（合同管理、用户认证）在同一个 Python 进程中，直接用函数调用代替 HTTP 通信，省掉双服务之间的序列化开销和网络延迟。

**5 人分工：**

| 角色 | 负责范围 | 技术栈 |
|------|---------|--------|
| 同学 A（前端）| 合同上传、列表、详情页 | Vue 3 + Element Plus |
| 同学 B（前端）| 审核结果、报告展示、风险热力图 | Vue 3 + Element Plus + ECharts |
| 同学 C（业务后端）| FastAPI 路由、数据库、认证、审核调度 | Python + FastAPI + SQLAlchemy |
| 同学 D（AI 引擎）| 文档解析、分类、要素抽取、证据裁决 | Python + LLM |
| 同学 E（AI 引擎）| 建议层、条款比对、报告生成 | Python + ChromaDB |

### 6.4 数据安全设计原则

合同是企业的核心商业机密。系统的设计原则是"数据本地处理，支持纯规则降级运行"——文档解析、规则引擎、确定性裁决全部本地完成；云端 LLM 调用仅传脱敏后的条款文本（公司名称替换为"甲方/乙方"，具体金额替换为"XX 万元"），不传整份合同。在无网络或用户拒绝调用 LLM 的场景下，系统可降级为纯规则引擎审核模式（fast），功能受限但仍能检出基础风险。完整安全方案见第十二章。

### 6.5 企业集成接口预留

为适配网新恒天等企业的真实 IT 环境，系统在设计层面预留三项集成接口（V2.0 实现）：

- **企业 OA/合同管理系统对接：** 预留 RESTful API 供外部系统推送合同文件并获取审核结果，接口格式与现有 `/api/contracts/upload` 和 `/api/contracts/{id}/audit-result` 一致，仅增加 API Key 认证头。
- **单点登录（SSO）集成点：** 用户认证模块通过 FastAPI Depends 注入，预留 OAuth2/OIDC 接口，可对接企业统一身份认证系统（如 LDAP、CAS），无需修改业务逻辑代码。
- **企业知识库对接：** ChromaDB 向量检索层设计为可替换的 Provider 接口，V2.0 可对接企业现有知识库系统，仅需实现 `search(query) -> List[Document]` 接口即可。

---

## <a id="7"></a>七、创新亮点总结

A24 的亮点策略是"一硬一软"双核心 + 配套支撑——集中火力打透两个记忆点，其他能力作为配套。

### 7.1 硬核技术创新：证据-裁决解耦（LLM 只抽事实 + 代码确定性裁决）★

A24 的核心技术壁垒是**「证据-裁决解耦」架构**——把「有没有风险」和「怎么改」彻底拆开：LLM 只负责从合同原文中抽取客观事实证据（不判断风险、不下法律结论），风险判定完全交给 Python 代码按 v1.2.4 口径的硬阈值裁决。

**为什么这能根治「幻觉」？** 直接让 LLM 判风险，它会「编」出不存在的条款（v4 过报 466 条，精准率仅 24%）；把阈值写进 Prompt，又会误杀召回（v5 召回率掉到 73.5%）。证据-裁决解耦后，LLM 只做它擅长的语义理解（抽事实），裁决交给稳定可复现的代码（硬阈值），精准率从 24% 拉到 76.6%，同时召回率守住 80.1%。

**核心记忆点（答辩用）：** "我们不问 AI『这条有没有风险』——AI 一判风险就爱编。我们只让 AI 做笔录（抽取条款事实），然后让代码当法官（按法条阈值一锤定音）。AI 记错了，法官核对原文就能发现；AI 判错了，没法核对。这就是从 24% 精准率到 76.6% 的关键。"

**配套创新：** ①建议层 grounding——AI 生成的修改建议里的每个数字、比例、法条都必须能在证据/法条里找到来源，杜绝编造；②图分析条款比对——用 depends_on/conflict_with 图论字段发现「前置条款缺失」「互斥条款并存」等单条比对发现不了的联动风险。

**关键技术指标（实测）：** 精准率 76.6%、召回率 80.1%（84 份真实测试集，详见 3.8 节和附录 D）。

**论文创新点落地全景：** 系统参考多篇前沿论文，关键思想逐一落地（未落地的如实标注，不夸大）：

| 论文 | 核心思想 | 项目落地 |
|------|---------|---------|
| Self-Consistency | 采样多样推理路径 + 多数投票 | 分类分块独立判 + 多数投票（第二章）|
| Lost-in-the-Middle | 长上下文首尾好、中间差 | 首尾块必含 + 中间采样；补全条款独立切出（第二/三章）|
| KATE / kNN-LM | kNN 检索相似样本作 in-context 示例、显式记忆 | RAG 分类器检索范本作少样本示例（第二章）|
| RAG / REALM | 检索增强 + provenance 可溯源 | 建议层按法条编号精确取 + grounding（第三章）|
| GRAPH-GRPO-LEX | 合同图建模 + contract linter 静态检查 | 条款比对 + 缺失检测 + depends_on/conflict_with 跨条款依赖/互斥（第四章）|
| RCBSF | Leader-Follower + 5 维风险约束 + 消除幻觉 | 改条款多智能体；建议层 5 维结构化约束（第三/五章）|
| PEAT-LLM4LCR | 提示工程 + Agent + 可解释可审计 | 证据抽取 schema + 证据链 + 法条依据 + 结构化报告（第三章）|
| LLM 司法·道德感知 | 不可量化不确定性交人工、不硬判 | R13 名实不符单列示警、不进 F1、定性交人工（第三章）|
| Sentence-BERT | 语义句向量检索 | text2vec 范本向量化检索底座（第二/四章）|
| HAN | 层次注意力网络 | 未落地（保留段落层级结构，未引入神经网络分类）|

> 其中 4 个创新点尤其值得一提：
> ① **不确定性分层处理**：R13「名实不符」涉及「名义 vs 实质」的定性判断、AI 不可量化，故单列示警、不进 F1、定性交人工复核——对应「不可量化的不确定性要保留、不强行量化」的思想；
> ② **建议层 5 维约束**：风险说明 / 修改建议 / 修改示例 / 法条依据 / grounding 校验，天然对应 RCBSF 的 5 维风险约束（Category / Location / Evidence / Issue / Suggestion）；
> ③ **全程可审计**：每条风险可追溯「抽了什么事实 → 按哪条阈值裁决 → 依据哪条法条 → 建议怎么改」，由 evidence / adjudicate / legal_basis / recommendation 四字段串起，对应「透明 reasoning chain + 可溯源（provenance）」；
> ④ **contract linter 类比**：条款比对 + 缺失检测 + 跨条款依赖/互斥风险，本质是把软件工程的 linter（静态检查）搬到合同上，对应 GRAPH-GRPO-LEX 的 contract linter 思想。

### 7.2 产品体验创新：风险热力图 + 原文一键跳转

"上传合同 → 30 秒出热力图 → 红色区域一眼看到风险在哪 → 点击风险条目自动跳转原文高亮定位"——这是 A24 在产品体验层面区别于命令行/纯文本输出的核心优势。

热力图将合同每一页的风险密度映射为颜色（绿 → 黄 → 橙 → 红），配合风险一键跳转原文定位（点击任一条审核结果，合同原文面板自动滚动到对应条款并高亮显示）和证据链与建议层的逐条追溯，形成"看到风险 → 一键跳转原文 → 理解 AI 判断逻辑"的三步交互闭环。

**pdf.js 降级方案：** P0 底线为点击页码跳转 + 侧栏文本展示，精确字符级高亮定位作为 P1 冲刺项——详见第六章架构设计。

### 7.3 配套技术支撑

| 能力 | 作用 | 对应章节 |
|------|------|---------|
| 分块采样投票分类 | 首尾块必含 + 中间等距采样 + 多数投票，抗长文本信息丢失 | 第二章 |
| 确定性硬阈值裁决 | 代码按 v1.2.4 口径裁决，模棱两可不标，从源头抑制误报 | 第三章 |
| 建议层 grounding | 拦截建议中编造的数字/比例/法条 | 第三章 |
| 图分析条款比对 | depends_on/conflict_with 检测跨条款联动风险 | 第四章 |
| 条款补全建议 | 不仅告诉「少了什么」，还直接给可用标准文本 | 第四章 |

### 7.4 赛题鼓励创新方向落地

赛题第 7 节明确鼓励三个创新方向，本系统均已落地实现：

**多轮对话式合同修改（RCBSF 多智能体）：** 审核结果页嵌入改条款功能，支持自然语言指令修改条款（如「将违约金改为以实际损失为限」），采用 Leader-Follower 多智能体架构（借鉴 RCBSF 的 Stackelberg 博弈思想）——Leader 生成修改草案、Follower 从法务角度审校，结合法条检索结果输出修改前后对比。

**知识图谱辅助跨条款关联风险：** 标准条款库带 depends_on/conflict_with 图论字段，检测「前置条款缺失导致下游失效」「互斥条款并存」两类跨条款联动风险（借鉴 GRAPH-GRPO-LEX 合同图建模与 LegalGraphRAG 图检索增强）。

**印章/签名区域定位：** 文档解析模块集成 PaddleOCR，实现印章/签名的检测与位置标注。

### 7.5 答辩叙事建议（三个场景）

**场景一（给评委看 — 技术深度）：** "一份 300 万的 IT 技术外包合同，系统先让 LLM 抽取 13 类条款的客观事实证据——违约金比例、赔偿范围、验收机制、竞业期限……然后 Python 裁决器按法条硬阈值逐条裁决：发现违约金日利率 1%（触发 R01）、赔偿范围含预期利益（触发 R02）、竞业条款全国范围 5 年（触发 R10）。每条风险都能追溯到『抽了什么事实 → 按哪条阈值裁决 → 依据哪条法条』。建议层再按法条生成修改建议，grounding 校验保证不编数字。整个流程可复现、可审计，这是纯 LLM 直判做不到的。"

**场景二（给企业看 — 商业价值）：** "网新恒天的法务团队每年审核数千份 IT 服务合同。按每份平均 2 小时算，AI 辅助下缩短到 15 分钟（人工复核），相当于节省了 80% 以上的人力投入——更重要的是，所有审核知识沉淀在系统中，不再依赖个别法务的个人经验。"

**场景三（给用户看 — 产品体验）：** "网新恒天的李律师休年假的第二天，同事接手了一份紧急的技术服务合同。打开系统搜索上个月类似合同的审核记录，AI 的标注、风险证据链、修改建议、最终决策，一目了然。5 分钟后审核意见发出——客户甚至没发现李律师不在。"

---

## <a id="8"></a>八、开发历程与里程碑

系统按「线下设计 → 线上开发 → 评测冲刺」三阶段推进，现已全部完成并交付：

| 阶段 | 关键成果 |
|------|---------|
| 线下设计周 | 需求定稿、技术方案评审、FastAPI 骨架 + 登录注册联调、Vue 3 前端框架搭建 |
| 线上开发 | P0 全流程跑通：上传 → 解析 → 分类 → 要素抽取 → 规则召回 → 证据裁决 → 建议层 → 条款比对 → 报告 |
| 评测冲刺 | 分类/要素/风险测试集落地，三大硬指标全部达标（分类 98.6% / 要素 F1 90.7% / 风险 P 76.6% + R 80.1%）|

**架构演进（关键）：** 风险引擎经过多轮对照实验迭代——v4（LLM 泛化 Prompt 直判，P 24%）→ v5（严格 Prompt，R 73.5%）→ v6.4（证据抽取 + 确定性裁决，P 76.6% / R 80.1%）→ v6.5（建议层）。每一次迭代都由真实测试集的精准率/召回率数据驱动，详见第三章 3.4 节与附录 D。

**MVP 交付闭环（全部完成）：**
1. 合同上传 + 文档解析 + OCR
2. LLM 分类（11 类）+ 要素抽取
3. 规则召回 + 证据抽取 + 确定性裁决 + 建议层
4. 标准条款比对 + 跨条款关联风险
5. 审核报告生成 + ECharts 风险热力图
6. Web 可视化界面 + 反馈标注

---

# 第二部分：工程实现篇

---

## <a id="9"></a>九、数据库设计

### 9.1 存储架构总览

系统采用"MySQL + ChromaDB + 文件系统"三层存储：

| 存储层 | 技术 | 存储内容 | 特点 |
|--------|------|---------|------|
| MySQL 8.0 | 关系型数据库 | 用户、合同、审核记录、报告、模板、反馈日志 | 结构化、事务支持、关联查询 |
| ChromaDB | 向量数据库 | 风险案例向量、标准条款向量、法律法规向量、审核经验向量 | 语义相似度检索 |
| 文件系统 | 本地磁盘 | 原始合同文件、上传图片、导出报告 | 大文件、二进制数据 |

### 9.2 核心表设计（8 张表）

**表 1：users（用户表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 用户 ID |
| username | VARCHAR(50) UNIQUE | 用户名 |
| password_hash | VARCHAR(255) | bcrypt 哈希 |
| role | VARCHAR(20) | 基础角色（P0 仅区分 admin/user，完整 RBAC 为 V2.0） |
| is_active | BOOLEAN | 账号启用状态 |

**表 2：contracts（合同主表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 合同 ID |
| user_id | BIGINT FK | 上传用户 |
| file_name | VARCHAR(500) | 原始文件名 |
| contract_type | VARCHAR(50) | 分类结果 |
| type_confidence | FLOAT | 分类置信度 |
| parsed_text | LONGTEXT | 解析全文 |
| extracted_elements | JSON | 要素抽取结果（JSON 设计理由：要素字段随合同类型动态变化，如采购合同有"付款条件"、劳动合有"试用期"，JSON 灵活适应异构结构） |
| status | VARCHAR(20) | uploaded / parsing / auditing / completed |
| audit_mode | VARCHAR(10) | fast / precise |
| template_version | INT | 审核时使用的模板版本号（用于审核追溯） |

**表 3：audit_records（风险审核记录）** — 核心业务表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 记录 ID |
| contract_id | BIGINT FK | 关联合同 |
| audit_batch | VARCHAR(36) | 审核批次号（UUID），同一合同多次审核按批次区分 |
| risk_type | VARCHAR(50) | R01-R12 |
| risk_level | VARCHAR(10) | high / medium / low |
| clause_text | TEXT | 被标记原文 |
| clause_position | JSON | 文档位置（JSON 设计理由：不同格式合同的位置描述异构——PDF 用页码、行号，DOCX 用段落序号，JSON 统一适配） |
| reason | TEXT | 判定理由（含法条引用）|
| suggestion | TEXT | 修改建议 |
| detection_method | VARCHAR(30) | rule / evidence |
| confidence | FLOAT | 置信度 0-1 |
| evidence | JSON | 证据链（LLM 抽取的原始事实证据，13 类字段）|
| recommendation | JSON | 建议层（风险说明/修改建议/修改示例/法条依据/grounding 校验结果）|
| feedback_status | VARCHAR(20) | pending / confirmed / corrected / disputed |

**表 4：audit_reports（审核报告）** — 与 audit_batch 一对一，同一合同可有多份报告

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 报告 ID |
| contract_id | BIGINT FK | 关联合同 |
| audit_batch | VARCHAR(36) UNIQUE | 关联审核批次（每次审核生成新报告，不覆盖历史） |
| report_html | LONGTEXT | HTML 报告 |
| risk_score | INT | 综合风险评分 0-100 |
| high_risk_count | INT | 高风险数 |
| mid_risk_count | INT | 中风险数 |
| low_risk_count | INT | 低风险数 |
| risk_heatmap_data | JSON | 热力图数据 |
| missing_clauses | JSON | 缺失条款列表 |
| created_at | DATETIME | 报告生成时间 |

**表 5：templates（标准条款模板）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 模板 ID |
| name | VARCHAR(200) | 模板名称 |
| contract_type | VARCHAR(50) | 适用合同类型 |
| clauses | JSON | 条款列表 [{title, content, priority: required/recommended/prohibited}] |
| is_builtin | BOOLEAN | 是否系统内置 |
| version | INT | 版本号 |
| previous_version_id | BIGINT | 上一版本 ID（版本链追溯） |

**表 6：feedback_log（用户反馈日志）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 日志 ID |
| record_id | BIGINT FK | 关联审核记录 |
| user_id | BIGINT FK | 反馈用户 |
| action_type | VARCHAR(20) | confirm / correct / miss / add / dispute |
| original_risk | JSON | AI 原始判断 |
| corrected_risk | JSON | 用户修正后 |
| comment | TEXT | 用户备注 |

**表 7：custom_rules（自定义审查规则）** — V2.0 规划，预留表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 规则 ID |
| name | VARCHAR(200) | 规则名称 |
| rule_type | VARCHAR(50) | 检查字段（penalty_ratio / contract_amount / court_location 等） |
| condition | JSON | 条件定义 {operator, threshold} |
| risk_level | VARCHAR(10) | 触发后的风险等级 |
| contract_type | VARCHAR(50) | 适用合同类型（NULL = 全部） |
| is_active | BOOLEAN | 启用状态 |
| created_by | BIGINT FK | 创建人 |

**表 8：knowledge_base（知识库元数据）** — V2.0 规划，预留表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 知识条目 ID |
| title | VARCHAR(500) | 条目标题 |
| content | TEXT | 原始文本 |
| source_type | VARCHAR(50) | law / case / template / custom |
| tags | JSON | 标签 |
| chroma_id | VARCHAR(100) | ChromaDB 中的向量 ID |
| uploaded_by | BIGINT FK | 上传人 |
| created_at | DATETIME | 创建时间 |

完整建表 SQL（含索引、外键、预置数据）见项目仓库 `backend/schema.sql`。

### 9.3 ChromaDB 向量数据库设计

| Collection | 内容 | 预置量 | 向量维度 | 检索参数 |
|:---|------|:---:|:---:|:---|
| risk_cases | 12 类风险典型案例 | ~80 条 | 1024 | Top-K=5, 阈值=0.70 |
| standard_clauses | 11 类合同标准条款 | 结构化 | 1024 | Top-K=5, 阈值=0.70 |
| laws | 《民法典》合同编 + 司法解释精编 | 161 条 | 1024 | Top-K=5, 阈值=0.70 |
| audit_history | 用户确认的历史审核记录 | 运行时积累 | 1024 | Top-K=5, 阈值=0.70 |

> 说明：建议层主流程按「法条编号精确取条文」（`laws.json`），不依赖向量检索兜底；ChromaDB 向量化主要用于改条款等辅助场景的知识检索。

### 9.4 数据安全等级

| 等级 | 内容 | 存储 | 加密 |
|:---:|------|------|------|
| L3 高敏 | 合同原文、用户密码 | MySQL（密码 bcrypt）| AES-256（合同可选）+ TLS 传输 |
| L2 中敏 | 审核记录、要素摘要 | MySQL | TLS 传输 |
| L1 低敏 | 模板、统计报表 | MySQL | TLS 传输 |

---

## <a id="10"></a>十、API 接口设计

### 10.1 接口总览

**认证：**
- `POST /api/auth/register` — 用户注册
- `POST /api/auth/login` — 用户登录（返回 JWT Token，24h 有效）

**合同管理：**
- `POST /api/contracts/upload` — 上传合同（multipart，支持批量）
- `GET /api/contracts` — 合同列表（分页、类型/状态筛选、搜索）
- `GET /api/contracts/{id}` — 合同详情（含要素、段落结构）
- `DELETE /api/contracts/{id}` — 删除合同（admin 权限）

**审核：**
- `POST /api/contracts/{id}/audit` — 触发审核（body: {mode: "fast"|"precise"}，异步执行）
- `GET /api/contracts/{id}/audit-status` — 审核进度查询
- `GET /api/contracts/{id}/audit-result` — 审核结果（含全部 risk 列表 + 证据链 + 建议层）

**报告：**
- `GET /api/contracts/{id}/report` — 获取报告（HTML）
- `GET /api/contracts/{id}/report/pdf` — 下载报告（PDF）
- `GET /api/contracts/{id}/heatmap` — 风险热力图数据

**反馈：**
- `POST /api/feedback` — 提交反馈标注
- `GET /api/feedback/stats` — 反馈统计

**模板：**
- `GET /api/templates` — 模板列表
- `POST /api/templates` — 创建/更新模板

### 10.2 核心接口示例

**触发审核：**
```
POST /api/contracts/42/audit
Body: {"mode": "precise"}

Response: {
  "contract_id": 42,
  "status": "auditing",
  "audit_mode": "precise",
  "pipeline_stage": "classification",
  "estimated_time": 25
}
```

**审核结果（精简版）：**
```
GET /api/contracts/42/audit-result

Response: {
  "contract_type": "采购合同",
  "type_confidence": 0.94,
  "audit_mode": "precise",
  "elements": {
    "party_a": {"name": "杭州科技有限公司", "role": "买方"},
    "party_b": {"name": "上海软件有限公司", "role": "卖方"},
    "amount": {"value": 500000, "currency": "CNY"}
  },
  "risks": [
    {
      "risk_type": "R01", "risk_level": "high",
      "clause_text": "任何一方违约需支付合同金额30%作为违约金",
      "reason": "违约金比例30%超过法定上限...",
      "detection_method": "corex_review",
      "confidence": 0.94,
      "corex_agents": ["legal✓", "compliance✓", "finance✓", "self_qa✓"]
    }
  ],
  "missing_clauses": ["不可抗力条款", "保密期限条款"],
  "risk_score": 72
}
```

完整 API 文档（含请求/响应 JSON Schema）由 FastAPI 自动生成 Swagger 页面（`/docs`），开发期间可实时查看和测试。

### 10.3 边界场景处理

系统在以下边界和异常场景下有明确的处理策略：

| 边界场景 | 处理策略 | 用户提示 |
|---------|---------|---------|
| 合同内容为空或无法解析 | 前端上传时校验文件大小 > 0；后端解析失败返回错误码 PARSING_ERROR | "文件解析失败（文件可能为空或格式不支持），请检查文件后重试" |
| 合同分类置信度 < 0.5 | 默认归类为"其他合同"类型，不阻塞后续流程；审核报告中标注"类型识别置信度低，建议人工确认" | "合同类型自动识别为'其他合同'（置信度较低），请在详情页手动选择类型以获得更精准的审核" |
| 审核未检出任何风险 | 审核报告显示"本次审核未发现风险条款"，同时标注"AI 审核无法替代专业法律判断，建议人工复核"——避免用户误以为"零风险 = 绝对安全" | "本次审核未发现风险条款。请注意：AI 审核结果仅供参考，建议进行人工复核" |
| 超大合同（> 200 页）超出 LLM 上下文 | 按章切分合同，分批送入 LLM 审核，每章审核结果汇总后统一生成报告。切分边界以"第X章"或空行分隔符为准 | "检测到合同篇幅较长，系统将分章节审核（共 X 章），审核时间可能延长" |
| LLM API 全部不可用 | 降级为纯规则引擎模式（fast），仅执行规则检测，证据裁决与建议层暂不可用。审核报告标注"快速检测模式（规则引擎），建议网络恢复后重新完整审核" | "云端 AI 服务暂时不可用，系统已切换为本地快速检测模式。检测范围有限，建议稍后重新进行完整审核" |

---

## <a id="11"></a>十一、部署与运维

### 11.1 Docker Compose 一键部署

系统包含 4 个容器：MySQL 8.0 + ChromaDB + FastAPI 后端 + Vue 前端（Nginx 静态服务）。评委从零到可演示约 15 分钟：

```bash
export DEEPSEEK_API_KEY=sk-your-key
docker-compose up -d
# 前端: http://localhost:3000
# 后端文档: http://localhost:8080/docs
```

### 11.2 低配置运行方案

| 组件 | CPU 方案 | 影响 |
|------|--------|------|
| OCR | PaddleOCR mobile 配置 | 速度稍慢 |
| LLM | 使用云端 API，不占本地算力 | 无影响 |
| 向量检索 | ChromaDB 本地嵌入式 | 无额外服务依赖 |

最低配置：4 核 CPU + 8GB RAM + 20GB 磁盘，可运行完整功能。

### 11.3 开源模型降级方案

当 DeepSeek API 不可用时，可切换为本地开源模型：

| 组件 | 默认 | 开源降级 | 切换方式 |
|------|:---:|------|------|
| LLM 分类/抽取/证据/建议 | DeepSeek（deepseek-chat）| Qwen2.5-7B-Instruct（vLLM 本地部署）| 修改 config.py 模型名 |

### 11.4 统一开发环境

为确保 5 人团队开发环境一致、避免"我这能跑你那报错"，团队统一以下环境配置：

**基础环境：**

| 组件 | 版本 | 用途 | 安装方式 |
|------|:---:|------|---------|
| Python | 3.11.x | 后端 + AI 引擎 | pyenv 或官网安装（3.10+ 均可，3.11 推荐） |
| Node.js | 20.x LTS | 前端构建 | nvm 管理版本 |
| MySQL | 8.0.x | 结构化数据存储 | Docker 容器或本地安装 |
| Docker | 24.x+ | 容器化部署 | Docker Desktop 或 Engine |
| Git | 2.40+ | 版本管理 | 系统包管理器 |

**Python 依赖（requirements.txt）：**

后端核心：fastapi、uvicorn、sqlalchemy、pydantic、python-jose（JWT）、passlib[bcrypt]、python-multipart、httpx、openai（DeepSeek API）。

AI 引擎：openai、chromadb、pdfplumber、python-docx、paddleocr、paddlepaddle（OCR）。

**前端依赖（package.json）：**

vue==3.4.0、element-plus==2.7.0、echarts==5.5.0、pdfjs-dist==4.0.0、axios==1.7.0、vite==5.2.0。

**环境搭建检查清单（新人入职 15 分钟完成）：**

1. `git clone` 项目仓库
2. `cp .env.example .env` 填入 DeepSeek API Key
3. `pip install -r requirements.txt --break-system-packages`（Linux）/ `pip install -r requirements.txt`（Windows）
4. `cd frontend && npm install`
5. `docker-compose up -d mysql chromadb`（仅启动数据库容器）
6. `python backend/main.py` 启动后端（开发模式）
7. `cd frontend && npm run dev` 启动前端（开发模式）
8. 访问 `http://localhost:5173` 确认前后端联通

以上检查清单与《A24-开工准备指南.md》配套使用，确保每位团队成员在 15 分钟内完成开发环境搭建。

---

## <a id="12"></a>十二、安全与企业适配

### 12.1 认证与基础权限（P0）

JWT Token 认证：用户登录后签发 24h 有效 Token，存前端内存（非 localStorage，防 XSS）。P0 阶段仅区分 admin（管理用户和模板）和 user（上传审核查看）两级角色。完整 RBAC 三级权限体系（admin/legal/viewer）列入 V2.0。

### 12.2 数据安全

- **合同原文：** 本地 MySQL 存储，云端 LLM 调用前自动脱敏（公司名 → 甲方/乙方，金额 → XX 万元），仅传条款片段，不传整份合同
- **用户密码：** bcrypt（cost=12）哈希，不可逆
- **API Key：** 环境变量注入，不入库、不入 Git
- **传输加密：** TLS 1.3（HTTPS）

### 12.3 审计日志（V2.0 规划）

关键操作（登录、上传、审核触发、报告导出、反馈提交）自动记录审计日志。完整审计日志功能（含操作人、时间、IP、操作详情、管理后台筛选查看）列入 V2.0。

### 12.4 企业部署模式

| 模式 | 适用场景 | 配置 |
|------|---------|------|
| 单机 Docker | 中小企业 1-5 人法务团队 | docker-compose up -d |
| 内网私有化 | 大型企业数据不出内网 | LLM 切换本地 Qwen2.5-7B |
| 云端 SaaS | 多租户按需付费 | 增加租户隔离 + 用量计费（V2.0）|

---



---

## <a id="13"></a>十三、V2.0 未来规划

以下功能已完成需求分析和概要设计，因三周开发周期限制，列入 V2.0 开发计划。答辩时可说明"已完成设计，列入下一阶段规划"，体现方案的前瞻性。

### 13.1 功能增强类

| 功能 | 当前状态 | V2.0 规划 | 优先级 |
|------|---------|----------|:---:|
| 自定义审查规则引擎 | 表结构已预留（custom_rules 表）| 可视化规则配置器 + 规则市场 | 高 |
| 合同版本差异对比 | 概要设计完成 | diff-match-patch + LLM 语义分析 | 高 |
| 企业知识库管理 | 表结构已预留（knowledge_base 表）| 知识库管理后台 + 自动向量化 | 中 |
| 多轮对话合同修改 | 初赛 demo 可演示单条款修改 | 完整自然语言指令理解 + 多条款联动 | 中 |
| RBAC 三级权限 | 概要设计完成 | 完整 admin/legal/viewer + 细粒度接口鉴权 | 中 |
| 完整操作审计日志 | 概要设计完成 | 审计日志存储 + 管理后台查询 + 导出 | 中 |

### 13.2 风险引擎增强类

| 功能 | 当前状态 | V2.0 规划 | 优先级 |
|------|---------|----------|:---:|
| 风险规则自定义配置 | 13 类规则硬编码于裁决器 | 可视化规则配置界面 + 阈值可调 | 高 |
| 多源知识检索 | 建议层按法条编号精确取条文 | 增加案例库/裁判文书语义检索 | 中 |
| 裁决口径扩展 | v1.2.4 口径（R01-R13）| 覆盖更多风险类型 + 行业专属规则 | 低 |

### 13.3 创新探索类

| 功能 | 当前状态 | V2.0 规划 | 优先级 |
|------|---------|----------|:---:|
| 知识图谱跨条款关联分析 | 已实现 depends_on/conflict_with 图分析 | 实体关系抽取 + Neo4j 可视化 + 跨条款推理 | 低 |
| 印章/签名区域定位与合规检查 | OCR 印章二元判断 | YOLO 印章检测 + 位置合规分析 + 视觉标注 | 低 |
| 企业 SSO 单点登录 | 接口已预留 | OAuth2/OIDC 对接企业统一认证 | 中 |

### 13.4 体验优化类

- 移动端适配（PWA）
- 合同模板市场（内置 20+ 行业标准模板）
- 批量合同审核（一次上传多份，并行审核）
- 风险趋势分析仪表盘（企业维度风险统计看板）

---

> **V2.0 规划的定位：** 以上功能说明 A24 具有清晰的演进路径，不是"做完就扔"的比赛项目。初赛阶段聚焦 P0/P1 功能实现完整演示闭环，V2.0 规划证明团队对产品的长期思考。



---

# 第三部分：附录

---

## <a id="14"></a>附录A：项目目录结构

### A.1 总体结构

项目仓库 `a24-contract-auditor/` 按前后端分离组织，AI 模块作为后端子包，部署通过 Docker Compose 统一编排。

### A.2 前端目录 (`frontend/`)

采用 Vue 3 + Element Plus 标准项目结构，Vite 构建。

`src/views/` 下包含五个核心页面：`Upload.vue`（合同上传）、`ContractList.vue`（合同列表，含筛选搜索分页）、`ContractDetail.vue`（合同详情，含要素展示和原文面板）、`AuditResult.vue`（审核结果，含风险列表、证据链与建议展示）、`ReportView.vue`（审核报告，含 ECharts 热力图和 pdf.js 原文高亮）。`src/components/` 下抽取通用组件：`RiskBadge.vue`（风险等级徽章）、`ClauseHighlighter.vue`（条款高亮定位）、`FeedbackPanel.vue`（反馈标注面板）、`HeatmapChart.vue`（热力图封装）。`src/api/` 按模块封装 Axios 请求（auth / contract / audit / report / feedback / template），统一拦截 JWT Token 注入和 401 跳转。

### A.3 后端目录 (`backend/`)

FastAPI 应用根目录。`main.py` 为应用入口，注册路由和中间件。`config.py` 集中管理环境变量（数据库连接、LLM API Key、模型路径等）。

`routers/` 下按业务模块拆分路由文件：`auth.py`（注册登录）、`contracts.py`（CRUD + 上传）、`audit.py`（审核触发 + 进度 + 结果）、`reports.py`（报告获取 + PDF 导出）、`feedback.py`（标注提交）、`templates.py`（模板管理）。`models/` 下为 SQLAlchemy ORM 模型（user / contract / audit_record / audit_report / template / feedback_log），与第九章 6 张表一一对应。`schemas/` 下为 Pydantic 请求/响应模型，用于 FastAPI 自动文档生成和输入校验。

`services/` 为业务逻辑层：`auth_service.py`（JWT 签发 + bcrypt 校验）、`contract_service.py`（合同生命周期管理）、`audit_service.py`（审核流程编排，调度文档解析 → 分类 → 抽取 → 风险检测 → 报告生成流水线）、`report_service.py`（报告模板渲染）、`template_service.py`（模板 CRUD + 版本管理）。

### A.4 AI 模块目录 (`backend/ai/`)

AI 核心引擎作为 `backend/ai/` 的子包，按职责分目录：

- `parser/`：多格式文档解析（docx / pdf / ocr）
- `classifier/`：纯 LLM 法理分类（11 类 + 服务外包标签，分块采样投票）
- `extractor/`：LLM 要素抽取（双方/金额/期限/争议解决）
- `auditor/`：审核核心——`rule_engine.py`（规则召回）、`evidence_extractor.py`（LLM 事实证据抽取）、`evidence_adjudicator.py`（确定性裁决）、`recommendation_engine.py`（建议层）
- `matcher/`：标准条款比对（结构化过滤 + LLM 逐条比对 + 图分析）
- `reporter/`：报告数据组装 + 热力图数据
- `rag/`：ChromaDB 向量检索封装（懒加载，不拖慢启动）
- `knowledge/`：预置知识 JSON（laws.json / risk_cases.json / standard_clauses.json）
- `reviser.py`：多轮对话式改条款（RCBSF 多智能体）
- `taxonomy.py`：分类体系唯一真源（分类器/条款比对/前端共用）

### A.5 部署目录 (`deploy/`)

`docker-compose.yml`（4 容器编排：MySQL + ChromaDB + FastAPI + Nginx）、`Dockerfile.backend` / `Dockerfile.frontend`、`init.sql`（建表 + 预置数据）、`.env.example`（配置模板）。

### A.6 文档目录 (`docs/`)

设计文档、API 文档、部署文档、用户手册。

---

## <a id="15"></a>附录B：技术风险与应对

### B.1 证据-裁决架构的工程风险

证据-裁决解耦架构的核心风险及应对方案：

| 风险 | 概率 | 影响 | 应对方案 | 降级策略 |
|------|:---:|:---:|---------|---------|
| LLM API 调用超时/限流 | 中 | 证据抽取/建议生成中断 | 每层调用设 30s 超时；证据正文与补全条款并行、建议层批量调用 + 重试 2 次 | 证据抽取失败回退规则引擎（fast 基线）；建议层失败回退护栏模板兜底 |
| LLM 输出格式不兼容 | 中 | 证据字段缺失 | 证据抽取用冻结 JSON Schema 约束；输出经 JSON 提取 + 字段校验 | 字段缺失时裁决器按空值处理，宁缺毋滥（不臆测）|
| 模型输出幻觉 | 中 | 建议层编造数字/法条 | 裁决层不依赖 LLM 判断（代码硬阈值）；建议层 grounding 校验拦截编造的数字/比例/法条 | grounding 未通过的内容不展示，回退护栏方向 |
| 审核耗时过长（precise）| 中 | 用户等待焦虑 | 分类/要素并行、证据正文与补全条款并行、条款比对分块并行 | 用户可切 fast 模式（纯规则，秒级）|

### B.2 其他技术风险

| 风险 | 概率 | 影响 | 应对方案 |
|------|:---:|:---:|---------|
| PDF 扫描件 OCR 质量差 | 中 | 分类和审核结果不可靠 | 扫描质量预检（DPI < 150 提示重新上传）；OCR 置信度标注 |
| 服务器性能不足（答辩现场网络差）| 中 | Demo 演示卡顿 | LLM 响应预缓存典型合同审核结果；fast 模式本地规则引擎不依赖网络 |

---

## <a id="16"></a>附录C：鼓励创新方向与 MVP 分阶段实现

### C.1 赛题第 7 节鼓励创新的三个方向

赛题在第 7 节"其他"中明确鼓励三个创新方向。以下逐一分析实现方案和初赛/决赛阶段规划：

**方向一：多轮对话式合同修改建议**

实现方案：在审核结果页面嵌入对话面板，用户可通过自然语言指令修改合同条款（如"将第 8 条违约金上限改为合同金额的 20%"），系统解析指令 → 定位条款 → 调用 LLM 生成修改文本 → 展示修改前后对比。技术路线：指令解析用 LLM + Function Calling（定义 modify_clause / delete_clause / add_clause 三个 function），条款定位复用第四章的语义对齐能力。

MVP 分阶段：初赛阶段（7.15-8.3）完成对话面板 UI 框架 + 单条款修改 demo（指定条款编号 + 简单数值修改）；决赛阶段（如晋级）实现完整的自然语言指令理解 + 多条款联动修改 + 修改历史回溯。

**方向二：知识图谱辅助跨条款关联风险分析**

实现方案：从合同中抽取实体（甲方、乙方、金额、日期、管辖法院等）和关系（付款方、收款方、管辖地等），构建轻量级合同知识图谱。利用图谱推理发现跨条款的隐含风险——例如"争议解决条款约定北京仲裁，但管辖条款约定杭州法院"，或"保密期限超出合同履行期限 3 年以上"。技术路线：实体关系抽取用 LLM Few-shot Prompting，图谱存储用 Neo4j（轻量部署）或直接 JSON 表示，关联规则用预定义模板（跨条款冲突模式库）。

MVP 分阶段：初赛阶段仅做预定义关联规则检查（如"违约金条款 → 合同金额条款"金额一致性校验），答辩 PPT 中展示知识图谱概念设计；决赛阶段实现完整的实体关系抽取 + Neo4j 可视化图谱 + 跨条款推理。

**方向三：扫描件印章/签名区域定位与合规性检查**

实现方案：利用 PaddleOCR 的印章检测能力或训练轻量 YOLO 印章检测模型，定位合同扫描件中的印章和签名区域。检查项包括：是否缺少公章、签名是否置于正确位置（盖章处/签字处）、印章是否覆盖正文文字（合规性问题）。技术路线：印章检测用 PaddleOCR 内置印章识别 + OpenCV 红章颜色过滤；签名检测用目标检测模型定位手写体区域。

MVP 分阶段：初赛阶段在文档解析模块中集成 PaddleOCR 印章检测 API，实现"是否有印章"的二元判断，结果展示在合同详情页；决赛阶段实现印章位置合规分析 + 签名区域定位 + 视觉标注展示。

### C.2 MVP 最低交付标准（重申）

如果时间紧张，初赛阶段至少确保以下功能可演示，并在答辩 PPT 中标注其他功能为"已完成设计、开发中"：

- 合同上传 + 文档解析 + OCR（基础功能）
- LLM 分类（11 类）+ 要素抽取（赛题指标 1、2）
- 规则召回 + 证据抽取 + 确定性裁决（赛题指标 3 基础要求）
- **证据-裁决解耦 + 建议层（核心差异化）**
- 标准条款比对与缺失检测（赛题要求）
- 审核报告生成 + ECharts 风险热力图
- 鼓励创新方向一的多轮对话基础 UI（展示创新能力）

---

## <a id="17"></a>附录D：量化实验指标与验证方案

### D.1 测试数据集构成（已落地）

| 数据集 | 用途 | 规模 | 标注内容 | 来源 |
|-------|------|:---:|---------|------|
| 分类 + 要素 + 风险测试集 | 评估分类准确率 / 要素抽取 F1 / 风险 P/R | 84 份 / 152 条 | 合同类型标签 + 四要素字段 + 风险条款（risk_type + desc）| 32 份真实已签署政府采购合同 + 47 份上市公司公告披露版合同 + 5 份人工构造 |

标注流程：组员**背对背独立标注**（只依口径清单 + 合同正文）→ 分歧点引入 **AI 辅助法理审核**裁定 → 定标写回 gold。口径清单 v1.2.4（R01–R13 逐类法理定义 + 三条上位规则 A/B/C）。测试集文件 `backend/evaluate/realtest.json`。

### D.2 核心指标验证结果（实测）

| 指标 | 赛题要求 | 实测 | 验证口径 |
|------|:-----:|:---:|---------|
| 合同分类准确率 | ≥ 85% | **98.6%**（145/147）| 147 份分类测试集正确分类数 / 总数，RAG 少样本分类 |
| 关键要素抽取 F1 | ≥ 80% | **90.7%**（披露样本加权综合 F1）| 值级严格比对（金额 10% 容差、期限年份交集、争议关键词匹配）|
| 风险识别召回率 | ≥ 75% | **80.1%**（TP 121 / FN 30）| 条款级严格比对，TP = 预测 ∩ gold |
| 风险识别精准率 | ≥ 75% | **76.6%**（TP 121 / FP 37）| 条款级严格比对，FP = 预测 − gold |

### D.3 风险引擎演进（对照实验）

| 版本 | 方案 | 精准率 | 召回率 | F1 |
|------|------|:---:|:---:|:---:|
| v4 | LLM 泛化 Prompt 直接判风险 | 24.0% | 97.4% | 38.0% |
| v5 | 阈值写进提示词（严格 Prompt）| 25.9% | 73.5% | 38.3% |
| **v6.4** | **LLM 抽事实 + 代码硬阈值裁决** | **76.6%** | **80.1%** | **78.3%** |

v4 让 LLM 直接判风险，过报 466 条；v5 把阈值写进 Prompt，召回率反被误杀。结论：LLM 擅长布尔/数值判断，复杂枚举/嵌套规则交给代码裁决——这正是证据-裁决解耦架构的由来。

### D.4 非功能指标

| 指标 | 实测 | 说明 |
|------|:-----:|------|
| 后端启动时间 | ~2 秒 | RAG/向量库懒加载，启动链不拖入重依赖 |
| 审核耗时（fast）| < 1 秒 | 纯规则引擎确定性基线 |
| 审核耗时（precise）| 约 30-60 秒 | 分类/要素/证据/建议并行化后进一步缩短 |
| LLM 一致性 | temperature=0 | 分类/要素/证据/建议全部 temperature=0，可复现 |

### D.5 用户满意度与可用性测试

面向 3-5 名法学专业同学做 30 分钟可用性测试：完成「上传合同 → 查看审核结果 → 修正 AI 标注 → 导出报告」四个任务，记录任务完成率和操作时间，收集主观反馈（SUS 量表），为产品迭代提供方向。

---

## <a id="18"></a>附录E：数据集与知识库

### E.1 数据来源

**测试集（84 份）：** 32 份真实已签署政府采购合同（公开采购渠道）+ 47 份上市公司公告披露版合同（公告披露核心条款 + 按行业惯例/官方范本补全风险条款）+ 5 份人工构造（4 劳动合同 + 1 保密协议）。

**知识库：** 《民法典》合同编条文 + 司法解释 + 劳动法/个保法等精编条文（`laws.json`，161 条）；12 类风险典型案例（`risk_cases.json`）；11 类合同标准条款（`standard_clauses.json`，含图论字段）。

### E.2 分类与抽取（纯 LLM，不微调模型）

不微调本地模型，采用纯 LLM + 提示工程方案（实测已远超阈值，无需小模型微调的训练/维护成本）：

- **分类：** 11 类法理分类 + 服务外包业务标签，分块采样 + 多数投票（首尾块必含、中间等距采样）。
- **要素抽取：** JSON Schema 约束 + Few-shot Prompting，长合同分块并行合并。
- **输出校验：** LLM 输出经 JSON 提取与字段校验，失败自动重试。

### E.3 知识库初始化

知识库以 JSON 文件存储在 `backend/ai/knowledge/` 目录下，建议层按法条编号精确取用，无需运行时向量检索兜底：

| 文件 | 内容 | 规模 |
|------|------|:---:|
| `laws.json` | 《民法典》+ 司法解释 + 劳动法/个保法精编条文 | 161 条 |
| `risk_cases.json` | 12 类风险典型案例 | ~80 条 |
| `standard_clauses.json` | 11 类合同标准条款（type/priority/depends_on/conflict_with）| 结构化 |

### E.4 版本管理

LLM Prompt 模板、规则配置文件、知识库 JSON、测试集 gold 均纳入 Git 版本管理。每次规则/Prompt 更新记录：更新原因 + 更新前后在测试集上的指标对比，确保口径可追溯、可复现。



---

## <a id="19"></a>附录F：赛题提交材料对应体系

赛题明确要求提交五类材料，以下逐项说明体系化组织方式：

### F.1 用户使用手册框架（对应提交材料⑤-①）

用户手册计划在开发完成后按以下结构编写：

1. 系统概述与适用场景
2. 快速开始（注册登录 → 上传第一份合同 → 查看审核结果）
3. 功能操作指南：合同上传与管理、审核结果解读（风险等级/置信度/Agent 日志）、报告导出（HTML/PDF/Markdown）、反馈标注操作
4. 自定义配置：模板管理（上传/编辑/版本切换）、比对严格度设置
5. 常见问题 FAQ（10+ 条目）

### F.2 模型训练/微调过程记录框架（对应提交材料⑤-②）

训练过程将按以下模板逐项记录并提交：

| 记录项 | 内容 |
|-------|------|
| 训练日期 | [实际日期] |
| 模型版本 | bert-contract-classifier-v1.0 |
| 训练数据规模 | 250 条（5 类 × 50 条）+ 数据增强后 ~500 条 |
| 训练参数 | lr=2e-5, batch=16, epochs=5, early_stopping patience=2 |
| 训练时长 | ~30 min（单卡 T4） |
| 验证集准确率 | [实际值]% |
| 测试集准确率 | [实际值]% |
| 消融实验结果 | 见附录 D.3 消融实验执行台账 |
| 训练过程中的问题与解决 | [如实记录遇到的问题和调整方案] |

### F.3 项目分工与开发过程记录框架（对应提交材料⑤-③）

| 角色 | 姓名 | 负责模块 | 第一周目标 | 第二周目标 | 第三周目标 |
|------|:---:|---------|---------|---------|---------|
| 前端 A | — | 合同上传/列表/详情页 | 页面框架 + 路由 | 联调后端接口 | 完善交互 + 异常处理 |
| 前端 B | — | 审核结果/报告/热力图 | ECharts 集成 + pdf.js | 报告页面 + 反馈面板 | 联调 + 体验优化 |
| 后端 C | — | FastAPI 路由/数据库/认证 | 表结构建完 + 基础 CRUD | 审核调度 + 报告导出 | 联调 + 性能优化 |
| AI D | — | 文档解析/分类/证据裁决 | 解析器 + LLM 分类管道 | 规则引擎 + 证据裁决 | 建议层 + 联调 |
| AI E | — | 知识库/条款比对/报告生成 | ChromaDB 初始化 + 知识库 | 条款比对 + 报告模板 | 联调 + 效果调优 |

每日进度同步机制：飞书/钉钉每日站会 + GitHub Issues 任务看板。开发过程记录将整理为每周汇总报告提交。

---

> **文档结束。** 本文档版本 v3.1，共 13 章正文 + 6 篇附录，完整覆盖赛题全部要求。方案设计篇面向答辩评审，工程实现篇面向开发落地，附录与未来规划篇面向细节支撑和长期演进。三篇各有侧重、互为补充，共同构成 A24 合同智能审核系统的完整设计方案。
