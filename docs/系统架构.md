# 系统架构

> 本文描述**当前仓库实际实现**的架构。所有模块名、文件名、字段名均以代码为准。
> 凡代码中已删除、已归档或未接入生产链路的组件，在文末「已废弃与不存在的组件」一节中明确列出，**不作为当前架构描述**。

---

## 一、项目定位

**A24 · 基于大模型的企业合同智能审核与风险预警系统**（命题企业：网新恒天）

面向企业法务与风控部门，覆盖「合同上传 → 解析/OCR → 分类 → 要素抽取 → 风险识别 → 修改建议 → 条款比对 → 审核报告 → 条款修改 → 修订版 DOCX 导出」的完整闭环。

系统的核心主张是**人机协同**：机器负责可重复的证据提取与确定性判定，人负责最终定性、位置确认与版本采纳。

---

## 二、一条不可动摇的技术边界

> **LLM 负责理解、抽取证据、生成建议；Python 负责最终风险裁决。**

这不是文档上的修辞，而是代码中的结构性约束：

| 环节 | 承担者 | 代码位置 | 是否允许 LLM 下风险结论 |
|---|---|---|---|
| 合同类型分类 | LLM + RAG 少样本 | `ai/classifier/rag_classifier.py` | 是（这是分类，不是风险裁决） |
| 要素抽取 | LLM | `ai/extractor/extractor.py` | 否（只抽事实） |
| **风险事实证据抽取** | **LLM，只抽客观事实** | `ai/auditor/evidence_extractor.py` | **明确禁止**（prompt 明令不得输出 `risk`/`is_risk` 字段） |
| **风险裁决** | **纯 Python 硬阈值** | `ai/auditor/evidence_adjudicator.py` | — |
| 风险说明 / 修改建议 | LLM + grounding 校验 | `ai/auditor/recommendation_engine.py` | 否（不改变 `risk_type` / `level`） |
| 条款比对 | LLM 比对 + Python 图分析 | `ai/matcher/matcher.py` | 否（比对结果不是 R01–R13 风险） |
| 条款修订 / 新增起草 | LLM | `ai/reviser.py` | 否（返回文本，不改风险结论） |

因此，本系统**不是**「把合同丢给大模型让它判断有没有风险」。正确表述是：

> 大模型把合同条款翻译成结构化事实证据，Python 按固定阈值裁决这些事实是否构成 R01–R12 风险。

`ai/auditor/evidence_extractor.py` 的 system prompt 是这条边界的直接体现：

> 「唯一任务：从合同原文中**只抽取客观事实**，**绝对不要判断是否构成风险、不要下法律结论、不要输出 risk/is_risk 字段**。」

---

## 三、技术栈（当前默认实际运行）

| 层 | 技术 | 版本约束 | 说明 |
|---|---|---|---|
| 后端框架 | FastAPI + Uvicorn | `fastapi>=0.110`、`uvicorn>=0.29` | 后端端口 **8080** |
| ORM | SQLAlchemy 2.x（`Mapped` / `mapped_column`） | `sqlalchemy>=2.0` | 声明式模型，`models/` 下 9 张表 |
| **数据库** | **SQLite（当前默认实际运行）** | 文件 `backend/contract.db` | `DATABASE_URL=sqlite:///./contract.db` |
| 数据库（备选） | MySQL（已实现但**非默认**） | `pymysql>=1.1.0` | 换 `DATABASE_URL` 即可切换；模型层已做方言对齐 |
| LLM | DeepSeek `deepseek-chat` | `openai>=1.0` SDK | 唯一出口 `ai/llm_client.py`；`base_url=https://api.deepseek.com` |
| 向量库 | ChromaDB **嵌入式持久化客户端** | `chromadb>=0.4` | `PersistentClient(path=backend/chroma_data)`，**不需要 Docker** |
| 嵌入模型 | `shibing624/text2vec-base-chinese` | `sentence-transformers>=2.2` | `max_seq_length=512` |
| 词面检索 | 自实现 BM25 + RRF 融合 | `ai/rag/bm25.py` | 与稠密检索融合 |
| 文档解析 | `pdfplumber`（PDF）、`python-docx`（DOCX） | — | `ai/parser/` |
| OCR | PaddleOCR（图片 / 扫描件） | `paddleocr>=2.7` | **可选**，惰性加载，未初始化时图片上传返回明确 422 |
| DOCX → PDF | Word COM → LibreOffice CLI → 报错 | `services/docx_converter.py` | 仅用于「原文查看」，Windows 优先用本机 Word |
| 前端框架 | Vue 3 + Element Plus + vue-router 4 | `vue ^3.5`、`element-plus ^2.14` | 前端端口 **5173** |
| 前端构建 | Vite 6 | `vite ^6.0` | `/api` 反向代理到 `localhost:8080` |
| 前端附加库 | ECharts、`pdfjs-dist`、`mammoth`、`docx-preview` | — | 图表、PDF 渲染、文档预览 |
| 鉴权 | PyJWT（HS256）+ passlib/bcrypt | — | 令牌有效期 7 天 |
| 密钥加密 | cryptography Fernet（AES-128-CBC + HMAC-SHA256） | `cryptography>=42.0` | 用户个人 DeepSeek Key 加密落库 |

> **关于「生产用 MySQL」的口径**：项目**当前默认运行在 SQLite** 上。MySQL 是已实现的备选路径（依赖已装、模型层已做方言对齐、`main.py` 内有一段 SQLite 外键列类型迁移正是为切 MySQL 做准备），但**不是默认部署方式**，交付环境也未启用。详见 [`部署与复现.md`](./部署与复现.md)。

---

## 四、分层架构

```
┌──────────────────────────────────────────────────────────────────────┐
│  表现层  frontend/  (Vue 3 + Element Plus + Vite, :5173)             │
│    HomeView / LoginView / ContractList / ContractUpload              │
│    ContractDetail（工作台，5 个 Tab）                                 │
│    AuditResult(+Detail) / AuditReport(+Detail) / TemplateList        │
│    ProfileView / UserManageView / NotFoundView                       │
│    状态集中：composables/useContractWorkspace.js                      │
│    纯逻辑抽离：composables/workspaceLogic.js（可 node 直测）           │
└───────────────────────────┬──────────────────────────────────────────┘
                            │  /api  （Vite dev proxy → :8080）
┌───────────────────────────▼──────────────────────────────────────────┐
│  接口层  backend/api/  (FastAPI routers, prefix=/api)                │
│    auth.py      认证 / 用户管理 / 个人信息 / 个人 Key                  │
│    contracts.py 上传·审核·定位·修改·修订·导出·报告·比对（主路由）      │
│    overview.py  总体修改会话（综合修改方案 plan / confirm）            │
│    feedback.py  人工反馈池 → 学习层（reviewer 审 / admin 批）          │
│    templates.py 企业标准条款模板（版本链）                             │
│    stats.py     首页仪表盘统计                                        │
│    deps.py      角色守卫 / 当前用户 / LLM Key 前置检查                 │
└───────────────────────────┬──────────────────────────────────────────┘
                            │ 同进程函数调用（不走 HTTP）
┌───────────────────────────▼──────────────────────────────────────────┐
│  服务层  backend/services/                                            │
│    auth.py  role_bootstrap.py  user_secret.py  warmup.py             │
│    docx_reviser.py（修订版 DOCX 生成）  docx_converter.py（DOCX→PDF）  │
│    feedback_experience.py（经验构造）   eval_isolation.py（评测集隔离闸）│
└───────────────────────────┬──────────────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────────────┐
│  AI 引擎层  backend/ai/                                               │
│    parser/       pdf_parser / docx_parser / ocr_parser               │
│    chunker.py    按条款标题边界分块（长合同不漏检）                     │
│    classifier/   rag_classifier（生产默认） / classifier（零样本回退）  │
│    extractor/    extract_elements（要素抽取）                          │
│    auditor/      rule_engine → evidence_extractor → evidence_adjudicator │
│                  → recommendation_engine                              │
│    matcher/      compare_clauses（标准条款比对 + 跨条款图分析）          │
│    rag/          vector_store / bm25 / feedback_store / init_chroma    │
│    reviser.py    条款修订（Leader-Follower）+ 新增条款起草              │
│    confidence.py 置信度校准                                           │
│    taxonomy.py   合同分类体系唯一真源                                  │
│    knowledge/    laws.json(161) / standard_clauses.json(257) / risk_cases.json(0) │
│    llm_client.py 全项目唯一 LLM 出口   llm_context.py 请求级 Key 绑定   │
└───────────────────────────┬──────────────────────────────────────────┘
                            │ SQLAlchemy ORM / 文件读写
┌───────────────────────────▼──────────────────────────────────────────┐
│  数据层  backend/database.py + backend/models/                        │
│    SQLite: backend/contract.db（WAL 模式，busy_timeout=30s）           │
│    users / contracts / audit_records / audit_reports / templates      │
│    feedback_logs / feedback_experiences / clause_revisions            │
│    revision_proposals                                                 │
│    ChromaDB 持久化目录: backend/chroma_data/（运行时构建，含 chroma.sqlite3）      │
│    上传文件: backend/data/（软删除归档到 backend/data/deleted/）        │
└──────────────────────────────────────────────────────────────────────┘
```

AI 引擎作为**同进程 Python 包**被业务后端直接调用（不走 HTTP），这也是 `ai/` 下所有 LLM 调用点签名一致、可由 `llm_context` 统一绑定请求级 Key 的前提。

---

## 五、核心审核链路（`precise` 精细审核）

入口 `POST /api/contracts/{id}/audit` → BackgroundTasks 执行 `api/contracts.py::_run_audit()`：

```
1. 规则引擎基线        run_rules(full_text)                  ai/auditor/rule_engine.py
   R01–R13 正则 / 缺失型规则，始终先跑，作为 fast 基线与降级兜底

2. 证据抽取（LLM）     extract_evidence_detailed(full_text)  ai/auditor/evidence_extractor.py
   · 按条款标题边界分块（MAX_CHARS=20000），多块并行
   · 正文与「## 补全风险条款」段各自分块并行，补全段做字段级覆盖
   · 只抽事实，禁止输出风险结论
   · 返回状态信封 {evidence, status(success|partial|failed), failed_chunks, total_chunks}

3. 确定性裁决（Python） adjudicate_risks(evidence)           ai/auditor/evidence_adjudicator.py
   按硬阈值裁决 R01–R12，输出完整风险对象
   （risk_type / level / name / clause_text / reason / suggestion /
     detection_method="evidence" / confidence / related_law）

4. 建议层（LLM）       build_recommendations(risks, evidence) ai/auditor/recommendation_engine.py
   · 按 RULE_LAWS 法条编号在 laws.json 精确取条文
   · 生成风险说明 / 修改建议 / 修改示例
   · recommendation_grounding_check() 拦截无来源的数字与比例
   · 只增加展示字段，绝不修改 risk_type / level

5. 条款比对            compare_clauses(...)                   ai/matcher/matcher.py
   · 结构过滤（按合同类型取标准条款）+ 分块并行 LLM 比对
   · 输出 clauses / summary / missing_critical / cross_clause_risks
   · 在写事务之前执行（LLM 耗时长，避免长时间持有 SQLite 写锁）

6. 置信度融合          enrich_confidences(all_risks)           ai/confidence.py

7. 落库（单事务）      _mark_audit_records(valid→superseded) + 逐条 AuditRecord
                      + AuditReport + contracts.status = "completed"
```

**降级与失败语义（以下均为代码中的显式分支）：**

| 情况 | 行为 |
|---|---|
| 证据抽取全部块失败（`status == "failed"`） | 记 error 日志，**降级为规则引擎粗筛结果**，不伪装成「0 风险」 |
| 证据抽取部分块失败（`status == "partial"`） | 保留成功块证据继续裁决，并显式 warning |
| 裁决结果为空但抽取成功 | **如实报 0 风险**，不回退规则引擎（避免把规则误报当结论） |
| 裁决抛异常 | 记 error，退回规则引擎结果 |
| 条款比对失败 | 不阻断审核，报告标注「条款比对未完成…可稍后重试」 |
| 后台审核整体异常 | 主事务 rollback + 独立 session best-effort 恢复合同状态为 `parsed`；进程重启时 `lifespan` 复位遗留的 `auditing` |
| `audit_mode == "fast"` | 跳过第 2–4 步，**只跑规则引擎**（快速初筛） |

`audit_mode` 只有 `precise`（默认）与 `fast` 两个取值。

---

## 六、RAG 体系

### 6.1 四个 Chroma collection

`ai/rag/vector_store.py` 的 `COLLECTIONS`：

| collection | 中文名 | 建库源 | 实际状态 |
|---|---|---|---|
| `laws` | 法律法规库 | `ai/knowledge/laws.json`（**161 条**） | 已建成 |
| `standard_clauses` | 标准条款库 | `ai/knowledge/standard_clauses.json`（**257 条**，覆盖 11 个合同类型） | 已建成 |
| `risk_cases` | 风险案例库 | `ai/knowledge/risk_cases.json` | **空集合（0 条）**，仅预留 |
| `contract_templates` | 合同范本分类库 | `03_数据集/测试集/testset.json`（范本样本集，363 条） | 供 RAG 分类检索范本 |

### 6.2 混合检索

`search_knowledge()` = **稠密语义检索（ChromaDB + text2vec）+ BM25 词面检索 → RRF 融合**。

- 稠密检索失败（模型未就绪等）自动降级为仅 BM25，不报错；
- 检索结果按 `clause_id` 关联回 JSON 下标空间（不靠 content 反查，避免同文重复条目张冠李戴）；
- 知识库 JSON 缓存按源文件 `(mtime_ns, size)` 版本号失效。

### 6.3 建库源与评测集分离（防泄漏）

- **建库源**：`03_数据集/测试集/testset.json`（合同范本）
- **评测集**：`backend/evaluate/classification_test.json`（真实合同）、`backend/evaluate/realtest.json`

两者**不同源**，避免「检索库里就有答案」。此外还有第二道闸 `services/eval_isolation.py`：对三份官方评测语料正文做归一化 sha256 指纹，任何与其完全一致的合同正文**禁止沉淀为学习经验**。

### 6.4 Feedback RAG（默认关闭）

开关是 `config.py` 的 `FEEDBACK_RAG_ENABLED`，**默认 False**。开启后仅在**生产审核链路的证据抽取阶段**注入「已由人工审核并批准」的历史经验，作为「应重点核查什么」的提示。

结构性边界（不是文案承诺）：

- 只影响 `extract_evidence_detailed` 的 prompt，**不进入** `rule_engine` / `adjudicate_risks`；
- 官方评测路径 `extract_evidence(full_text)` 的签名**不含** `feedback_context`，**无法被注入**；
- 检索或注入失败一律降级为「不使用历史经验」，绝不让主审核失败。

---

## 七、LLM 出口与密钥管理

全项目**唯一** LLM 出口是 `ai/llm_client.py` 的 `llm_client.chat()`。

**Key 解析优先级：用户个人 Key > `.env` 的 `DEEPSEEK_API_KEY`（系统默认 Key）**

- 请求级绑定在 `main.py` 的 HTTP 中间件 `bind_user_llm_key`（必须放中间件：依赖运行在线程池线程，改不到端点上下文）；
- 绑定通过 `ai/llm_context.py` 的 ContextVar 实现，天然按请求隔离，不存在串 Key；
- `ThreadPoolExecutor` 新线程**不继承** contextvars，因此所有并发提交统一走 `submit_with_context()`；
- 两者都没有 Key 时抛 `LLMConfigError`，API 层转成明确的 400 提示（**绝不发送空 Key**，也不静默降级）；
- 用户个人 Key 由 `services/user_secret.py` 用 Fernet 加密落库（密钥由 `.env` 的 `SECRET_KEY` 经 SHA-256 派生）；任何接口**只返回 `deepseek_key_configured` 布尔值**，不回显 Key 本身或片段。

---

## 八、数据模型（9 张表，`backend/models/`）

| 表 | 模型 | 关键字段 |
|---|---|---|
| `users` | `User` | username / email / hashed_password / **role** / is_active / `deepseek_api_key_enc`（加密密文） |
| `contracts` | `Contract` | user_id / file_name / contract_type / type_confidence / **is_outsourcing** / parsed_text / extracted_elements / **status** / **audit_mode** / stored_path |
| `audit_records` | `AuditRecord` | contract_id / **audit_batch** / risk_type / risk_level / clause_text / **clause_position(JSON)** / reason / suggestion / detection_method / confidence / evidence(JSON) / recommendation(JSON) / feedback_status / **result_status** / learning_context(JSON) |
| `audit_reports` | `AuditReport` | contract_id / audit_batch / report_html / risk_score / high·mid·low_risk_count / risk_heatmap_data / **missing_clauses(JSON，含条款比对全文)** |
| `templates` | `Template` | name / contract_type / clauses(JSON) / is_builtin / version / previous_version_id（版本链） |
| `feedback_logs` | `FeedbackLog` | record_id / user_id / action_type / status / feedback_reason / target_type / reviewed_by / approved_by / model_evidence / model_result |
| `feedback_experiences` | `FeedbackExperience` | source_feedback_id（唯一外键）/ kind / **status(active\|revoked)** / query_text / experience_text / doc_id / index_version |
| `clause_revisions` | `ClauseRevision` | contract_id / **scope(clause\|overview)** / **operation(replace\|add_clause)** / **position(JSON)** / **clause_key** / clause_no / clause_text / **original_clause_text** / instruction / revised_clause / explanation / constraints / legal_basis / remaining_risks / **adopted(bool)** |
| `revision_proposals` | `RevisionProposal` | contract_id / user_id / instruction / **status(draft\|partially_applied\|applied)** / summary / items(JSON) / confirmed_ids(JSON) |

**三个字段必须理解清楚：**

- `audit_records.audit_batch`：一次审核批次的 UUID。`audit-result` 只返回**最新批次**，避免并发审核多批次混杂。
- `audit_records.result_status`：`valid` / `rejected` / `superseded`。重新审核时旧的 `valid` 置 `superseded`，驳回时置 `rejected`，**都不物理删除**（保留审计轨迹）。
- `clause_revisions.adopted`：用户是否**明确确认采用**该版本。同一 `(contract_id, clause_key)` 至多一行为 `True`。

`clause_revisions` 上有复合索引 `ix_clause_revisions_contract_key (contract_id, clause_key)`。

---

## 九、合同状态机（`contracts.status`）

```
uploaded ──解析成功──> parsed ──触发审核──> auditing ──审核完成──> completed
                         ▲                    │                      │
                         │                    │ 失败 / 重启复位        │ reviewer 复核通过
                         │                    └──────────────────────┤
                         │                                           ▼
                         └──── reviewer 驳回 ◄──── completed     reviewed
                                                                     │ approver 验收
                                                                     ▼
                                                                 approved
（任意状态）── 删除 ──> deleted（软删除，文件归档到 data/deleted/）
```

前端展示映射（`constants/reportTokens.js`）：`uploaded 已上传 / parsed 已解析 / auditing 审核中 / completed 审核完成 / reviewed 待验收 / approved 已验收`。

---

## 十、权限体系（4 个角色，服务端强制）

角色常量在 `api/deps.py`：`ROLE_UPLOADER` / `ROLE_REVIEWER` / `ROLE_APPROVER` / `ROLE_ADMIN`。

**判定权威来源是数据库中的真实角色**（`get_current_user` 每次从库读用户），与 localStorage / JWT 中客户端可控的 role 字段无关。

| 能力 | uploader | reviewer | approver | admin |
|---|---|---|---|---|
| 上传合同（`POST /contracts/upload`） | ✅ | ❌ 403 | ❌ 403 | ✅（`require_role` 恒通过） |
| 查看合同列表 / 详情 | 仅**自己**的 | 公司范围全部 | 公司范围全部 | 全部 |
| 删除合同 | 仅自己的 | ❌ | ❌ | 任意 |
| 触发审核 / 改条款 / 导出修订版 | 自己可见的合同 | 全部可见合同 | 全部可见合同 | 全部 |
| 复核（`/review`） | ❌ | ✅（**不得复核自己上传的**） | ❌ | ✅ |
| 验收（`/approve`） | ❌ | ❌ | ✅（**不得验收自己上传的**） | ✅ |
| 模板新建 / 修改 | ❌ | ✅ | ❌ | ✅ |
| 模板删除 | ❌ | ❌ | ❌ | ✅ |
| 用户列表 / 改角色（`/auth/users`） | ❌ | ❌ | ❌ | ✅ |
| 反馈审核（reviewer）/ 批准（admin） | ❌ | 审核 | ❌ | 批准 |
| 个人信息 / 个人 Key | ✅ | ✅ | ✅ | ✅ |

补充规则（均为代码中的真实约束）：

- **公开注册固定为 `uploader`**：`RegisterRequest` 不接受 `role` 字段，客户端传 `role=admin` 也会被忽略；
- 已删除（`status == "deleted"`）的合同**所有角色一律不可见**；
- 管理员**不得修改自己的角色**，且系统必须**至少保留一名 admin**；
- 第一个管理员通过一次性部署开关 `BOOTSTRAP_ADMIN_USERNAME` 产生：仅当库中**0 个 admin** 且指定账号**已存在**时提升该账号，**绝不自动建号、不改密码**。

---

## 十一、并发与事务设计要点

| 问题 | 措施 |
|---|---|
| 重复审核 | `UPDATE ... WHERE status != 'auditing'` 原子占位（乐观锁），并发只允许一个请求成功，否则 409 |
| SQLite 写锁竞争 | `busy_timeout=30s` + WAL 模式；LLM 长调用（条款比对）**放在写事务之外** |
| 审核失败残留 `auditing` | 独立 session best-effort 恢复为 `parsed`（退避重试 3 次）；进程启动时再复位一次 |
| 审核中途异常丢数据 | 记录 + 报告 + 状态合并为**一个事务**，异常统一 rollback |
| 并发审核批次混杂 | `audit-result` 只返回最新 `audit_batch` |
| 启动冷启动 | `services/warmup.py` 静启动：daemon 线程后台预热 torch / 向量模型 / 向量库，启动不阻塞；进度可查 `GET /api/health → warmup` |

---

## 十二、已废弃与不存在的组件（**不得写入当前架构**）

以下内容在历史版本中存在过或曾在文档中出现，**当前生产链路中已不存在或从未接入**：

| 名称 | 当前真实状态 |
|---|---|
| **Corex / 多 Agent 审核编排** | **已归档，不在生产链路**。代码中仅存历史遗留痕迹：`audit_records.corex_agent_log` 列（保留兼容，注释明确「Corex 已归档，不得恢复其语义」）、`_build_evidence()` 里的 `corex_review` 分支（当前不会产生该 `detection_method`）。前端 `DETECTION_METHOD_LABELS` 也只为已有历史数据保留中性标签「多智能体验证」。**不得描述为当前架构。** |
| **reporter（报告生成 Agent）** | 当前**不存在**。审核报告由 `api/contracts.py` 的 `_build_risk_rows` / `_build_compare_rows` 直接生成 HTML 存入 `audit_reports.report_html`。 |
| **Dify / Dify 工作流** | 当前代码**不引用**。`.env` 中残留一个未被任何代码读取的 `DIFY_API_KEY` 项，属历史遗留配置，**生产链路不经过 Dify**。 |
| **KNN 分类器** | 当前**不作为生产分类器**。生产默认是 **RAG 少样本分类**（`classify_by_rag`：检索 top-K 范本作示例喂 LLM），检索无结果时才降级为纯 LLM 零样本分块投票（`ai/classifier/classifier.py`）。「kNN 投票」仅是 `rag_classifier.py` docstring 中的设计背景说明。 |
| **已删除的旧风险判断逻辑** | 当前唯一生产裁决入口是 `ai/auditor/evidence_adjudicator.py`。`ai/auditor/llm_auditor.py` 仍存在于代码库，但**只被评测脚本 `evaluate/evaluate_risks.py` 引用**，不进入 `api/` 生产链路。 |
| **已废弃的旧修改系统** | 当前修改系统是 `clause_revisions`（专项会话）+ `revision_proposals`（总体方案）+ `services/docx_reviser.py`（DOCX 应用）。历史上「把整份合同重写一遍存成 `overview+replace` 讨论稿」的做法**已不是导出路径**：`download_revised_docx` 的取数口径只取 `scope=="clause"` 或 `scope=="overview" AND operation=="add_clause"`。 |
| **risk_cases 风险案例库** | collection 已创建但**内容 0 条**，属预留占位，不得描述为已建成的知识库。 |
| **`deploy/` 目录** | 当前**只有 `.gitkeep`**，无任何部署脚本，不得描述为「完整 Docker 化部署目录」。 |
| **`mysql_data/` 目录** | 早期 MySQL 尝试遗留的 InnoDB 文件，**当前默认部署不使用**，且已被 `.gitignore` 忽略。 |
| **根目录 `chroma_data/`** | 仓库根另有一个 `chroma_data/`（仅 `.gitkeep`），是早期 Docker 挂载约定留下的空占位；**代码实际使用的向量库目录是 `backend/chroma_data/`**（`vector_store.py::CHROMA_DIR`）。 |
| **审核报告 PDF / Word 导出接口** | **不存在**。报告页只提供浏览器打印。 |
| **`backend/schema.sql`、`backend/routers/`、`backend/schemas/`** | **均不存在**（`api/` 即路由目录，直接挂在 `main.py` 上）。 |

---

## 十三、当前架构的真实局限（如实列出）

1. **SQLite 单写者**：并发写能力有限。已用 WAL + `busy_timeout` + 缩短写事务窗口缓解，但这**不是**解决并发写的方案。
2. **证据抽取依赖 LLM 质量**：某块失败只计入 `failed_chunks`，对应条款的风险要素**可能丢失**（系统显式记录，不静默吞掉）。
3. **R05 无 Gold、R13 不入 F1**：风险评测只对 R01–R12 中**实际有 Gold** 的风险计算 P/R/F1（详见 [`评测方法.md`](./评测方法.md)）。
4. **DOCX 修订版只支持 `.docx` 原始合同**：PDF / 图片合同**不支持**导出修订版文件。
5. **修订版不生成 Word 修订痕迹（Track Changes）**，也不做完整版本管理。
6. **同一段原文被两个不同会话都确认采用时**，DOCX 归并仍按锚点后写覆盖（已在代码注释中记录的已知边界）。
7. **条款比对质量取决于该合同类型是否有标准条款**；无对应条款时回退到全部条款或提示「没有可用的标准条款范本」。
8. **审核报告无正式导出接口**：没有 PDF / Word 报告导出 API，仅支持浏览器打印。

---

## 十四、相关文档

- 审核流水线逐步说明：[`审核流程.md`](./审核流程.md)
- 用户可见功能与页面：[`系统功能说明.md`](./系统功能说明.md)
- R01–R13 规则与阈值：[`风险规则说明.md`](./风险规则说明.md)
- 条款修改、采用态与 DOCX 导出：[`条款修改与修订机制.md`](./条款修改与修订机制.md)
- 审核报告两页与可视化：[`审核报告与可视化.md`](./审核报告与可视化.md)
- 目录职责：[`项目目录说明.md`](./项目目录说明.md)
- 环境与启动：[`部署与复现.md`](./部署与复现.md)
