# 部署与复现

> 按当前仓库**实际实现**说明：环境、依赖、`.env`、启动、测试与三套评测的复现命令。
> 所有路径相对仓库根（`LLM-ContractAudit-WarnSystem/`）。
> 术语约定：**「当前默认实际运行」**指交付环境真正在用的配置；**「已实现但非默认」**指代码支持但未启用；**「预留 / 未使用」**指代码或目录存在但不参与运行。

---

## 一、环境要求

| 组件 | 要求 | 状态 |
|---|---|---|
| Python | 3.10+（开发验证环境为 3.10.x） | **当前默认实际运行** |
| Node.js | 20.x LTS | **当前默认实际运行** |
| 操作系统 | Windows（开发验证环境） | **当前默认实际运行** |
| 数据库 | **SQLite**（文件 `backend/contract.db`） | **当前默认实际运行** |
| 数据库 | MySQL 8.0 | **已实现但非默认**（换 `DATABASE_URL` 即可切换） |
| 浏览器 | 现代 Chromium / Firefox（需支持 ES2020+、`sessionStorage`、`requestAnimationFrame`） | 必需 |

> **重要口径**：`config.py` 里 `DATABASE_URL` 的**默认值**是 MySQL 连接串，但**项目根目录的 `.env` 实际把它覆盖为 `sqlite:///./contract.db`**。因此交付环境运行的是 **SQLite**，MySQL 是备选路径而非默认部署方式。

---

## 二、安装依赖

### 后端

```bash
cd backend
python -m venv venv
venv\Scripts\activate            # Windows
pip install -r requirements.txt
```

`requirements.txt`（逐项对照实际文件）：

| 分类 | 依赖 | 用途 | 状态 |
|---|---|---|---|
| 框架 | `fastapi`、`uvicorn`、`sqlalchemy`、`pydantic`、`python-multipart` | Web / ORM / 表单 | 必需 |
| 鉴权 | `passlib`、`bcrypt>=4.0,<4.1`、`pyjwt` | 密码哈希 + JWT | 必需 |
| 密钥加密 | `cryptography` | 个人 DeepSeek Key 的 Fernet 加密 | 必需 |
| 配置 | `python-dotenv` | `.env` 加载 | 必需 |
| 文档解析 | `pdfplumber`（PDF）、`python-docx`（DOCX） | 文本提取 | 必需 |
| LLM | `openai` | 指向 DeepSeek 的 OpenAI 兼容 SDK | 必需 |
| RAG | `chromadb`、`sentence-transformers` | 向量库 + 句向量（会拉入 torch） | 必需 |
| 校验 | `email-validator` | 注册接口的邮箱校验 | 必需 |
| 数据库驱动 | `pymysql` | 仅切 MySQL 时生效 | 已装但当前不使用 |
| **OCR** | `rapidocr==3.9.2`、`onnxruntime==1.23.2` | 图片 / 扫描件合同识别（**模型随 RapidOCR wheel 自带，无需联网下载**） | 随 `requirements.txt` 安装；引擎不可用时图片/扫描件上传返回明确 422，不影响 DOCX / PDF |
| **DOCX→PDF** | `pywin32` | Windows 下走 Word COM 转 PDF | **可选**（缺失自动降级 LibreOffice） |

### 前端

```bash
cd frontend
npm install
```

`package.json` 运行时依赖：`vue ^3.5.13`、`vue-router ^4.5.0`、`element-plus ^2.14.3`、`@element-plus/icons-vue ^2.3.2`、`axios ^1.18.1`、`echarts ^6.1.0`、`pdfjs-dist ^6.1.200`、`mammoth ^1.12.0`、`docx-preview ^0.3.7`；开发依赖：`vite ^6.0.0`、`@vitejs/plugin-vue ^5.2.0`。

> 说明：`docx-preview` 与 `mammoth` 虽在 `package.json` 中声明，但 `src/` 中**目前没有任何引用**（不影响功能）。

---

## 三、`.env` 配置

在**项目根目录**创建 `.env`（模板见 `.env.example`）：

```ini
# DeepSeek API Key（可留空：留空后由各用户在「个人信息」页配置自己的 Key；
# 两者都没有时后端仍可启动，但第一次需要 DeepSeek 的操作会返回明确提示）
DEEPSEEK_API_KEY=

# 数据库：默认部署用 SQLite
DATABASE_URL=sqlite:///./contract.db

# 必填：JWT 签名密钥，必须是非占位随机串
SECRET_KEY=请生成随机长字符串

# 允许的前端来源，逗号分隔
CORS_ORIGINS=http://localhost:5173

# 可选：一次性产生第一个管理员
BOOTSTRAP_ADMIN_USERNAME=

# 可选：Feedback RAG（默认关闭）
# FEEDBACK_RAG_ENABLED=false
```

### 关键约束

1. **`SECRET_KEY` 是必填项**：为空或等于占位串 `change-me-to-random-string` 时，`config.py` 会直接 `raise RuntimeError`（防止弱密钥导致 JWT 在 `--reload` / 多 worker 下失效）。生成方法：

   ```bash
   python -c "import secrets;print(secrets.token_urlsafe(32))"
   ```

2. **不要把真实 Key 提交到 Git**：`.env` 已被 `.gitignore` 忽略，仓库只保留 Key 项留空的 `.env.example` 模板。若曾误提交真实 Key，请立即到 DeepSeek 平台**撤销并重新生成**。
3. `DIFY_API_KEY` / `DIFY_BASE_URL` 是**历史遗留的死配置**（Dify 已从代码中移除），**没有任何代码读取它们**，无需填写、不影响运行。
4. 切换 MySQL：把 `DATABASE_URL` 改为 `mysql+pymysql://user:pass@host:3306/contract_audit`。模型层已做方言对齐（时间统一为应用层写入的 naive UTC；含一段 SQLite 外键列类型迁移）。**注意：SQLite 的 `busy_timeout` 与 WAL 设置仅在 SQLite 下生效。**
5. `DATABASE_URL` 使用相对路径 `./contract.db` 时，数据库文件位置取决于**启动时的工作目录**。仓库现有的 `start-backend.bat` 会先 `cd /d "%~dp0backend"`，因此实际文件是 `backend/contract.db`。

### DeepSeek Key 的两种来源与优先级

```text
系统默认 Key：.env → DEEPSEEK_API_KEY            （部署方配置，供未配个人 Key 的用户共用）
用户个人 Key：前端「个人信息」→ DeepSeek API Key  （用户自己配置，只作用于该账号）
优先级：      个人 Key  >  系统默认 Key
删除个人 Key：自动回退 系统默认 Key
```

- 部署方的 `DEEPSEEK_API_KEY` **可以留空** —— 留空时由各用户在「个人信息」页配置自己的 Key；
- 两者都没有时，需要 LLM 的操作返回明确提示（HTTP 400）：

  > 「未配置可用的 DeepSeek API Key：请在「个人信息」页配置你自己的 DeepSeek API Key，或联系部署管理员在 .env 中配置系统默认 Key（DEEPSEEK_API_KEY）。」

- **缺 Key 的报错在「调用期」而不是「import 期」**：`ai/llm_client.py` 刻意不做 import 期硬校验，否则「`.env` 不配系统 Key、由每个用户各自配个人 Key」这种部署方式将无法启动；
- **存储**：个人 Key 以 **Fernet**（AES-128-CBC + HMAC-SHA256，密钥由 `SECRET_KEY` 经 SHA-256 派生）加密后存入 `users.deepseek_api_key_enc`，**不是明文**；任何接口只返回 `deepseek_key_configured: true/false`，**任何角色（含 admin）都拿不到 Key 本身**；
- **局限（如实说明）**：安全性等价于 `SECRET_KEY` 的保管强度 —— 拿到 `SECRET_KEY` 即可解密。**轮换 `SECRET_KEY` 会使已存个人 Key 解不开**，此时按「未配置」处理并自动回退系统默认 Key（不会崩溃）；
- **并发隔离**：Key 按请求上下文解析（`ai/llm_context.py` 的 ContextVar，由 `main.py` 的 HTTP 中间件绑定），`DeepSeekClient` 不保存 `api_key`，并**按 Key 哈希缓存** OpenAI client。因此 A 用户的请求永远用 A 的 Key，B 用 B 的，不会串用。

### 首次部署：产生第一个管理员（可选，一次性）

公开注册**只能**得到 `uploader`，其余角色只能由管理员在「用户管理」页分配；而全新库里没有管理员、也就无人可分配，因此提供这个**可选的一次性**开关。

```ini
BOOTSTRAP_ADMIN_USERNAME=      # 填一个【已经注册好】的用户名
```

**正确顺序**（该账号必须已存在，否则不会生效）：

1. 先启动后端，在前端注册一个普通账号（角色为 `uploader`）；
2. 把该用户名填进 `.env` 的 `BOOTSTRAP_ADMIN_USERNAME`；
3. 重启后端 —— 启动时完成提升；
4. 用它登录，在「用户管理」页分配其余账号的角色；
5. 之后不再需要这项配置（留着或删掉都可以）。

**实际行为**（以 `services/role_bootstrap.py` 实现为准）：

| 情况 | 结果 |
|---|---|
| 未配置 / 留空 | 完全跳过（默认行为） |
| 系统里**已有** admin | 完全跳过，**不改动任何角色** |
| 填的用户名**不存在** | 跳过并告警，**不会自动创建账号** |
| 系统里**没有** admin 且该账号存在 | 把**这一个账号**提升为 `admin` |

补充约束：

- **不创建新账号、不改任何密码、不批量修改其他用户** —— 整个过程只写这一个账号的 `role` 字段；
- 每次启动都会检查，但**只有「系统里没有任何 admin」时才会改动**。因此初始化完成后即使配置一直留着，也不会把事后被降权的管理员「复活」；
- 角色分配本身有两条保护：管理员**不能修改自己的角色**；系统**必须至少保留 1 个 admin**。

### Feedback RAG（默认关闭，可选）

```ini
FEEDBACK_RAG_ENABLED=false     # 默认值
```

开启后，**生产审核链路**在证据抽取阶段会检索「已由人工批准且状态为 active 的历史经验」并作为「应重点核查什么」注入 LLM prompt。

**严格边界**（以代码行为为准）：

- 只影响 `extract_evidence_detailed` 的 prompt，**不进入** `rule_engine` / `adjudicate_risks`（裁决仍完全由 Python 决定）；
- **官方评测路径不受影响**：`evaluate/run_evidence.py` 调用的 `extract_evidence(full_text)` 签名不含 `feedback_context`，prompt **逐字节不变**；
- 检索或注入失败只降级为「不使用历史经验」，绝不导致主审核失败。

---

## 四、启动行为（静启动 / warmup）

启动时 `lifespan` 会依次完成：建表 → SQLite 列迁移 → **复位遗留的 `auditing` 状态** → 一次性 bootstrap 管理员 → **启动后台静启动预热**。

**静启动**（`services/warmup.py`）：进程启动后在**后台 daemon 线程**里提前加载 `torch` / `sentence-transformers` / `chromadb` 与向量库，**不阻塞启动、用户无感知**，避免把冷启动成本（分段实测约 9.2 秒，其中 import 占 7.4 秒）推到「第一次上传合同」上。

进度可**被动查询**：`GET /api/health` → `warmup` 字段：

```json
{ "status": "ok",
  "warmup": { "rag": "ready", "ocr": "idle", "rag_seconds": 8.1, "ocr_seconds": null } }
```

`rag` / `ocr` 取值：`idle` / `warming` / `ready` / `failed`（OCR 另有 `unavailable`）。

| 环境变量 | 作用 |
|---|---|
| `DISABLE_WARMUP=1` | 关闭静启动（单测 / CI 用，避免无谓拖入 torch） |
| `WARMUP_OCR=1` | 额外预热 OCR 引擎（RapidOCR；模型随 wheel 自带、**不联网下载**，默认关闭） |
| `WARMUP_STRICT=1` | 预热异常时抛出（默认只告警，不影响服务） |

预热失败**绝不影响服务**：只记 warning，真正的首次使用仍会照常惰性加载并重试。

---

## 五、LLM 配置

| 项 | 值 |
|---|---|
| 模型 | `deepseek-chat` |
| 接口 | `https://api.deepseek.com`（OpenAI 兼容，`ai/llm_client.py` 硬编码） |
| API Key | 用户个人 Key（优先）或 `.env` 的 `DEEPSEEK_API_KEY`；都没有时**调用期**报错 |
| 超时 / 重试 | 单次调用 `timeout=30s`、`max_retries=0`；**重试由各调用方自行实现** |
| temperature | 分类 **0.1**、条款比对 **0.1**、改条款 **0.1**、证据抽取 **0.0**、要素抽取 **0.0**、建议层 **0.0**、Leader/Follower/QA **0.1** |

调用方自带重试的实际实现：**建议层最多尝试 2 次**（`for attempt in range(2)`，即失败后重试 1 次）；其余调用点失败即返回错误（修订类端点返回 502，不再伪装成成功）。

---

## 六、RAG 配置

| 项 | 值 |
|---|---|
| 向量库 | ChromaDB **嵌入式持久化客户端**，目录 **`backend/chroma_data`**（`anonymized_telemetry=False`）；`chroma.sqlite3` 与集合数据在此 |
| Embedding | `sentence-transformers` 的 `shibing624/text2vec-base-chinese`，`max_seq_length=512` |
| 集合 | `laws`（161 条）、`standard_clauses`（257 条）、`risk_cases`（**空，仅预留**）、`contract_templates`（范本分类库，供 RAG 少样本分类） |
| 检索 | `search_knowledge()` = Chroma 稠密 + BM25 词面 → **RRF 融合**；`search_similar_templates()` 取 top-K 相似范本 |
| 离线模式 | 模块 import 时 `setdefault` 设 `HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1`（避免 HuggingFace 不通时长时间卡住）；因此**首次使用前需先联网把 embedding 模型下载到本地缓存** |
| 初始化建库 | 首次运行由懒加载自动触发；也可显式执行 `cd backend && python -m ai.rag.init_chroma` |

> **关于 `chroma_data/` 路径**：代码里 `CHROMA_DIR` 指到的是 **`backend/chroma_data`**。仓库根另有一个 `chroma_data/`（仅有 `.gitkeep`），是早期 Docker 挂载约定留下的空占位，**当前不使用**。
>
> **关于 Docker**：仓库根的 `docker-compose.yml` **只有 1 个服务**（`chromadb/chroma:0.5.23`，映射 8001）。由于应用使用的是 **ChromaDB 嵌入式持久化客户端**，**该容器在当前部署中并不需要、也未使用**；`deploy/` 目录内只有 `.gitkeep`，**没有**任何 Dockerfile 或编排文件。因此**不要把本项目描述为「Docker 一键部署」**。

---

## 七、OCR（图片 / 扫描件合同）

- 引擎：**RapidOCR 3.9.2 + ONNX Runtime 1.23.2（单引擎，已不再使用 PaddleOCR / PaddlePaddle）**，入口统一在 `ai/parser/ocr_engine.py`，由 `ai/parser/ocr_parser.py` 适配成与 `parse_pdf` / `parse_docx` 同形的结果；**惰性初始化**（首次用到才实例化），失败则 OCR 不可用；
- **模型随 RapidOCR wheel 自带**（`rapidocr/models/`：PP-OCRv6 det/rec small + 方向分类），因此**不需要联网下载 OCR 模型**，也不需要 Docker / poppler / ImageMagick / ghostscript；
- 扫描 PDF 的栅格化走 `pdfplumber.page.to_image()`（底层 **pypdfium2**，已随 pdfplumber 安装），默认 150 DPI，单份最多 OCR **50 页**（超出截断并记录）；
- 触发路径：`POST /api/contracts/upload`——图片扩展名 `jpg/jpeg/png/tiff/tif/bmp` 直接走 OCR；`.pdf` **先**按原 `parse_pdf` 取文字层，**取不到文字时才**转 OCR（扫描件），普通文字 PDF 不经过 OCR；`.docx` 永远不经过 OCR；
- **质量闸门在解析阶段**（`ocr_engine.assess_quality`，阈值：`MIN_VALID_CHARS = 10`、`MIN_MEAN_CONFIDENCE = 0.75`、`MAX_LOW_CONF_RATIO = 0.5`、低置信判定 `LOW_CONF_THRESHOLD = 0.6`）。命中任一条即判 `ocr_quality="low"` 并直接返回 **422**（同时清理落盘文件）：
  - 无识别结果 → 拒绝；
  - 平均置信度过低（< 0.75）→ 拒绝；
  - 低置信结果比例过高（> 50%）→ 拒绝；
  - 有效字符过少（< 10 字）→ 拒绝。
  只有 `ocr_quality="normal"` 的文本才会进入后续**分类 / 要素抽取 / 风险审核**；
- 输入 → 输出闭环：扫描 PDF / 图片与 DOCX、普通 PDF 一样，可**审核 → 修改 → 导出修订版 DOCX**（由 `parsed_text` 重建中间 DOCX 后再应用修改）。

---

## 八、DOCX→PDF（用于「查看合同原文」）

`services/docx_converter.py::docx_to_pdf()` 的优先级：

1. **Word COM**（`pywin32`）：`win32com.client.Dispatch('Word.Application')` → `SaveAs(FileFormat=17)`；
2. **LibreOffice CLI**：`soffice --headless --convert-to pdf`（`timeout=60s`；会搜索常见安装路径与注册表）；
3. 两者都失败 → `RuntimeError`。

- 有 PDF 缓存（PDF 的 mtime ≥ docx 时复用）；
- 调用点：`GET /api/contracts/{id}/file`，当原文件为 `.docx` 时实时转 PDF 供前端 pdf.js 渲染（完整审核报告页加载较慢即由此而来）；**转换失败会降级返回原 `.docx`**（前端会提示「原文文件无法按 PDF 渲染」）；
- `pywin32` 是**可选依赖**（import 失败自动降级 LibreOffice）。

---

## 九、启动

```bash
# 一键启动（后端 + 前端两个窗口）
start.bat

# 或分别启动
start-backend.bat      # 后端：http://localhost:8080/docs
start-frontend.bat     # 前端：http://localhost:5173
```

等价的手动启动：

```bash
# 后端
cd backend
venv\Scripts\python.exe -m uvicorn main:app --reload --host 0.0.0.0 --port 8080

# 前端
cd frontend
npm run dev
```

启动后：

- 前端 `http://localhost:5173`
- 后端 API 文档 `http://localhost:8080/docs`
- 健康检查 `GET http://localhost:8080/api/health`（含 `warmup` 字段）

> **关于跨域**：前端 `utils/request.js` 使用**绝对** `baseURL = http://localhost:8080/api`，请求**不经过** Vite 的 `/api` 代理（`vite.config.js` 里的代理配置实际未被用到）。因此后端 `.env` 的 `CORS_ORIGINS` 必须包含前端的实际来源（默认 `http://localhost:5173`）；换端口部署时务必同步修改。

---

## 十、测试

### 后端（`unittest`）

```bash
cd backend
python -m unittest discover -s tests -t .
```

- **框架是 `unittest`**（不是 pytest；项目内没有 `pytest.ini` / `conftest.py`）；
- 规模：**29 个测试文件 / 288 个测试方法**；
- 也可单文件运行，例如 `python -m unittest tests.test_permissions -v`；
- 测试使用**临时 SQLite 库与临时目录**，通过 `TestClient` 走真实 HTTP，**不触碰生产 `backend/contract.db`**；
- 覆盖范围（按文件）：

  | 主题 | 文件 |
  |---|---|
  | 权限隔离 / 角色管理 / bootstrap | `test_permissions.py`(10)、`test_user_management.py`(13) |
  | 个人 DeepSeek Key | `test_user_api_key.py`(14)、`test_api_key_config.py`(7) |
  | 总体修改会话 | `test_overview_session.py`(21) |
  | 修订会话 + 修订版 DOCX | `test_revision_export.py`(39)、`test_revise_link.py`(6) |
  | 新增条款 / 条款定位 | `test_add_clause.py`(18)、`test_locate_clause.py`(18) |
  | 反馈闭环 / Feedback RAG | `test_feedback_experience.py`(22)、`test_feedback_rag.py`(29) |
  | 静启动 | `test_warmup.py`(6) |
  | BUG 回归（17 个文件） | `test_bug_003_007_008.py` 等，合计 **85** |

### 前端（`node --test`）

```bash
cd frontend
node --test src/
```

覆盖 6 个纯逻辑测试文件 / **95 个测试**：`constants/navigation.test.mjs`(25)、`composables/overviewLogic.test.mjs`(28)、`composables/workspaceLogic.test.mjs`(28)、`api/adoptContract.test.mjs`(6)、`api/overviewApi.test.mjs`(4)、`api/timeouts.test.mjs`(4)。

> `package.json` 里**没有** `test` 脚本，需直接调用 `node --test`。

---

## 十一、三套评测与正式指标复现

工作目录 `backend/`。

### 1. 分类（147 条正式测试集）

```bash
python evaluate/evaluate_rag.py
```

- 输入：`evaluate/classification_test.json`（147 条，Gold 冻结，正文已去答案元信息并脱敏）
- 输出：准确率 + 每类 P/R/F1 + 判错样本 + 预测快照 `evaluate/rag_predictions.json`
- **正式口径：98.6%（145/147）**

### 2. 要素抽取（84 份）

```bash
python evaluate/evaluate_elements.py
```

- 输入：`evaluate/realtest.json`（84 份）；LLM 结果按 md5 缓存于 `evaluate/cache_elements.json`
- 输出：四要素逐项 P/R/F1 + 披露样本加权综合 F1
- **正式口径：93.7%（P 94.8% / R 92.7%；双方 100.0 / 金额 87.2 / 期限 90.8 / 争议解决 96.3）**

### 3. 风险识别（84 份，证据 + 裁决）

```bash
python evaluate/run_evidence.py        # 预计算 LLM 证据 → evidence.json（有缓存可跳过）
python evaluate/evaluate_evidence.py
```

- 输入：`evaluate/realtest.json` + `evaluate/evidence.json`；裁决走**生产同一函数** `ai.auditor.evidence_adjudicator.adjudicate_risks`（R01–R12）
- 输出：TP/FP/FN + P/R/F1 + 分类别明细 + FN/FP 明细
- **正式口径：P 76.6% / R 80.1% / F1 78.3%（TP 121 / FP 37 / FN 30）**

### 4. 可选：数据集重建与非正式对照

```bash
python evaluate/build_realtest.py              # 重建 realtest
python evaluate/build_classification_test.py   # 重建分类测试集（147 = 84 + 55 + 8）
python evaluate/evaluate_classifier.py         # 零样本分类基线（非正式指标）
python evaluate/evaluate_risks.py --engine both  # 双引擎六列对照（非正式指标）
```

> 三套评测均走 DeepSeek `deepseek-chat`。分类 temperature=0.1，存在 **145↔147 的轻微波动**，**正式口径冻结在 98.6%（145/147）**，不因内部重跑出现更高结果而替换。
> **风险指标覆盖 R01–R12 中实际有 Gold 的风险；R05 无 Gold；R13 不进 F1。** 详见 [`评测方法.md`](./评测方法.md) 与 [`数据集说明.md`](./数据集说明.md)。

---

## 十二、常见问题

| 问题 | 处理 |
|---|---|
| 启动报 `必须配置 SECRET_KEY` | `.env` 的 `SECRET_KEY` 为空或仍是占位串，改为随机长字符串 |
| 调用 LLM 报「未配置可用的 DeepSeek API Key」 | 在「个人信息」页配置个人 Key，或在 `.env` 配置 `DEEPSEEK_API_KEY`。**这是调用期报错，不影响后端启动** |
| 第一次上传合同很慢 | 静启动预热尚未完成（可查 `/api/health` 的 `warmup`）；预热完成后第一次上传明显变快 |
| 图片 / 扫描件上传返回 422 | OCR 引擎不可用、或**质量闸门未通过**（无识别结果 / 平均置信度 < 0.75 / 低置信占比 > 50% / 有效字数 < 10）→ 换更清晰的原件重新上传 |
| DOCX 原文预览失败 | 未装 Word 或 LibreOffice；两者都无时后端会降级返回原 `.docx`，前端提示「无法按 PDF 渲染」 |
| 前端请求被 CORS 拦截 | `utils/request.js` 用绝对地址直连 8080，必须在 `.env` 的 `CORS_ORIGINS` 里加入前端实际来源 |
| 分类结果在 145/146/147 之间波动 | temperature=0.1 的正常波动；**正式口径冻结在 98.6%（145/147）** |
| 修订版 DOCX 下载返回 400 | 见 [`条款修改与修订机制.md`](./条款修改与修订机制.md) 第 5.2 节的前置换条件；最常见原因是「有修订尚未建立可靠原文定位」。**注意：非 DOCX（PDF / 图片）同样可以导出修订版**，它们由 `parsed_text` 重建中间 DOCX 后再应用修改 |
| 并发审核出现 `database is locked` | SQLite 单写者限制。已用 WAL + `busy_timeout=30s` + 缩短写事务窗口缓解；**高并发场景应切 MySQL**（非默认） |

---

## 十三、相关文档

- 架构、数据模型、权限矩阵：[`系统架构.md`](./系统架构.md)
- 目录与文件职责：[`项目目录说明.md`](./项目目录说明.md)
- 评测口径与冻结指标：[`评测方法.md`](./评测方法.md)
- 数据集与 Gold：[`数据集说明.md`](./数据集说明.md)
- 修订版 DOCX 导出的前置换条件：[`条款修改与修订机制.md`](./条款修改与修订机制.md)
