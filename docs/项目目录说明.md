# 项目目录说明

> 以当前仓库实际内容为准（2026-09 阶段冻结版）。标注「已删除 / 已归档 / 仅存活于代码注释」的内容不属于当前生产架构。

```
LLM-ContractAudit-WarnSystem/
├── backend/                            后端（FastAPI + SQLAlchemy + AI 引擎，同进程）
│   ├── main.py                         应用入口：挂载 6 个路由 + 3 个内置端点、CORS、
│   │                                   启动建表 + SQLite 列迁移 + 复位遗留 auditing 状态 +
│   │                                   bootstrap 管理员 + 静启动预热；含请求级个人 Key 绑定中间件
│   ├── config.py                       .env 加载；DATABASE_URL / SECRET_KEY / CORS_ORIGINS /
│   │                                   FEEDBACK_RAG_ENABLED（SECRET_KEY 缺失或为弱默认时启动即失败）
│   ├── database.py                     engine/session；SQLite 启用 WAL + busy_timeout=30s；
│   │                                   utcnow_naive()（应用层统一写 naive UTC）
│   ├── requirements.txt                Python 依赖
│   ├── contract.db                     ★ 当前实际运行数据库（SQLite，git 忽略）
│   ├── chroma_data/                    ★ ChromaDB 持久化目录（含 chroma.sqlite3，运行时构建）
│   │
│   ├── api/                            REST 路由层（共 51 个端点）
│   │   ├── auth.py                     注册/登录/用户列表/改角色/个人信息/个人 DeepSeek Key（7）
│   │   ├── contracts.py                ★ 主路由：上传·审核·复核·验收·定位·修改·修订·导出·
│   │   │                               审核结果·报告·条款比对（19）
│   │   ├── overview.py                 ★ 总体修改会话：总览 / 方案 plan / 方案查询 / 确认（5）
│   │   ├── feedback.py                 反馈池 → 学习层：提交/统计/池/规则池/经验/审核/批准/撤销（10）
│   │   ├── templates.py                标准条款模板 + 版本链（6）
│   │   ├── stats.py                    首页仪表盘统计（1）
│   │   └── deps.py                     角色守卫（require_role）、get_current_user、
│   │                                   require_llm_key_configured
│   │
│   ├── services/                       业务服务层
│   │   ├── auth.py                     密码哈希（bcrypt）+ JWT 签发/校验
│   │   ├── role_bootstrap.py           一次性产生第一个管理员（只在 0 个 admin 且账号已存在时提升）
│   │   ├── user_secret.py              个人 DeepSeek Key 的 Fernet 加密落库/解密/清除
│   │   ├── warmup.py                   静启动：后台线程预热 torch/向量模型/向量库 + 状态查询
│   │   ├── docx_reviser.py             ★ 修订版 DOCX 生成：锚点替换 + 新增条款插入 + 编号顺延 +
│   │   │                               采用态优先归并
│   │   ├── docx_converter.py            DOCX→PDF（Word COM → LibreOffice CLI → 报错）
│   │   ├── feedback_experience.py       反馈 → 经验构造（只写「应重点核查什么」，不写风险结论）
│   │   └── eval_isolation.py            评测语料隔离闸（三份评测语料 sha256 指纹比对）
│   │
│   ├── models/                         SQLAlchemy 模型（9 张表）
│   │   ├── user.py                     users（含 role / deepseek_api_key_enc）
│   │   ├── contract.py                 contracts
│   │   ├── audit_record.py             audit_records（含 clause_position / evidence /
│   │   │                               recommendation / result_status / learning_context）
│   │   ├── audit_report.py             audit_reports（含 report_html / missing_clauses）
│   │   ├── template.py                 templates（版本链 previous_version_id）
│   │   ├── feedback_log.py             feedback_logs（反馈池 + 复核/批准留痕）
│   │   ├── feedback_experience.py      feedback_experiences（学习层，status: active/revoked）
│   │   ├── clause_revision.py          clause_revisions（★ 修订会话；scope/operation/position/
│   │   │                               original_clause_text/adopted）
│   │   └── revision_proposal.py        revision_proposals（★ 总体方案，不参与 DOCX 导出）
│   │
│   ├── ai/                             AI 引擎
│   │   ├── taxonomy.py                 ★ 合同分类体系唯一真源（11 类启用 + 业务标签「服务外包」）
│   │   ├── llm_client.py               ★ 全项目唯一 LLM 出口（deepseek-chat；Key 优先级解析）
│   │   ├── llm_context.py              请求级个人 Key 绑定（ContextVar + submit_with_context）
│   │   ├── chunker.py                  按条款标题边界分块
│   │   ├── confidence.py               规则置信度表 + 多来源交叉验证上调
│   │   ├── reviser.py                  条款修订（Leader-Follower 三步）+ 新增条款起草
│   │   ├── utils.py                    JSON 抽取等通用工具
│   │   ├── parser/                     docx_parser / pdf_parser / ocr_parser（+ detect_and_parse）
│   │   ├── classifier/                 rag_classifier（RAG 少样本，★ 生产默认）+
│   │   │                               classifier（零样本分块投票，回退/基线）
│   │   ├── extractor/                  extract_elements（双方/金额/日期/期限/争议解决/适用法律）
│   │   ├── auditor/                    ★ 风险引擎
│   │   │   ├── rule_engine.py          R01–R13 正则 + 缺失型规则 + 安全模式/否定语境 + RULE_LAWS
│   │   │   ├── evidence_extractor.py   LLM 只抽事实证据（禁止输出风险结论）
│   │   │   ├── evidence_adjudicator.py Python 硬阈值裁决 R01–R12
│   │   │   ├── recommendation_engine.py 建议层：法条精确取条文 + LLM 建议 + grounding 检查
│   │   │   └── llm_auditor.py          ⚠️ 已归档：只被 evaluate/evaluate_risks.py 引用，
│   │   │                               不在生产链路
│   │   ├── matcher/                    compare_clauses（标准条款比对 + 跨条款图分析）
│   │   ├── rag/                        __init__ / vector_store（ChromaDB + 句向量 + RRF 混合检索）/
│   │   │                               bm25 / init_chroma / feedback_store /
│   │   │                               init_feedback_experiences
│   │   └── knowledge/                  laws.json（161 条）/ standard_clauses.json（257 条，
│   │                                   11 个类型）/ risk_cases.json（★ 空数组，仅预留）
│   │
│   ├── evaluate/                       评测与数据集（9 个脚本）
│   │   ├── realtest.json               风险/要素测试集（84 份，含 elements/risks/business_risks）
│   │   ├── classification_test.json    分类正式测试集（147 条，Gold 冻结）
│   │   ├── evidence.json               预计算 LLM 证据抽取结果（84 个键）
│   │   ├── rag_predictions.json        分类预测快照
│   │   ├── cache.json / cache_elements.json / cache_risks_llm.json   评测缓存
│   │   ├── build_realtest.py           构建 realtest
│   │   ├── build_classification_test.py 构建分类测试集（147 = 84 + 55 + 8）
│   │   ├── deidentify.py               脱敏模块（法定代表人/手机号/身份证/合同编号）
│   │   ├── run_evidence.py             生成 evidence.json（调用冻结签名的 extract_evidence）
│   │   ├── evaluate_rag.py             ★ 分类正式评测
│   │   ├── evaluate_elements.py        ★ 要素正式评测
│   │   ├── evaluate_evidence.py        ★ 风险正式评测（证据 + 生产裁决函数）
│   │   ├── evaluate_risks.py           双引擎六列对照（非正式指标；引用 llm_auditor）
│   │   ├── evaluate_classifier.py      零样本分类基线（非正式指标）
│   │   └── _pilot5/                    试点阶段留存的若干合同正文 txt
│   │
│   ├── tests/                          后端单测（unittest，29 个测试文件 / 288 个测试方法）
│   │   ├── test_permissions.py         多用户权限隔离（10）
│   │   ├── test_user_management.py     角色管理 + bootstrap（13）
│   │   ├── test_user_api_key.py        个人 Key 解析/回退/隔离（14）
│   │   ├── test_api_key_config.py      Key 配置安全边界（7）
│   │   ├── test_overview_session.py    总体修改会话（21）
│   │   ├── test_revision_export.py     修订会话 + 修订版 DOCX（39）
│   │   ├── test_revise_link.py         风险/比对 → 改条款链路（6）
│   │   ├── test_add_clause.py          新增条款（18）
│   │   ├── test_locate_clause.py       只读条款定位（18）
│   │   ├── test_feedback_rag.py        Feedback RAG（29，离线）
│   │   ├── test_feedback_experience.py 反馈→经验生命周期（22）
│   │   ├── test_warmup.py              静启动（6）
│   │   ├── test_bug_0xx*.py            BUG 回归（17 个文件，共 85）
│   │   └── feedback_test_utils.py      离线测试夹具（非测试）
│   │
│   └── data/                           上传合同存储（运行时生成，git 忽略；
│                                       └── deleted/ 软删除归档目录）
│
├── frontend/                           Vue 3 + Element Plus + Vite
│   ├── index.html / vite.config.js / package.json
│   └── src/
│       ├── main.js                     入口（ElementPlus + 主题 + 路由）
│       ├── App.vue                     应用壳：导航菜单（按角色显隐）+ 返回条
│       ├── router/index.js             13 条路由 + 唯一守卫（token + 游客模式）
│       ├── views/
│       │   ├── HomeView.vue            首页（Hero + KPI + 核心能力）
│       │   ├── LoginView.vue           登录/注册 + 游客模式开关
│       │   ├── ContractUpload.vue      合同上传
│       │   ├── ContractList.vue        合同列表
│       │   ├── ContractDetail.vue      ★ 合同详情工作台（5 Tab 页面壳）
│       │   ├── AuditResult.vue         审核历史列表
│       │   ├── AuditResultDetail.vue   单合同风险明细
│       │   ├── AuditReport.vue         审核报告列表 + 内联预览（ECharts）
│       │   ├── AuditReportDetail.vue   ★ 独立完整审核报告（支持打印）
│       │   ├── TemplateList.vue         标准条款模板管理
│       │   ├── ProfileView.vue         个人信息 + DeepSeek Key
│       │   ├── UserManageView.vue      用户管理（管理员）
│       │   ├── NotFoundView.vue         404
│       │   └── contract-detail/         ★ 工作台子组件（11 个）
│       │       ├── OriginalTextPanel.vue   原始文本 Tab：条款导航 + 正文 + 搜索 + 风险标注
│       │       ├── RiskPanel.vue           风险详情 Tab：统计 + 筛选 + 风险卡 + 反馈
│       │       ├── ComparisonPanel.vue     条款比对 Tab：缺失/偏离/已覆盖
│       │       ├── RevisionWorkbench.vue   修改合同 Tab：三栏工作台
│       │       ├── ReportPanel.vue         ★ 审核报告 Tab：规则扫描 + 条款完整性 + 重点风险
│       │       ├── OverviewPlanPanel.vue   总体会话总控台（方案逐项处理）
│       │       ├── LocateClausePanel.vue   定位面板（条款号 / 搜索原文 / 描述位置）
│       │       ├── InlinePositionPicker.vue 插入位置选择器（5 种模式）
│       │       ├── AddClauseWizard.vue     ⚠️ 新增条款对话框壳（存在已知缺陷，见下）
│       │       ├── AddClauseSteps.vue      ⚠️ 仅被 AddClauseWizard 引用
│       │       └── ClauseDiffView.vue      差异对比（2 栏 / 3 栏）
│       ├── components/                 RiskBadge.vue / FeedbackPanel.vue
│       ├── composables/
│       │   ├── useContractWorkspace.js  ★ 工作台全部状态与逻辑（含 API 编排）
│       │   ├── workspaceLogic.js        ★ 纯逻辑（会话分组/导出状态机/位置规则，可 node 直测）
│       │   └── useFeedback.js           反馈标注逻辑
│       ├── constants/
│       │   ├── riskTypes.js            ★ 风险代码→中文名（R01–R13）唯一前端真源
│       │   ├── reportTokens.js         报告双页共用展示 token + 文案清理
│       │   ├── navigation.js           ★ Tab/锚点/返回位置快照约定（可 node 直测）
│       │   └── contractTypes.js        合同类型展示
│       ├── api/                        contract.js / template.js / profile.js / user.js /
│       │                               timeouts.js（分端点长超时）+ *.test.mjs
│       ├── utils/                      request.js（axios + JWT 拦截）/ format.js
│       └── styles/theme.css            Element Plus 主色覆盖 + A24 品牌 token + 通用类
│
├── docs/                               项目文档（见文末索引）
├── 02_项目文档/总体会话-后端API契约.md   总体修改会话的字段级接口契约（前端对接用）
├── A24-详细设计文档-v4.0.md             申报交付物（详细设计文档）
├── README.md                           项目入口说明
├── start.bat / start-backend.bat / start-frontend.bat   启动脚本
├── docker-compose.yml                  ⚠️ 仅 1 个服务 chromadb（当前未使用，见下）
├── deploy/                             ⚠️ 仅 .gitkeep，无任何部署脚本
├── mysql_data/                         ⚠️ 早期 MySQL 尝试遗留，git 忽略，当前不使用
├── chroma_data/                        ⚠️ 空占位（仅 .gitkeep）；实际向量库在 backend/chroma_data/
├── data/                               空占位（实际上传文件在 backend/data/）
├── sketches/                           早期 UI 设计草图（HTML）
├── 答辩PPT/                            答辩材料
├── .env                                本地配置（git 忽略；含 DEEPSEEK_API_KEY / SECRET_KEY /
│                                       DATABASE_URL=sqlite:///./contract.db）
└── .gitignore
```

---

## 当前文档索引（`docs/`）

| 文档 | 内容 |
|---|---|
| `系统架构.md` | 分层架构、技术栈、数据模型、权限体系、状态机、已废弃组件、真实局限 |
| `审核流程.md` | 上传 → 解析 → 分类 → 要素 → 规则 → 证据 → 裁决 → 建议 → 比对 → 报告 的逐步说明 |
| `系统功能说明.md` | 页面与路由、每个 Tab 的功能、API 一览、交互约定、功能边界 |
| `风险规则说明.md` | R01–R13 名称/等级/触发条件/硬阈值/法条映射 + Gold 覆盖情况 |
| `条款修改与修订机制.md` | 两种修改范围、多轮会话、采用态语义、DOCX 修订版导出、R09 补充 |
| `审核报告与可视化.md` | 审核报告 Tab、独立完整审核报告、13 类规则扫描、条款完整性、导航与位置记忆 |
| `数据集说明.md` | 分类/风险数据集规模与来源结构、Gold 规模与分布、脱敏、泄漏排查 |
| `评测方法.md` | 三套正式评测的脚本/公式/冻结指标 + 非正式脚本说明 |
| `风险标注与Gold规范.md` | 标注原则、边界规则、三条上位规则、质控（含如实的方法学说明） |
| `项目目录说明.md` | 本文件 |
| `部署与复现.md` | 环境、依赖、`.env`、启动、测试、评测复现、FAQ |
| `参考论文与技术依据.md` | 可核验的公开文献与技术点映射 |
| `变更记录.md` | 变更台账 + 文档影响速查 + 待办 |

---

## 已删除 / 已归档 / 不存在的组件（**不得写入当前架构**）

| 名称 | 当前状态 | 说明 |
|---|---|---|
| **Corex 多 Agent** | **已删除**（git 历史可证） | 原「多 Agent 交叉验证」已由「证据抽取 + 确定性裁决 + 多来源置信度融合」取代 |
| **`reporter/`（报告生成 Agent）** | **已删除** | 报告组装已内联到 `api/contracts.py`（`_build_risk_rows` / `_build_compare_rows`） |
| **Dify** | **已删除** | `dify_client.py` 与 Dify 样例已移除；`.env` 中残留的 `DIFY_API_KEY` **不被任何代码读取** |
| **KNN classifier** | **已删除** | 分类统一走 RAG 少样本 + 零样本兜底；`rag_classifier.py` docstring 中的「kNN」只是方法背景说明 |
| **`audit_with_llm`** | **已归档** | 旧「precise = 规则 + LLM 直审」口径，现只被 `evaluate/evaluate_risks.py` 引用，**不在生产链路** |
| **报告导出（HTML/PDF/Markdown 接口）** | **不存在** | 报告为在线页面；完整报告页只提供浏览器打印。**注意**：系统确有 **DOCX 修订版合同导出**（`GET /contracts/{id}/revised-docx`），那是合同文件而非报告 |
| **heatmap 端点** | **不存在** | 风险等级分布随 `/audit-report` 的 `risk_heatmap_data` 一起返回 |
| **`backend/schema.sql`** | **不存在** | 建表由 SQLAlchemy `Base.metadata.create_all` + `main.py::_ensure_columns` 完成 |
| **`backend/routers/`、`backend/schemas/`** | **不存在** | 路由在 `backend/api/`，直接挂到 `main.py` |
| **`_inspect_data.py`** | **不存在** | 历史文档中的死引用 |
| **完整 Docker 化部署** | **不存在** | 根 `docker-compose.yml` 只有 1 个 `chromadb` 服务；`deploy/` 为空目录 |

### 代码中仍存在的历史残留（文档已声明删除，但代码未清干净）

如实列出，便于后续收尾；**它们不影响功能，也不代表这些组件仍在架构中**：

| 位置 | 残留内容 |
|---|---|
| `backend/models/audit_record.py` | 仍有列 `corex_agent_log`（注释已注明「Corex 已归档，不得恢复其语义」，保留仅为历史数据兼容） |
| `backend/api/contracts.py::_build_evidence` | 仍有 `if method == "corex_review"` 分支（当前不会产生该 `detection_method`） |
| `backend/api/contracts.py` | 仍有 `corex_agent_log=r.get("corex_agent_log")` 的写入（值恒为 `None`） |
| `backend/ai/chunker.py` / `utils.py` / `confidence.py` | 注释中提及 `Corex` / `corex.orchestrator` / `corex` 来源 |
| `backend/ai/confidence.py::enrich_confidences` | ≥3 来源的 +0.12 上调分支：当前生产只有 `rule` 与 `evidence` 两种来源，该分支实际不会触发 |
| 前端 `constants/riskTypes.js` | 保留 `corex_review → 多智能体验证` 的中性标签映射，仅供历史数据展示 |
| 根 `chroma_data/` | 空占位目录；实际向量库在 `backend/chroma_data/` |

---

## 已知前端缺陷（本轮为文档重构，未修改代码）

| 位置 | 现象 |
|---|---|
| `views/contract-detail/AddClauseWizard.vue` | 根节点 `v-if="mode === 'dialog'"` 引用了**未定义**的 `mode`（该组件只声明 `ws` 一个 prop），因此对话框在运行时不渲染；`AddClauseSteps.vue` 只被它引用，也随之不显示。**可用的新增条款路径是「修改合同」Tab 右栏的内联流程**（`InlinePositionPicker` + 「生成推荐草案」/「确认新增」）。 |
| `views/TemplateList.vue` | `openHistory()` 调用 `getTemplateHistory()`，但该函数**未被导入**；异常被自身空 `catch` 吞掉，界面表现为「暂无历史版本」。 |
| 前端依赖 | `package.json` 声明的 `docx-preview` 与 `mammoth` 在 `src/` 中**无任何引用**（无功能影响）。 |
| `vite.config.js` | 配置了 `/api` 开发代理，但 `utils/request.js` 使用**绝对** `baseURL`，请求不经过该代理，跨域由后端 `CORS_ORIGINS` 放行。 |
| 总体会话确认 | 「纳入方案」仅为前端会话状态，当前 UI **未调用** `POST /overview/confirm`（该端点已实现并有测试覆盖）。 |
