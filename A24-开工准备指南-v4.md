# A24 合同智能审核系统 — 超级详细开工指南

> 版本：v2.0（配合设计文档 v3.1 / 开工日期 7.17）
> 日期：2026年7月17日
> 原则：**每天每人都有具体任务、具体文件、具体代码、具体验证方法。照着做就行，不用动脑。**

---

## 团队分工总览

| 代号 | 角色 | 负责模块 | 一句话职责 |
|:---:|------|---------|---------|
| A | 前端 | 合同上传页、合同列表页、合同详情页、反馈标注面板 | 用户能看到的所有"合同管理"界面 |
| B | 前端 | 审核结果页、审核报告页、ECharts 风险热力图、pdf.js 原文高亮 | 用户能看到的所有"审核结果"界面 |
| C | 后端 | FastAPI 路由、SQLAlchemy 模型、JWT 认证、审核调度、全部 API | 前后端之间的所有数据通道 |
| D | AI 引擎 | 文档解析器、合同分类器、要素抽取、规则引擎、Corex 多 Agent | AI 审核的前半段（从文件到风险） |
| E | AI 引擎 | ChromaDB 知识库、RAG 检索、条款比对、审核报告生成、Dify 工作流 | AI 审核的后半段（从知识到报告） |

**协作边界约定：**
- C 写的 `services/audit_service.py` 直接 `from ai.auditor import audit_contract`，AI 模块是 Python 函数调用，不是 HTTP 微服务
- D 和 E 开发的 AI 函数，由 C 在审核调度中串联：解析 → 分类 → 抽取 → 规则引擎 → RAG 审核 → Corex → 比对 → 报告
- A 和 B 各自负责独立页面，路由不冲突，组件可互相引用
- 每天 21:00 飞书/钉钉群发当日进度（模板：`[7.17] A: 完成首页布局 ✓ | B: ECharts 学习中 | C: 建表完成，接口写了 3 个 | D: 解析器完成 docx | E: ChromaDB 初始化成功`）

---

## 通用篇：全员 Day 0 环境搭建（7.17 周五）

> 这一天只有一个目标：**5 个人的电脑都能把前后端跑起来，浏览器能看到页面。**

### Step 1：安装基础工具（全员，30 分钟）

**Git：**
```bash
# 下载安装 https://git-scm.com/downloads
# 装完验证
git --version   # 应显示 git version 2.40+
```

**VS Code：**
```bash
# 下载安装 https://code.visualstudio.com/
# 必装插件（点左侧 Extensions 图标搜索安装）：
#   - Chinese Language Pack（中文界面）
#   - GitLens（Git 可视化）
#   - Vue（A 和 B 必装）
#   - Python（C、D、E 必装）
#   - Thunder Client（C 必装，调 API 用，比 Postman 轻量）
```

**Node.js（A 和 B）：**
```bash
# 下载 LTS 版本 https://nodejs.org/ （v20.x）
node --version   # 应显示 v20.x.x
npm --version    # 应显示 10.x.x
```

**Python（C、D、E）：**
```bash
# 下载 3.11.x https://www.python.org/downloads/
# 安装时一定勾选 "Add Python to PATH"
python --version   # 应显示 Python 3.11.x
```

**Docker Desktop（全员，但 C/D/E 必须装，A/B 可不装）：**
```bash
# 下载 Docker Desktop https://www.docker.com/products/docker-desktop/
# Windows 用户：安装后按引导启用 WSL2（Docker Desktop 自动处理）
# 装完验证
docker --version    # 应显示 Docker version 24.x+
docker ps           # 不报错 = ✅（空列表也正常）
```

### Step 2：克隆仓库 + 初始化项目（全员，60 分钟）

> VS Code 的 Git 功能已经足够日常使用，以下同时给出终端方式和 VS Code 方式。**推荐新手用 VS Code 方式。**

**前置操作 — 配置 VS Code Git（每人做一次，2 分钟）：**

1. 打开 VS Code → 左侧点第三个图标（源代码管理，或按 `Ctrl+Shift+G`）
2. 如果提示"未找到 Git"，点"安装 Git"或检查 Git 是否已装好
3. 文件 → 首选项 → 设置 → 搜 "Git: Enabled" → 确保勾选
4. 按 `Ctrl+Shift+P` → 输入 "Git: Clone" → 这就是以后克隆仓库的入口

> 以下命令由组长（假设是 C）先执行，其他人等组长推送后再 clone。

---

**C 执行（组长，创建项目骨架）：**

<details>
<summary><b>终端方式</b>（点开查看）</summary>

```bash
cd ~/Desktop
mkdir a24-contract-auditor
cd a24-contract-auditor
git init
git checkout -b main

# 创建目录骨架
mkdir -p backend/api backend/models backend/services backend/ai/parser backend/ai/classifier
mkdir -p backend/ai/extractor backend/ai/auditor backend/ai/matcher
mkdir -p backend/ai/reporter backend/ai/knowledge backend/ai/rag backend/ai/corex
mkdir -p frontend/src/views frontend/src/components frontend/src/api frontend/src/router
mkdir -p data docs deploy

# 创建 .gitignore
cat > .gitignore << 'GITEOF'
venv/
__pycache__/
*.pyc
node_modules/
dist/
.env
*.db
backend/uploads/
data/*.docx
data/*.pdf
data/models/
chroma_data/
.DS_Store
Thumbs.db
.vscode/
.idea/
GITEOF

cat > .env.example << 'ENVEOF'
DEEPSEEK_API_KEY=sk-your-key-here
DIFY_API_KEY=app-your-key-here
DATABASE_URL=sqlite:///./contract.db
SECRET_KEY=change-me-to-random-string
ENVEOF

cat > docker-compose.yml << 'DCEOF'
# ChromaDB 向量数据库 — 开发环境（前后端跑宿主机）
services:
  chromadb:
    image: chromadb/chroma:0.5.23
    ports:
      - "8001:8000"
    volumes:
      - ./chroma_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE
    restart: unless-stopped
DCEOF

mkdir -p chroma_data

# 初始化前端
npm create vue@latest frontend-tmp
# 全部选 No，Vue Router 选 Yes
# 把 frontend-tmp 内容移到 frontend/，删掉 frontend-tmp

git add .
git commit -m "init: project skeleton"
```
</details>

**VS Code 方式（推荐）：**

1. 在电脑上手动创建文件夹 `~/Desktop/a24-contract-auditor`
2. VS Code → 文件 → 打开文件夹 → 选择这个文件夹
3. 按 `Ctrl+Shift+G` 打开源代码管理面板 → 点"初始化仓库"
4. 在 VS Code 左侧文件面板里右键创建目录骨架（backend/api、frontend/src/views 等，同上）
5. 右键新建文件创建 `.gitignore` 和 `.env.example`（内容同上）
6. 源代码管理面板会显示所有更改 → 在 Message 框输入 `init: project skeleton` → 点 ✓ 提交

---

**C 创建 GitHub 仓库并推送：**

1. 打开 https://github.com → 登录 → 右上角 "+" → New repository
2. Repository name: `a24-contract-auditor` → Private → Create

<details>
<summary><b>终端方式</b></summary>

```bash
git remote add origin https://github.com/你的用户名/a24-contract-auditor.git
git branch -M main
git push -u origin main
git checkout -b dev
git push -u origin dev
```
</details>

**VS Code 方式：**
- 按 `Ctrl+Shift+P` → 输入 "Git: Add Remote" → 输入 `https://github.com/你的用户名/a24-contract-auditor.git` → 命名为 `origin`
- 源代码管理面板 → 点右上角 `...` → 分支 → 创建分支 → 输入 `dev`
- 之后每次提交完，点底部状态栏的 ↕ 同步按钮即可 push

---

**其他 4 人（等组长推送后执行）：**

**VS Code 方式（最简单）：**
1. 打开 VS Code → 欢迎页 → "克隆 Git 仓库"（或 `Ctrl+Shift+P` → "Git: Clone"）
2. 输入组长给的仓库地址 `https://github.com/组长用户名/a24-contract-auditor.git`
3. 选择存到桌面 → 点"打开" 
4. 打开后点左下角当前分支名 → 选择 `dev` 分支

<details>
<summary><b>终端方式</b></summary>

```bash
cd ~/Desktop
git clone https://github.com/组长用户名/a24-contract-auditor.git
cd a24-contract-auditor
git checkout dev
```
</details>

---

### Step 2.5：VS Code Git 速查卡（日常最常用的 5 个操作）

> 每天开发时只需要记住这 5 个操作，全部在 VS Code 里完成，不用开终端。

| 你要做什么 | VS Code 操作 | 快捷键 |
|-----------|------------|:-----:|
| **拉取最新代码** | 点底部状态栏左下角的 ↕ 同步按钮，或 `...` → 拉取 | 点一下 |
| **查看改了什么** | 按 `Ctrl+Shift+G` 打开源代码管理面板，所有修改的文件都列在那里。点文件名看差异对比 | `Ctrl+Shift+G` |
| **暂存 + 提交** | 在源代码管理面板里，点文件旁边的 `+`（暂存）→ 在 Message 框写提交说明 → 点 ✓ | 鼠标操作 |
| **推送** | 点底部状态栏 ↕ 同步按钮 | 点一下 |
| **切换分支** | 点左下角当前分支名 → 在列表里选目标分支 | 点两下 |

**每天的标准流程（30 秒）：**

```
到公司 → 打开 VS Code → 点左下角 ↕ 拉取最新代码
       → 写代码
       → Ctrl+Shift+G 看看改了哪些文件
       → 点 + 暂存 → 写 commit message → 点 ✓
       → 点 ↕ 推送
```

**遇到冲突怎么办：** VS Code 会在源代码管理面板标出冲突文件（红色 C 标记）。点开冲突文件 → VS Code 会用不同颜色标出"你的版本"和"远程版本"→ 点"保留双方"或手动选择 → 保存 → 点 + 暂存标记为已解决 → 提交。

### Step 3：安装项目依赖（按角色分头执行，30 分钟）

**A 和 B（前端）：**
```bash
cd ~/Desktop/a24-contract-auditor/frontend
npm install
npm install element-plus @element-plus/icons-vue axios echarts pdfjs-dist
npm run dev
# 浏览器打开 http://localhost:5173 → 看到 Vue 欢迎页 = ✅
```

**C、D、E（后端+AI）：**
```bash
cd ~/Desktop/a24-contract-auditor/backend
python -m venv venv

# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate
# 看到命令行前面有 (venv) = ✅

pip install fastapi==0.110.0 uvicorn[standard]==0.29.0 sqlalchemy==2.0.30 pymysql==1.1.1 pydantic==2.7.0
pip install python-jose[cryptography]==3.3.0 passlib[bcrypt]==1.7.4 python-multipart==0.0.9 httpx==0.27.0
pip install torch==2.3.0 transformers==4.41.0 onnxruntime==1.18.0
pip install chromadb==0.5.0 sentence-transformers==2.7.0
pip install python-docx==1.1.0 pdfplumber==0.10.0 pypdf2==3.0.0
pip install paddlepaddle==3.0.0 paddleocr==2.8.0
pip install openai==1.30.0  # DeepSeek API 调用
pip freeze > requirements.txt
```

### Step 3.5：启动 Docker 服务（C/D/E，5 分钟）

> ChromaDB 向量数据库用 Docker 跑，不需要每个人在本地编译它的 C++ 扩展。chromadb Python 包只作为 HTTP 客户端连接 Docker 里的服务。

```bash
cd ~/Desktop/a24-contract-auditor
docker compose up -d

# 验证 ChromaDB 已启动
docker ps | grep chromadb
# 应该看到一行 chromadb 容器，STATUS 是 Up
# 浏览器访问 http://localhost:8001/api/v1/heartbeat → 看到 {"nanosecond heartbeat":...} = ✅
```

> 以后每次开机开发前，先 `docker compose up -d` 把 ChromaDB 拉起来。前后端代码照常在宿主机跑，不需要进容器。

### Step 4：验证前后端连通（全员一起，30 分钟）

**C 创建第一个后端文件 `backend/main.py`：**
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="A24 合同审核系统", version="0.1.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/")
def root():
    return {"message": "A24 合同智能审核系统 v0.1.0"}

@app.get("/api/health")
def health():
    return {"status": "ok"}
```

**C 启动后端：**
```bash
cd ~/Desktop/a24-contract-auditor/backend
source venv/bin/activate  # Windows: venv\Scripts\activate
uvicorn main:app --reload --host 0.0.0.0 --port 8080
# 看到 "Uvicorn running on http://0.0.0.0:8080" = ✅
# 浏览器访问 http://localhost:8080/docs → 看到 Swagger 页面 = ✅
```

**A 验证前后端连通——修改 `frontend/vite.config.js`（添加代理）：**
```js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

**A 修改 `frontend/src/App.vue`，加一个测试按钮：**
```vue
<template>
  <div style="padding:50px;text-align:center">
    <h1>A24 合同智能审核系统</h1>
    <p>后端状态：{{ backendStatus }}</p>
    <el-button type="primary" @click="checkBackend">测试后端连通性</el-button>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import axios from 'axios'

const backendStatus = ref('未检测')

async function checkBackend() {
  try {
    const res = await axios.get('/api/health')
    backendStatus.value = '✅ 后端连通！' + JSON.stringify(res.data)
  } catch (e) {
    backendStatus.value = '❌ 后端未启动，请确认 uvicorn 在运行'
  }
}
</script>
```

**验证标准：** 浏览器打开 http://localhost:5173 → 点击按钮 → 看到 "✅ 后端连通！{"status":"ok"}" = ✅✅✅

**D 验证 DeepSeek API（在 backend/ 下创建 `test_llm.py`）：**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-你的DeepSeek-API-Key",
    base_url="https://api.deepseek.com"
)

response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": "一句话说明合同审核的核心要点"}]
)
print(response.choices[0].message.content)
# 能返回一句通顺的中文 = ✅
```

**E 验证 ChromaDB（在 backend/ 下创建 `test_chroma.py`）：**
```python
import chromadb
# 连接 Docker 里的 ChromaDB 服务（不是本地嵌入模式）
client = chromadb.HttpClient(host='localhost', port=8001)

# get_or_create 避免重复创建报错
collection = client.get_or_create_collection("test")
collection.add(documents=["测试文本，合同审核系统"], ids=["1"])
results = collection.query(query_texts=["测试"], n_results=1)
print(results["documents"])
# 输出 [['测试文本，合同审核系统']] = ✅
```

### Step 5：注册必需的云服务账号（全员，Day 0 内完成）

| 服务 | 谁需要 | 网址 | 做什么 |
|------|:-----:|------|------|
| DeepSeek API | D、E、C | https://platform.deepseek.com | 注册 → 创建 API Key → 充值 20 元 |
| Dify Cloud | E、D | https://cloud.dify.ai | 注册 → 创建工作流应用 |
| GitHub | 全员 | https://github.com | 注册 → 告诉组长用户名 |

## AI 辅助开发篇：用 Cowork 加速开发（全员必读）

> 本次开发全程有 AI 辅助工具 Cowork（桌面 AI 编程助手）可用。**把重复、机械的代码工作交给 AI，把精力留给架构设计和业务逻辑。** 以下按角色说明 AI 使用方式。

### 什么是 Cowork？

Cowork 是一个桌面 AI 编程助手，可以直接在你的项目文件夹中工作——读代码、写代码、执行命令、调试错误。每个团队成员都应该在自己的电脑上打开 Cowork，把项目文件夹挂载进去。

**核心能力：**
- 根据自然语言描述生成完整代码文件（Vue 组件、Python 模块、SQL 脚本等）
- 读取和理解现有代码，在此基础上修改或扩展
- 执行命令行操作（安装依赖、运行测试、启动服务）
- 调试报错——把错误信息贴给它，它能定位问题并给出修复方案
- 生成 Git commit message、写注释、整理文档

### 通用使用技巧（5 个人都适用）

**1. 说清楚上下文，不要只给一句话指令。**

❌ 差的提示："帮我写一个登录页面"
✅ 好的提示："在 frontend/src/views/ 下创建 LoginView.vue，使用 Element Plus 的 el-form 组件，包含用户名和密码两个字段，调用 POST /api/auth/login 接口，登录成功后把 JWT token 存 localStorage 并跳转到 /dashboard"

**2. 把相关文件打开后再提问。** Cowork 能看到你当前打开的文件内容。修改现有代码前，先把被修改的文件和相关依赖文件打开，AI 生成的代码会更准确。

**3. 分步来，不要一口气。** 不要期望 AI 一次性生成 500 行的完整模块。正确做法：先让它写类定义和接口 → 确认方向对 → 再让它逐个实现方法 → 最后让它写测试。

**4. 生成的代码一定要自己读一遍再提交。** AI 生成的代码可能包含不存在的 API、错误的数据结构、或不符合项目约定的命名。**提交前必须：** 本地跑通 → 人工检查关键逻辑 → 删除 AI 编造的注释。

### 分角色 AI 使用场景

#### 同学 A、B（前端 — Vue 3 + Element Plus）

| 场景 | 怎么让 Cowork 帮你 | 示例提示词 |
|------|-------------------|-----------|
| 创建新页面 | 告诉它页面路径、使用的组件库、页面结构 | "在 frontend/src/views/ 下创建 ContractUpload.vue，使用 Element Plus 的 el-upload、el-form、el-button。上传组件支持拖拽.docx/.pdf，表单包含合同名称和合同类型下拉框" |
| 写 API 调用层 | 告诉它后端接口的路径和参数 | "在 frontend/src/api/ 下创建 contract.js，封装以下接口：POST /api/contracts/upload、GET /api/contracts、GET /api/contracts/:id，使用 axios，统一处理 JWT token 注入和 401 跳转登录页" |
| 调 ECharts 图表 | 告诉它数据结构和想要的图表类型 | "在 AuditReport.vue 中，用一个 ECharts 饼图展示风险等级分布（high/medium/low 三类），数据从 props.riskSummary 取，格式是 {high: 3, medium: 7, low: 5}" |
| Element Plus 组件不熟 | 直接问 | "Element Plus 的 el-table 怎么做多选 + 批量操作？给我完整代码" |
| 页面布局调试 | 把当前页面代码给它，描述想要的布局 | "当前这个 ContractList.vue 的搜索栏和表格之间间距太大，帮我调整成紧凑布局，搜索条件在一行显示" |

**创建项目并关联 Git：**
1. 在 Cowork 中打开 frontend 文件夹
2. 对 Cowork 说："在当前项目中初始化 Vue 3 项目，使用 Vite 构建，安装 element-plus、axios、echarts、pdfjs-dist 依赖"
3. Cowork 执行完后，在 VS Code 中按 `Ctrl+Shift+G` → 查看修改 → 点 + 暂存全部 → Message 框写 `init: Vue 3 前端项目初始化` → 点 ✓ → 点底部 ↕ 推送

#### 同学 C（后端 — Python FastAPI）

| 场景 | 怎么让 Cowork 帮你 | 示例提示词 |
|------|-------------------|-----------|
| 创建 API 路由 | 告诉它路由文件路径、接口列表、请求/响应格式 | "在 backend/api/ 下创建 contract_router.py，实现以下端点：GET /api/contracts（分页列表）、POST /api/contracts/upload（文件上传）、GET /api/contracts/{id}（详情）。使用 FastAPI 的 APIRouter" |
| 写 SQLAlchemy 模型 | 给它表结构描述 | "在 backend/models/ 下创建 contract.py，定义 Contract 模型，字段包括：id（UUID主键）、filename、file_path、contract_type、status、uploader_id、created_at。使用 SQLAlchemy declarative_base" |
| JWT 认证 | 告诉它认证流程 | "在 backend/api/ 下创建 auth_router.py，实现 POST /api/auth/register 和 POST /api/auth/login。密码用 bcrypt 哈希，登录成功后返回 JWT token（payload 含 user_id，有效期 24h）。同时写一个 get_current_user 依赖注入函数用于保护其他路由" |
| 串联 AI 模块 | 告诉它调用顺序 | "在 backend/services/ 下创建 audit_service.py，实现 audit_contract 异步函数，按顺序调用：文档解析 → 分类 → 要素抽取 → 规则引擎 → RAG审核 → Corex Review → 比对 → 报告生成。每个步骤的异常单独捕获并记录，不阻塞后续步骤" |

#### 同学 D、E（AI 引擎 — Python）

| 场景 | 怎么让 Cowork 帮你 | 示例提示词 |
|------|-------------------|-----------|
| 文档解析器 | 告诉它支持的格式和输出格式 | "在 backend/ai/parser/ 下创建 docx_parser.py，使用 python-docx 读取 .docx 文件，提取全文文本和段落层级结构（标题/正文/表格），输出为 {'paragraphs': [{'text': ..., 'style': 'heading1'}, ...]}" |
| 调 LLM API | 告诉它用哪个模型、什么参数 | "在 backend/ai/classifier/ 下创建 classifier.py，使用 openai 库调用 DeepSeek-V3 API（base_url='https://api.deepseek.com'），System Prompt 要求将合同分为 5 类（采购/销售/保密协议/服务外包/劳动），temperature=0.1，输出为 JSON" |
| ChromaDB 操作 | 告诉它数据格式和检索逻辑 | "在 backend/ai/rag/ 下创建 vector_store.py，封装 ChromaDB 操作：初始化集合、批量添加文档（sentence-transformers 向量化）、语义检索（返回 Top-20 候选）、Cross-Encoder 重排序（精排为 Top-5）" |
| 规则引擎 | 给它规则定义 | "在 backend/ai/auditor/ 下创建 rule_engine.py，实现 12 条合同风险检测规则（R01-R12），每条规则是一个函数，输入合同文本，输出 {risk_type, level, clause_text, reason} 或 None" |
| 搭建 Dify 工作流 | 描述工作流节点和逻辑 | "帮我在 Dify 中搭建一个合同风险初审工作流：开始节点 → 知识检索节点（查《民法典》条文）→ LLM 节点（DeepSeek-V3，按 12 类风险逐条分析）→ 结束节点输出 JSON。给我每一步的配置截图说明" |

### Cowork + VS Code Git 协作流程

整个团队使用 Cowork 辅助开发时，推荐在 VS Code 内完成全部 Git 操作（不用开终端）：

```
1. 在 VS Code 中写好功能描述注释
       ↓
2. 打开 Cowork，把相关文件拖入上下文
       ↓
3. 对 Cowork 说："根据注释实现 XXX 功能"
       ↓
4. Cowork 生成代码 → 本地跑通验证
       ↓
5. 人工审查代码逻辑，修正 AI 的错误
       ↓
6. Ctrl+Shift+G → 查看修改 → 点 + 暂存 → 写 commit message → 点 ✓
       ↓
7. 点底部 ↕ 同步按钮推送到 GitHub
       ↓
8. 对 Cowork 说："帮我写一个规范的 commit message，描述刚才的改动"（把 AI 写的粘贴到 VS Code 的 Message 框）
```

**关键纪律：** AI 生成的代码必须经过人工审查才能提交。不要在没读过代码的情况下直接 commit AI 的输出。AI 是你的"副驾驶"，不是"自动驾驶"——方向你来定，油门你来踩，AI 只负责帮你换挡。

### 常见坑和解决方案

| 问题 | 解决方案 |
|------|---------|
| AI 生成了不存在的 API | 检查 import 语句，确保所有库都已安装 |
| AI 写的代码风格不统一 | 在提示词中指定风格要求（如"使用 async/await，不用 .then()"）|
| AI 编造了假数据或假接口 | 确认所有外部调用（API 端点、数据库表名）与项目实际一致 |
| 长文件 AI 看不到全貌 | 分模块提问，每次只让 AI 处理 200 行以内的代码 |
| AI 生成的 Element Plus 属性不存在 | 让 AI 重新生成前加上"请使用 Element Plus 官方文档中的真实属性" |

---


## Day 1（7.18 周六）— 设计文档通读 + 骨架代码 + 原型草图

> 今日目标：每个人理解自己模块的边界和接口，骨架代码能 import 不报错，页面草图能讲清楚交互流程。

### A（前端 — 页面原型 + 路由 + 骨架页面）

1. 精读设计文档第六、七章（架构 + 创新亮点）
2. 在纸上或 Figma 画 5 个核心页面的原型草图：
   - 登录/注册页 → 首页 Dashboard → 合同上传页 → 合同列表页 → 合同详情页
3. 写 `frontend/src/router/index.js`，配好 5 条路由（含路由守卫，未登录跳 /login）
4. 在 `frontend/src/views/` 下创建 5 个骨架 .vue 文件（每个只写 `<template><div>页面名称</div></template>`）
5. 在 `frontend/src/api/` 下创建 `request.js`（axios 实例，baseURL + 拦截器骨架）

**✅ Day 1 A 验收：** `npm run dev` 后在浏览器手动改 URL 能跳到 5 个页面。

### B（前端 — 原型 + 图表试验）

1. 精读设计文档第六、七章 + 第五章（审核报告）
2. 画审核结果页 + 审核报告页的原型草图
3. 在 `frontend/src/views/` 下创建 AuditResult.vue 和 AuditReport.vue 骨架
4. 在 AuditReport.vue 里跑通一个 ECharts 测试图表（饼图，硬编码数据）
5. 安装 pdfjs-dist，在 AuditReport.vue 里渲染一页测试 PDF

**✅ Day 1 B 验收：** 页面能渲染一个 ECharts 饼图 + 一页 PDF。

### C（后端 — FastAPI 骨架 + 数据库连接）

1. 精读设计文档第三、九、十章（Corex、数据库、API）
2. 创建 `backend/main.py`：FastAPI 入口 + CORS 中间件 + 路由注册占位
3. 创建 `backend/config.py`：SQLite 数据库连接 + SessionLocal
4. 创建 `backend/api/__init__.py` + `backend/api/auth_router.py`（只有 router = APIRouter() 骨架）
5. 创建 `backend/models/__init__.py` + `backend/models/user.py`（User 模型骨架：id, username, password_hash, created_at）
6. 创建 `backend/services/__init__.py`
7. `uvicorn main:app --reload` 能启动，`/docs` 能看到 Swagger

**✅ Day 1 C 验收：** 浏览器访问 `/docs` 看到 Swagger 页面，`/api/health` 返回 `{"status": "ok"}`。

### D（AI — 模块骨架 + LLM 调通）

1. 精读设计文档第二、三章（分类、风险识别、Corex）
2. 在 `backend/ai/` 下全部 8 个子目录创建 `__init__.py`
3. 创建 `backend/test_llm.py`，用 openai 库调 DeepSeek API，能返回一句话
4. 安装全部 AI 依赖（paddlepaddle, paddleocr, sentence-transformers, chromadb, torch）
5. 创建 `backend/ai/parser/__init__.py` + `backend/ai/parser/docx_parser.py`（骨架函数 `def parse_docx(filepath) -> dict`）

**✅ Day 1 D 验收：** `python test_llm.py` 打印出 DeepSeek 返回的一句话。

### E（AI — 模块骨架 + ChromaDB 连通）

1. 精读设计文档第三、四章（RAG、条款比对）+ 设计文档 3.8 节（数据获取策略）
2. 在 `backend/ai/` 下全部子目录创建 `__init__.py`
3. 创建 `backend/ai/rag/__init__.py` + `backend/ai/rag/vector_store.py`（ChromaDB 连接 + 创建集合 + 写入测试向量 + 检索验证）
4. 安装全部 AI 依赖
5. 创建 `backend/ai/knowledge/` 目录，放入 5 条民法典合同编条文（.md 格式）
6. 注册 Dify 账号 + 创建空白工作流应用

**✅ Day 1 E 验收：** ChromaDB 检索返回测试结果；Dify 工作流应用创建成功。

### 依赖关系

- C 的 API 骨架今天必须跑通 → Day 2 A 才能调登录接口
- D 和 E 的 LLM/ChromaDB 今天必须调通 → Day 3-5 才能开始写业务代码
- **今晚 21:00 检查：后端 Swagger 页面截图 + 前端路由跳转截图 + DeepSeek API 返回截图，三张图发群里**


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**A — 创建 Vue 路由和骨架页面：**
> 在 frontend/src/router/index.js 中配置 Vue Router，创建 5 条路由：/login（LoginView）、/（HomeView）、/contracts/upload（ContractUpload）、/contracts（ContractList）、/contracts/:id（ContractDetail）。加路由守卫 beforeEach，检查 localStorage 是否有 token，没有就跳 /login。同时在 frontend/src/views/ 下创建这 5 个 .vue 骨架文件，每个只写 `<template><div>页面名称</div></template>`。

**B — ECharts 测试图表 + pdf.js 测试：**
> 在 frontend/src/views/ 下创建 TestChart.vue，使用 ECharts 渲染一个饼图，数据写死：{高风险: 3, 中风险: 7, 低风险: 5}。同时在这页面里用 pdfjs-dist 加载一个本地 test.pdf（放在 public/ 下），渲染第一页。安装 echarts 和 pdfjs-dist 依赖。

**C — FastAPI 骨架：**
> 创建 backend/main.py，初始化 FastAPI app，加 CORS 中间件（允许 localhost:5173），注册一个 /api/health 端点返回 {"status": "ok"}。创建 backend/config.py，用 SQLAlchemy 配置 SQLite 连接（sqlite:///./contract.db）。创建 backend/api/auth_router.py，定义一个 APIRouter(prefix="/api/auth")，先写一个空的 POST /register 骨架。

**D — DeepSeek API 调通：**
> 在 backend/ 下创建 test_llm.py，用 openai 库调 DeepSeek API（base_url='https://api.deepseek.com'，api_key 从环境变量 DEEPSEEK_API_KEY 读取），发送一句"用一句话说明合同审核的重点"，打印返回结果。

**E — ChromaDB 连接测试：**
> 在 backend/ai/rag/ 下创建 test_chroma.py，用 chromadb.HttpClient(host='localhost', port=8001) 连接 Docker 里的 ChromaDB 服务（确保先执行 docker compose up -d）。用 get_or_create_collection 创建一个名为 test 的集合，插入一条测试文档（"这是一条测试文本"），然后检索并打印结果。

---

## Day 2（7.19 周日）— 数据库建表 + 登录注册 + 首页 + Dify 工作流

> 今日目标：**浏览器能注册账号、登录、看到首页 + Dify 工作流搭建完成并可被后端调用。** 这是 Phase 1 的核心交付物。

### C（后端 — 数据库 + 认证接口，今天最关键的人）

1. 写 `backend/schema.sql`（8 张表完整建表 SQL：users, contracts, audit_records, audit_reports, audit_batch, templates, feedback_log, custom_rules）
2. 完善 `backend/models/user.py`（username, password_hash, email, role, created_at）
3. 创建 `backend/models/contract.py`（id UUID, filename, file_path, contract_type, status, uploader_id, created_at）
4. 写 `backend/api/auth_router.py`：
   - `POST /api/auth/register`（bcrypt 哈希密码 → 写入 users 表 → 返回 JWT）
   - `POST /api/auth/login`（验密码 → 生成 JWT，payload: user_id + exp 24h）
   - `GET /api/auth/me`（依赖注入 get_current_user，返回用户信息）
5. 测试：用 Swagger 调 register → login → /me，三个接口都通

**✅ Day 2 C 验收：** Swagger 里注册 → 拿到 token → 用 token 调 /me 返回用户信息。

### A（前端 — 登录注册页 + 首页）

1. 写 `frontend/src/views/LoginView.vue`：
   - el-card 居中布局，el-form（用户名 + 密码），登录/注册切换 tab
   - 调 `/api/auth/login`，成功存 token 到 localStorage → `router.push('/')`
2. 写 `frontend/src/views/HomeView.vue`（首页 Dashboard）：
   - 顶部：欢迎语 + 用户信息
   - 中部：四个统计卡片（今日审核数、待处理合同数、本月风险发现数、审核通过率——先写死，后面接真实数据）
   - 下部：最近合同列表（el-table，先写死数据）
3. 在 `router/index.js` 加路由守卫：`beforeEach` 检查 localStorage token，无 token 跳 /login

**✅ Day 2 A 验收：** 浏览器注册 → 登录 → 看到首页 Dashboard（卡片+表格有数据）。

### B（前端 — 首页图表 + 审核入口）

1. 在 HomeView.vue 底部加一个 ECharts 柱状图（近 7 天审核量，数据先写死）
2. 在首页加"上传新合同"按钮（el-button primary，跳 /contracts/upload）
3. 完善 request.js 的 axios 拦截器：
   - request 拦截器：自动从 localStorage 取 token 注入 Authorization header
   - response 拦截器：401 自动跳 /login，其他错误统一弹 el-message

**✅ Day 2 B 验收：** 首页图表渲染 + "上传新合同"按钮可点击跳转。

### D（AI — 文档解析器完成）

1. 完善 `backend/ai/parser/docx_parser.py`：
   - 用 python-docx 读取 .docx，提取全文文本 + 段落样式（heading1/2/3 等）
   - 输出格式：`{'paragraphs': [{'text': '...', 'style': 'heading1', 'index': 0}, ...], 'full_text': '...'}`
2. 创建 `backend/ai/parser/pdf_parser.py`：
   - 用 pdfplumber 提取文本 + 页码标记
   - 输出格式同上但增加 page_number 字段
3. 创建 `backend/ai/parser/__init__.py`：
   - `detect_and_parse(filepath)` 统一入口，根据扩展名分发到 docx/pdf 解析器
4. 测试：准备 1 个 .docx 测试合同 + 1 个 .pdf 测试合同，跑 `detect_and_parse` 打印输出

**✅ Day 2 D 验收：** 两个测试合同都能解析出全文文本 + 段落层级。

### E（AI — 知识库初始化 + 合同模板准备）

1. 整理民法典合同编关键条文（50 条，.md 格式），存入 `backend/ai/knowledge/laws/`
2. 编写 20 条风险案例（覆盖 R01-R12，每条含：合同片段 + 风险类型 + 等级 + 判定理由 + 参考法条），存入 `backend/ai/knowledge/cases/`（JSON 格式）
3. 写知识库向量化脚本 `backend/ai/rag/init_kb.py`：读取 laws/ 和 cases/ 目录 → sentence-transformers 向量化 → 写入 ChromaDB 两个集合
4. 准备 3 种合同类型的标准条款模板（采购、销售、保密协议），各 10 条核心条款
5. **🎯 在 Dify 平台搭建"合同风险初审"工作流（今天最重要的任务之一！）：**
   - 创建知识库：上传 Day 1 整理的 20-30 条民法典条文 + 10-15 条风险案例
   - 构建工作流：开始节点 → 知识检索节点（查民法典条文 + 风险案例库）→ LLM 节点（DeepSeek-V3，按 12 类风险逐条分析，输出 JSON）→ 结束节点
   - 发布工作流 → 获取 API Key → 记到 .env 文件（`DIFY_API_KEY=app-xxx`）
   - 用 Dify 内置的"调试"功能测试：输入一段合同条款 → 确认能返回 JSON 风险分析结果

**✅ Day 2 E 验收：** 运行 init_kb.py 后 ChromaDB 两个集合各有数据 → 检索测试返回相关结果；**Dify 工作流发布成功，调试预览能返回风险分析 JSON。**

### C（后端补充 — Dify 接口封装）

6. 创建 `backend/services/dify_client.py`：
   - `class DifyClient`: `__init__(api_key, base_url)` + `async def run_workflow(contract_text: str) -> dict`
   - 用 httpx.AsyncClient 调 Dify API（POST /v1/workflows/run，response_mode: blocking）
   - 异常处理：超时 20s + 网络错误 → 返回 `{"error": "Dify 服务不可用"}`
7. 在 `backend/main.py` 中创建测试端点 `GET /api/ai/test`：
   - 调用 DifyClient.run_workflow("测试合同条款：甲方有权随时单方面解除本合同，乙方不得提出异议。")
   - 返回 `{"status": "ok", "dify_connected": true, "sample_result": {...}}`

**✅ Day 2 C 验收（补充）：** Swagger 里调 GET /api/ai/test → 返回 Dify 工作流真实分析结果。

### 依赖关系

- **C 必须先完成** → A 才能调登录接口 → A 调通了 B 才能测首页图表
- **E 必须先完成 Dify 工作流** → C 才能写 dify_client + 测试端点 → 为 Day 3 A 调用做准备
- D 和 E（非 Dify 部分）各自独立，不依赖其他人


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**C — 数据库建表 + 认证接口：**
> 在 backend/schema.sql 中写 8 张表的完整建表 SQL：users(id INT PK AUTO_INCREMENT, username VARCHAR, password_hash VARCHAR, email VARCHAR, role VARCHAR, created_at DATETIME)、contracts(id VARCHAR UUID PK, filename, file_path, contract_type, status, uploader_id FK, created_at)、audit_records、audit_reports、audit_batch、templates、feedback_log、custom_rules（后 5 张表先写基础字段）。然后在 backend/models/user.py 和 backend/models/contract.py 中用 SQLAlchemy 写对应的 ORM 模型。在 backend/api/auth_router.py 中实现 POST /register（bcrypt 哈希密码，返回 JWT）和 POST /login（验证密码，返回 JWT，payload 含 user_id + exp 24h），以及 GET /me（从 Authorization header 解析 JWT 返回用户信息）。

**A — 登录注册页 + 首页：**
> 在 frontend/src/views/LoginView.vue 中创建登录注册页，使用 Element Plus 的 el-card + el-form + el-tabs。登录 tab：用户名 + 密码 + 登录按钮。注册 tab：用户名 + 密码 + 确认密码 + 邮箱 + 注册按钮。调 POST /api/auth/login 或 /api/auth/register，成功存 token 到 localStorage，跳转 /。在 frontend/src/views/HomeView.vue 中创建首页 Dashboard：顶部欢迎语 + 四个 el-statistic 统计卡片（今日审核/待处理/本月风险/通过率，数据先写死）+ 下部 el-table 最近合同列表（写死 5 条假数据）。

**B — 首页图表 + axios 拦截器：**
> 在 HomeView.vue 底部加一个 ECharts 柱状图（近 7 天每日审核量，x 轴周一到周日，y 轴写死 3,5,2,8,4,6,7）。在 frontend/src/api/request.js 中完善 axios 实例：request 拦截器从 localStorage 取 token 注入 Authorization header，response 拦截器在 401 时跳 /login，其他错误统一用 ElMessage.error 弹提示。

**D — 文档解析器：**
> 在 backend/ai/parser/ 下创建 docx_parser.py 和 pdf_parser.py。docx_parser 用 python-docx 读取 .docx，提取每段的文本和段落样式（判断是 heading 还是 body），输出 {'paragraphs': [{'text':..., 'style':..., 'index':0},...], 'full_text':'...'}。pdf_parser 用 pdfplumber 提取文本和页码，输出同样的格式但增加 page_number 字段。在 __init__.py 中写 detect_and_parse(filepath) 函数，根据扩展名分发。

**E — 知识库初始化：**
> 在 backend/ai/knowledge/laws/ 下创建 contract_law.md，包含《民法典》合同编 50 条关键条文（从第 463 条到第 594 条范围内选取最重要的 50 条），每条标注条款编号和标题。在 backend/ai/knowledge/cases/ 下创建 risk_cases.json，包含 20 条风险案例（12 类风险各 1-2 条），每条格式为 {"risk_type":"R01","level":"high","clause_text":"...","reason":"...","reference":"民法典第X条"}。在 backend/ai/rag/ 下创建 init_kb.py，用 sentence-transformers 的 text2vec-large-chinese 模型把 laws/ 和 cases/ 的内容向量化，写入 ChromaDB 两个集合（laws 和 cases）。

**E — Dify 工作流搭建（🎯 关键任务）：**
> 帮我在 Dify 平台（cloud.dify.ai）中搭建一个"合同风险初审"工作流。步骤：（1）创建知识库，上传约 20 条民法典合同编条文和 10 条风险案例作为知识库文档；（2）创建工作流：类型选 Chatflow → 开始节点 → 知识检索节点（配置检索上面创建的知识库，Top-K=5，score 阈值 0.5）→ LLM 节点（选 DeepSeek-V3，System Prompt："你是一位合同审核专家。请根据以下合同条款和知识库中的法条/案例，逐条分析是否存在以下 12 类风险：R01 违约金过高、R02 无限责任、R03 单方解约权...对每类风险输出：风险等级(high/medium/low)、涉及条款原文、判定理由、参考法条编号。输出 JSON 格式。"，temperature=0.1）→ 结束节点（输出变量设为 LLM 节点的 text 输出）。发布后获取 API Key，给我完整配置截图。

**C — Dify 接口封装（🎯 关键任务）：**
> 在 backend/services/dify_client.py 中创建 DifyClient 类：__init__(self, api_key, base_url='https://api.dify.ai/v1')，async run_workflow(contract_text: str) -> dict，用 httpx.AsyncClient 调 POST /workflows/run，传 inputs={"query_text": contract_text}，response_mode="blocking"，超时 20s，异常捕获返回 {"error": "Dify 服务不可用"}。在 backend/main.py 中加测试端点 GET /api/ai/test，硬编码一段测试合同条款调 DifyClient.run_workflow，返回 {"status":"ok","dify_connected":true,"sample_result":{...}}。

---

## Day 3（7.20 周一）— 合同上传 + 列表 + 分类器 + Dify 前端集成

> 今日目标：用户能上传合同文件，后端解析+自动分类，前端能看到合同列表，**Dify AI 分析可在首页一键测试**。
> **今天是 Phase 1 最后一天。下午 17:00 全队检查登录注册+首页+合同上传是否能完整走通。**

### C（后端 — 合同管理 API）

1. 创建 `backend/api/contract_router.py`：
   - `POST /api/contracts/upload`（接收文件 → 存到 uploads/ → 调 D 的 parser → 调 D 的 classifier → 写入 contracts 表 → 返回 contract_id + contract_type）
   - `GET /api/contracts`（分页列表，支持 status/type 筛选，按 created_at 倒序）
   - `GET /api/contracts/{id}`（合同详情，含解析文本 + 分类结果）
   - `DELETE /api/contracts/{id}`（软删除，status = deleted）
2. 完善 `backend/services/contract_service.py`：封装数据库操作，异步函数
3. 测试：Swagger 里上传文件 → 看到返回 contract_id

**✅ Day 3 C 验收：** Swagger 里能上传 docx → 列表能查到 → 详情能展开。

### D（AI — 合同分类器）

1. 创建 `backend/ai/classifier/classifier.py`：
   - `classify_contract(full_text: str) -> dict`，调 DeepSeek API
   - System Prompt 要求将合同分为 5 类（采购/销售/保密协议/服务外包/劳动）
   - temperature=0.1，输出 JSON：`{"contract_type": "采购合同", "confidence": 0.92}`
2. 创建 `backend/ai/extractor/extractor.py`：
   - `extract_elements(full_text: str, contract_type: str) -> dict`
   - JSON Schema 约束（parties, amount, date, dispute_resolution, governing_law 等字段）
   - Few-shot prompting（3-5 个标注示例）
3. 测试：给 1 份测试合同，跑 classify → print 类型和置信度

**✅ Day 3 D 验收：** 分类器返回正确的合同类型，置信度 > 0.8。

### A（前端 — 合同上传页 + 列表页）

1. 写 `frontend/src/views/ContractUpload.vue`：
   - el-upload（拖拽上传 .docx/.pdf，限制 10MB）
   - el-form（合同名称 + 合同类型下拉框，默认"自动识别"）
   - 上传后显示进度条 → 成功后跳转到合同详情页
2. 写 `frontend/src/views/ContractList.vue`：
   - 顶部搜索栏（合同名称 + 类型筛选 + 状态筛选）
   - el-table（文件名/类型/上传时间/状态/操作）
   - 分页组件
3. 在 `frontend/src/api/` 下创建 `contract.js`（封装上传+列表+详情接口）
4. **🎯 在 HomeView.vue 中增加"AI 智能测试"按钮（今天最重要的集成任务）：**
   - 在首页统计卡片区域下方加一个 el-card，标题"AI 智能审核测试"
   - 内含一个 el-input（多行文本，默认填入一条测试合同条款）+"开始 AI 分析"按钮
   - 点击后调 GET /api/ai/test → 显示 Dify 返回的风险分析结果（JSON 格式化展示或 el-descriptions 渲染）
   - 这是验证"Dify 工作流→后端→前端"整条链路是否打通的关键验收点

**✅ Day 3 A 验收：** 上传一份合同 → 列表页看到 → 点击进入详情页；**首页 AI 测试按钮能调通 Dify 并展示分析结果。**

### B（前端 — 合同详情页骨架 + PDF 预览面板）

1. 写 `frontend/src/views/ContractDetail.vue`：
   - 顶部：合同元信息卡片（文件名/类型/上传时间/页数）
   - 中部：el-tabs（原始文本 / 审核结果 / 条款比对 / 审核报告）
   - 右侧：PDF 原文预览面板（pdf.js，P0 为按页码跳转 + 文本展示）
2. 在 `frontend/src/api/` 下创建 `audit.js`（封装审核触发+结果查询+报告获取接口）

**✅ Day 3 B 验收：** 合同详情页四个 tab 切换正常，PDF 预览能看到合同原文。

### E（AI — RAG 检索链路调通）

1. 完善 `backend/ai/rag/vector_store.py`：`search(query_text, collection_name, top_k=20)` + Cross-Encoder Rerank（精排为 Top-5）
2. 写测试脚本：给一个条款片段 → 检索风险案例库 → 打印 Top-5 检索结果 + 相似度分数
3. 知识库扩充：风险案例增加到 40 条（12 类风险各 3-4 条）
4. 创建 `backend/ai/rag/__init__.py`：统一检索入口 `retrieve(query, collections=['laws', 'cases'])`

**✅ Day 3 E 验收：** 检索测试能返回相关的风险案例，Top-1 相似度 > 0.7。

### Phase 1 交付检查（下午 17:00）

| 检查项 | 负责人 | 标准 |
|-------|:-----:|------|
| 注册 → 登录 → 首页 | A + C | 完整走通 |
| 合同上传 → 解析 → 分类 → 列表展示 | A + C + D | 完整走通 |
| 4 个核心页面可访问 | A + B | 登录/首页/上传/列表 |
| Swagger 文档完整 | C | 3+ 个 API 可用 |
| AI 管道验证 | D + E | 解析器 + 分类器 + 检索都跑通 |
| **🎯 Dify 工作流可调用** | **E + C + A** | **Dify 平台工作流发布 → 后端 GET /api/ai/test 返回结果 → 前端首页 AI 按钮展示结果** |

---

### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**C — 合同管理 API：**
> 在 backend/api/contract_router.py 中创建 APIRouter(prefix="/api/contracts")，实现以下端点：（1）POST /upload — 接收文件上传（用 fastapi.UploadFile），保存到 uploads/ 目录，调用 parser 解析文本，调用 classifier 分类，写入 contracts 表，返回 contract_id 和 contract_type；（2）GET / — 分页列表（page/size 参数），支持 type 和 status 筛选，按 created_at 倒序；（3）GET /{id} — 返回合同详情（含解析文本和分类结果）；（4）DELETE /{id} — 软删除（status='deleted'）。同时在 backend/services/contract_service.py 中封装这些数据库操作。

**D — 合同分类器 + 要素抽取：**
> 在 backend/ai/classifier/classifier.py 中实现 classify_contract(full_text: str) -> dict，用 openai 库调 DeepSeek-V3，System Prompt 要求将合同分为 5 类之一（采购合同/销售合同/保密协议/服务外包合同/劳动合同），temperature=0.1，输出 JSON {"contract_type":"...","confidence":0.92}。在 backend/ai/extractor/extractor.py 中实现 extract_elements(full_text, contract_type) -> dict，用 JSON Schema 约束输出（parties/amount/date/dispute_resolution/governing_law 等字段），配合 3-5 个 Few-shot 示例。

**A — 合同上传页 + 列表页：**
> 在 frontend/src/views/ContractUpload.vue 中创建合同上传页：用 Element Plus 的 el-upload（拖拽模式，限制 .docx/.pdf，最大 10MB），el-form（合同名称 input + 合同类型 el-select，默认"自动识别"），上传后显示进度条，成功跳转到合同详情页 /contracts/{id}。在 frontend/src/views/ContractList.vue 中创建合同列表页：顶部搜索栏（el-input 搜索名称 + el-select 类型筛选 + el-select 状态筛选），el-table（文件名/类型/上传时间/状态/操作），el-pagination 分页。在 frontend/src/api/contract.js 中封装 upload/list/detail/delete 四个接口。

**A — 首页 AI 测试按钮（🎯 关键集成任务）：**
> 在 HomeView.vue 的统计卡片区域下方增加一个 el-card，标题"AI 智能审核测试"。卡片内含：（1）一个 el-input type="textarea" 多行文本框，默认填入测试文本"甲方有权随时单方面解除本合同，乙方不得提出异议，且乙方需赔偿甲方全部损失。"；（2）一个"开始 AI 分析"按钮（el-button type="success" :loading="aiAnalyzing"）；（3）结果展示区：调用 GET /api/ai/test，成功后在 el-card 中渲染返回的 JSON 结果——风险等级用 el-tag 颜色区分，原文片段用 el-text type="warning" 高亮，判定理由用 el-alert type="info" 展示。如果调用失败，显示 el-empty description="AI 服务暂不可用，请检查 Dify 配置"。

**B — 合同详情页骨架：**
> 在 frontend/src/views/ContractDetail.vue 中创建合同详情页：顶部 el-descriptions 展示合同元信息（文件名/类型/上传时间/页数/状态），中部 el-tabs（原始文本/审核结果/条款比对/审核报告四个 tab），右侧 PDF 预览面板用 pdfjs-dist 渲染（P0 实现：显示总页数 + 点击页码跳转 + 侧栏文本展示，使用 pdfjs-dist 的 getDocument API）。

**E — RAG 检索链路：**
> 在 backend/ai/rag/vector_store.py 中完善 search 函数：用 ChromaDB 的 query 方法做余弦检索（top_k=20），然后用 sentence-transformers 的 cross-encoder 模式对候选做重排序（精排为 top_k=5）。写一个测试脚本，检索"违约金超过合同金额的 30% 是否有效"，打印 Top-5 结果及相似度分数。在 backend/ai/rag/__init__.py 中暴露统一的 retrieve(query, collections=['laws','cases']) 入口。

---

## Day 4（7.22 周二）— 合同详情闭环 + 审核引擎核心

> 今日目标：**点"开始审核"，后端跑完完整 AI 管道，前端审核结果页能看到风险列表。**

### C（后端 — 审核调度核心，今天最关键）

1. 创建 `backend/services/audit_service.py`：
   - `async def audit_contract(contract_id, mode='light')` 异步主函数
   - 管道顺序：解析 → 分类 → 要素抽取 → 规则引擎 → RAG 检索 → LLM 审核
   - 每步的输入输出结构化存入 audit_records 表
   - 审核状态机：pending → processing → completed/failed
2. 创建 `backend/api/audit_router.py`：
   - `POST /api/audit/start`（接收 contract_id + mode，启动审核 → 返回 audit_batch UUID）
   - `GET /api/audit/status/{batch_id}`（查询审核进度）
   - `GET /api/audit/result/{batch_id}`（获取审核结果）
3. 创建 `backend/models/audit_record.py` + `backend/models/audit_batch.py`

**✅ Day 4 C 验收：** Swagger 里调 start → 轮询 status → 拿到 result（含风险列表）。

### D（AI — 规则引擎 + LLM 审核）

1. 创建 `backend/ai/auditor/rule_engine.py`：
   - 12 条规则（R01-R12），每条规则一个函数
   - 输入合同文本 → 返回 `[{risk_type, level, clause_text, reason, detection_method: 'rule'}]`
2. 创建 `backend/ai/auditor/llm_auditor.py`：
   - `audit_with_llm(full_text, rag_context) -> list`，调 DeepSeek API
   - System Prompt 含 12 类风险定义 + 量化等级标准 + RAG 检索到的参考案例
   - temperature=0.1，输出 JSON 格式风险列表
3. 创建 `backend/ai/auditor/__init__.py`：统一审核入口，先规则引擎再 LLM

**✅ Day 4 D 验收：** 给测试合同 → 规则引擎输出 3-5 条风险 → LLM 审核输出 5-10 条风险（含置信度）。

### A（前端 — 合同详情页功能完善）

1. 完善 ContractDetail.vue：
   - "开始审核"按钮（el-button type="primary"，点击调 /api/audit/start）
   - 审核进度轮询（el-progress 显示 processing 状态）
   - 审核完成后自动切换到"审核结果"tab
2. 合同列表页增加状态标签（待审核/审核中/已完成）
3. 合同删除功能（确认弹窗 → 软删除）

**✅ Day 4 A 验收：** 点"开始审核"→看到进度条→自动跳到审核结果 tab。

### B（前端 — 审核结果页）

1. 写 `frontend/src/views/AuditResult.vue`：
   - 顶部：审核概要卡片（总风险数 / 高风险数 / 中风险数 / 低风险数，彩色数字）
   - 中部：风险列表 el-table（类型/等级/原文片段/判定理由/置信度），等级用 el-tag 颜色区分（红/橙/黄）
   - 筛选栏：按风险等级筛选 + 按置信度筛选
   - 点击某条风险 → PDF 面板跳转到对应页码
2. 风险列表每行可展开（展示详细修改建议 + 参考法条 + 审核方法标记）

**✅ Day 4 B 验收：** 审核完成后审核结果页展示风险列表，等级颜色正确。

### E（AI — 审核知识增强）

1. 编写 Prompt 模板库：为每种风险类型准备一个专用的审核 Prompt（含法条引用 + 典型表述 + 判断标准）
2. 将规则引擎的 12 条规则与 RAG 检索结果关联——规则命中的条款作为 RAG 检索 query，提升检索相关性
3. 知识库扩充到：60 条风险案例 + 100 条法律法规 + 100 条标准条款
4. 测试 RAG 检索在不同合同类型上的覆盖率

**✅ Day 4 E 验收：** 5 种合同类型各测一次 RAG 检索，每种至少返回 3 条相关知识。

### 依赖关系

- **C 必须先完成 audit_service** → D 的模块才能被串联 → A 和 B 才能看到审核结果
- D 和 E 的模块被 C 调用，D 和 E 今天各自独立开发，不需要互相等


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**C — 审核调度核心（最关键）：**
> 在 backend/services/audit_service.py 中实现 async def audit_contract(contract_id, mode='light') 异步主函数。管道顺序：从 DB 读取合同 → parser 解析文本 → classifier 分类 → extractor 要素抽取 → rule_engine 规则检测 → RAG 检索 → LLM 审核 → 结果写入 audit_records 表。每条审核记录字段：id UUID、contract_id FK、risk_type(R01-R12)、risk_level(high/medium/low)、clause_text、reason、confidence、detection_method(rule/rag/llm)、agent_logs JSON、created_at。审核状态用 audit_batch 表管理（id UUID、contract_id、mode、status pending/processing/completed/failed、started_at、completed_at）。在 backend/api/audit_router.py 中创建 POST /api/audit/start（接收 contract_id + mode，返回 batch_id）、GET /api/audit/status/{batch_id}（返回 status + progress）、GET /api/audit/result/{batch_id}（返回完整风险列表）。

**D — 规则引擎 + LLM 审核：**
> 在 backend/ai/auditor/rule_engine.py 中实现 12 条风险检测规则（R01-R12），每条规则一个函数 detect_XXX(text) -> list[dict]。R01 违约金：正则匹配"违约"关键词 + 百分比数字，>20% 高风险/10-20% 中风险。R02 无限责任：匹配"全部损失""一切责任""无限"等关键词。R03 单方解约：匹配"甲方有权随时解除""任意解除权"。依次类推到 R12。返回格式 [{"risk_type":"R01","level":"high","clause_text":"...","reason":"违约金比例 30% 超过法定上限","detection_method":"rule"}]。在 backend/ai/auditor/llm_auditor.py 中实现 audit_with_llm(full_text, rag_context) -> list，调 DeepSeek-V3，System Prompt 含 12 类风险定义 + 量化等级标准 + RAG 检索到的参考案例，temperature=0.1，输出 JSON 数组。

**A — 合同详情页审核触发：**
> 在 ContractDetail.vue 的"原始文本"tab 下方加"开始审核"按钮（el-button type="primary" :loading="auditing"），点击调 POST /api/audit/start。审核中显示 el-progress 进度条（每 2 秒轮询 GET /api/audit/status）。审核完成后自动切换到"审核结果"tab。合同列表页 ContractList.vue 的状态列加 el-tag：待审核（info）、审核中（warning）、已完成（success）。

**B — 审核结果页：**
> 在 frontend/src/views/AuditResult.vue 中创建审核结果页。顶部四个 el-statistic 卡片：总风险数/高风险数/中风险数/低风险数，数字用不同颜色（红/橙/黄/蓝）。中部风险列表 el-table：类型(R01-R12 转中文标签)/等级(el-tag 颜色区分)/原文片段/判定理由/置信度(el-progress 百分比)。筛选栏：el-radio-group 按等级筛选 + el-slider 按置信度筛选。点击某条风险的原文片段 → PDF 面板跳转到对应页码（用 pdfjs-dist 的 scrollIntoView 或 page navigation）。每条风险可展开：显示修改建议 + 参考法条 + detection_method 标签。

**E — 审核知识增强：**
> 在 backend/ai/knowledge/ 下创建审核 Prompt 模板文件 audit_prompts.py。为每种风险类型（R01-R12）写一个专用的审核 Prompt 模板字符串，包含：该风险的《民法典》法条引用 + 3 种典型危险表述示例 + 3 种安全表述示例 + 判断标准。同时把 RAG 检索与规则引擎联动——规则命中的条款文本自动作为 RAG 检索 query，提高检索命中率。

---

## Day 5（7.23 周三）— 审核报告 + 条款比对 + 前端完整闭环

> 今日目标：审核完成后自动生成报告（含热力图），条款比对功能可用。

### C（后端 — 报告 API + 模板管理）

1. 创建 `backend/api/report_router.py`：
   - `GET /api/reports/{batch_id}`（获取审核报告 HTML）
2. 创建 `backend/services/report_service.py`：从 audit_records 聚合数据 → 调 E 的报告生成器 → 返回 HTML
3. 创建 `backend/api/template_router.py`：
   - `GET /api/templates`（获取合同类型对应的标准模板列表）

**✅ Day 5 C 验收：** 审核完成后调报告 API → 返回完整 HTML 报告。

### E（AI — 报告生成 + 条款比对）

1. 创建 `backend/ai/reporter/reporter.py`：
   - `generate_report(audit_results, contract_info) -> str`，Jinja2 模板渲染 HTML
   - 报告六章：基本信息 / 审核概要 / 风险清单 / 条款比对 / 风险热力图数据 / 建议汇总
2. 创建 `backend/ai/matcher/matcher.py`：
   - `compare_clauses(contract_clauses, template_clauses) -> dict`
   - 双层对齐：关键词归一化（20-30 组硬编码同义词）→ 向量语义匹配
   - 输出：已覆盖 / 缺失 / 偏离（含相似度分数）
3. 写 `backend/ai/reporter/heatmap_data.py`：从 audit_records 计算每页风险密度数据（给前端 ECharts 用）

**✅ Day 5 E 验收：** 跑一份测试报告的生成 → 打开 HTML 看到完整六章结构。

### B（前端 — 审核报告页 + 热力图）

1. 写 `frontend/src/views/AuditReport.vue`：
   - 使用 E 生成的 HTML 渲染报告（v-html）或自己用 Vue 组件组装
   - ECharts 热力图：页码 × 风险类型矩阵，颜色深度 = 风险密度
   - ECharts 饼图：风险等级分布
   - ECharts 柱状图：各类型风险数量
2. 条款比对展示：并排对比视图（标准模板 vs 合同条款），差异高亮
3. 报告导出按钮（调用后端导出 PDF/HTML）

**✅ Day 5 B 验收：** 审核报告页完整展示热力图 + 饼图 + 条款比对结果。

### A（前端 — 前端闭环完善）

1. 合同详情页的"审核结果"tab 内嵌审核结果组件（复用 AuditResult.vue）
2. 合同详情页的"审核报告"tab 内嵌报告组件（复用 AuditReport.vue）
3. 全局 loading 状态 + 错误页面（404/500）
4. 导航栏完善（el-menu，当前页面高亮）

**✅ Day 5 A 验收：** 上传合同 → 审核 → 详情页看结果 → 切到报告 tab 看完整报告。

### D（AI — 引擎完善 + 错误处理）

1. 规则引擎增加错误处理（空文本/格式异常/超长文本截断）
2. LLM 审核增加 JSON 解析容错（如果 LLM 返回非法 JSON，正则提取 + 重试 1 次）
3. 分类器和抽取器增加降级逻辑（LLM 超时 → 返回默认值 + 标记低置信度）
4. 写单元测试：规则引擎 12 条规则各测 2 个正例 + 2 个负例

**✅ Day 5 D 验收：** 12 × 2 = 24 个单元测试全部通过。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**C — 报告 API + 模板管理：**
> 在 backend/api/report_router.py 中创建 APIRouter(prefix="/api/reports")，实现 GET /{batch_id} — 从 audit_records 和 audit_batch 表聚合审核数据，调 reporter 生成 HTML 报告，返回 {"html": "..."}。在 backend/api/template_router.py 中创建 GET /api/templates — 查询 templates 表返回所有合同类型的标准模板列表。

**E — 报告生成 + 条款比对：**
> 在 backend/ai/reporter/reporter.py 中实现 generate_report(audit_results, contract_info) -> str，用 Jinja2 模板渲染 HTML。报告六章结构：（1）合同基本信息（2）审核概要（3）风险清单（表格+等级颜色）（4）条款比对结果（5）风险热力图数据（JSON，给前端 ECharts 用）（6）建议汇总。在 backend/ai/matcher/matcher.py 中实现 compare_clauses(contract_clauses, template_clauses) -> dict：第一层关键词归一化（用 30 组硬编码同义词对，如"违约责任/违约条款/违约处理"→"违约责任"），第二层用 text2vec-large-chinese 做向量语义匹配。输出 matched/missing/divergent 三类。在 backend/ai/reporter/heatmap_data.py 中写从 audit_records 计算每页风险密度的函数。

**B — 审核报告页 + 三个图表：**
> 在 frontend/src/views/AuditReport.vue 中创建审核报告页。用 ECharts 渲染三个图表：（1）热力图 — x 轴页码、y 轴风险类型，颜色深度表示风险密度，数据从后端 heatmap_data 获取；（2）饼图 — 风险等级分布 high/medium/low；（3）柱状图 — 12 类风险各检出多少条。条款比对结果用 el-descriptions + el-tag 展示：缺失条款标红色"缺失"、偏离条款标橙色"偏离（相似度 XX%）"、匹配条款标绿色。加"导出 HTML"按钮调 GET /api/reports/{id}。

**A — 前端闭环完善：**
> 在 ContractDetail.vue 中，把"审核结果"tab 的内容改为内嵌 AuditResult 组件（用 props 传 auditResults 数据），把"审核报告"tab 的内容改为内嵌 AuditReport 组件。全局加 el-menu 导航栏（首页/合同管理/关于），当前页面高亮。创建 404 页面 NotFoundView.vue（el-empty + "返回首页"按钮）。所有异步操作加 el-loading 或 v-loading 指令。

---

## Day 6（7.24 周四）— 全管道联调 + 反馈面板

> 今日目标：**端到端完整走通 3 次（3 份不同合同），每个环节不出错。**

### 全员（上午 9:00-12:00 — 集中联调）

1. C 准备 3 份测试合同（采购/销售/保密协议各 1 份）
2. 全部 5 人一起走一遍完整流程：上传 → 审核 → 结果 → 报告
3. 记录每一步的问题（谁出的问题 / 什么现象 / 怎么复现）
4. 优先修复阻塞性问题（流程走不下去的）

**联调检查清单：**

| 步骤 | 检查点 | 状态 |
|------|-------|:--:|
| 1. 上传 | 文件接收 → 解析成功 → 存入数据库 | ☐ |
| 2. 分类 | 自动识别合同类型，置信度 > 0.8 | ☐ |
| 3. 审核 | 规则引擎命中 + LLM 审核输出 + RAG 检索有结果 | ☐ |
| 4. 结果 | 风险列表正确展示，等级/置信度合理 | ☐ |
| 5. 报告 | 热力图渲染 + 条款比对 + 六章结构完整 | ☐ |

### A + B（下午 — 反馈标注面板）

1. 写 FeedbackPanel.vue（在审核结果页的风险列表中每条风险右侧加操作按钮）：
   - "确认"（绿色勾）→ feedback_log 记录 confirmed
   - "修正"（黄色笔）→ 弹出编辑框，修改等级/理由 → 记录 corrected
   - "误报"（红色叉）→ 记录 false_positive
   - "补充"（蓝色加号）→ 打开文本输入框，新增一条风险标注
2. 反馈数据调 POST /api/feedback（C 今天下午写好接口）

**✅ Day 6 A+B 验收：** 每条风险都能标注 → 标注后状态实时更新。

### C（下午 — 反馈 API + 错误处理统一）

1. 创建 `backend/models/feedback_log.py`
2. 创建 `backend/api/feedback_router.py`：POST /api/feedback（接收标注）+ GET /api/feedback/{contract_id}
3. 全部 API 加统一异常处理（try/except → 返回标准错误 JSON）
4. 全部 API 加请求日志（请求路径 + 耗时 + 状态码）

**✅ Day 6 C 验收：** 前端调反馈接口存储成功，再次查询能看到历史标注。

### D + E（下午 — AI 管道调优）

1. 根据上午联调发现的问题，修复 AI 模块的 bug
2. RAG 检索质量优化（调整 chunk 大小、相似度阈值）
3. LLM Prompt 微调（根据 3 份测试合同的审核结果）
4. 规则引擎补充——联调中发现的漏检场景加规则

**✅ Day 6 D+E 验收：** 3 份测试合同各跑 1 次审核，输出稳定一致。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**A + B — 反馈标注面板：**
> 在 frontend/src/components/FeedbackPanel.vue 中创建反馈标注组件。每条风险右侧加 4 个操作按钮：（1）确认 — el-button type="success" size="small" 图标 Check；（2）修正 — el-button type="warning" size="small" 图标 Edit，点击弹出 el-dialog 编辑框修改等级和理由；（3）误报 — el-button type="danger" size="small" 图标 Close，点击弹出确认框"确定标记为误报？"；（4）补充 — el-button type="primary" size="small" 图标 Plus，点击弹出文本输入框新增一条风险标注。每次操作调 POST /api/feedback，成功后按钮状态更新（已确认的变灰不可再点）。

**C — 反馈 API + 错误处理：**
> 在 backend/models/feedback_log.py 中创建 FeedbackLog 模型（id, contract_id FK, audit_record_id FK nullable, user_id FK, action_type confirmed/corrected/false_positive/supplemented, original_data JSON, corrected_data JSON nullable, created_at）。在 backend/api/feedback_router.py 中实现 POST /api/feedback（接收标注数据写入 feedback_log）和 GET /api/feedback/{contract_id}（返回该合同所有反馈记录）。给全部 API 路由加统一的异常处理中间件——try/except SQLAlchemyError → 返回 500、ValueError → 返回 400、其他 → 返回 500，统一格式 {"error": "错误描述", "code": 500}。

**D + E — AI 管道调优：**
> 请在 backend/ai/auditor/rule_engine.py 中为每条规则增加至少 2 个正例和 2 个负例的单元测试（用 pytest）。在 backend/ai/auditor/llm_auditor.py 中增加 JSON 解析容错——如果 LLM 返回的不是合法 JSON，用正则提取 risk_type/level/clause_text/reason 字段，提取失败则重试 1 次，仍失败则返回 [{"error": "LLM 返回格式异常，请人工审核"}]。在 backend/ai/rag/vector_store.py 中增加检索 score 阈值过滤——Top-5 中 score < 0.5 的条目不注入 Prompt，score 0.5-0.7 的附加"低相关性参考"标记。

---

## Day 7（7.25 周五）— 第二周交付前冲刺

> 今日目标：**所有核心功能闭环，明天交付前最后检查。**

### A + B（全天 — 前端打磨 + 响应式适配）

1. UI 统一：所有页面统一间距（el-card padding 20px）、颜色（风险等级红 #F56C6C / 橙 #E6A23C / 黄 #F2D024）、字体大小
2. 响应式：列表页在窄屏下表格转卡片、表单在小屏下全宽
3. 空状态：所有列表/表格在无数据时显示 el-empty 组件 + 引导文字
4. 加载状态：所有异步操作显示 el-loading 或骨架屏
5. 错误状态：所有 API 调用的错误统一 toast 提示 + 错误详情

**✅ Day 7 A+B 验收：** 5 个核心页面在 1920px 和 1366px 两种分辨率下布局正常。

### C（全天 — 接口文档 + 后端启动说明 + Docker 整理）

1. 整理完整的 API 文档（Swagger 导出 + 注释完善）
2. `docker-compose.yml` 已在 Day 0 创建，今天检查并完善：增加 backend 服务定义（方便评委一键部署），确保 `docker compose up` 能启动 FastAPI + ChromaDB 两个服务
3. `backend/Dockerfile` 已创建，今天验证：`docker build -t a24-backend ./backend && docker run -p 8080:8080 a24-backend`
4. 准备一份"后端启动说明.md"（含宿主机开发和 Docker 部署两种方式）

**✅ Day 7 C 验收：** docker compose up 后 FastAPI + ChromaDB 都启动成功。评委拿到代码能一键跑起来。

### D + E（全天 — 知识库完善 + RAG 效果验证）

1. 知识库填充到目标量：80 条风险案例 + 150 条法律法规 + 150 条标准条款
2. 做 RAG 检索效果测试：20 个测试 query（4 类风险 × 5 种表述），统计 Top-5 相关率
3. 调优 Cross-Encoder Rerank 阈值（当前 score > 0.7 正常注入，< 0.5 过滤）
4. 写一份"AI 模块使用说明.md"：每个模块的输入/输出格式、调用示例

**✅ Day 7 D+E 验收：** RAG 检索 Top-5 相关率 > 70%（即 20 个 query 中 ≥ 14 个的 Top-5 中包含正确答案）。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**A + B — 前端全局 UI 统一：**
> 请在 frontend/src/ 下创建一个全局样式文件 styles/global.css，定义：（1）所有 el-card 统一 padding: 20px, border-radius: 8px；（2）风险等级颜色变量 —high: #F56C6C, —medium: #E6A23C, —low: #67C23A, —info: #909399；（3）页面标题统一 h3 + el-divider；（4）空状态统一用 el-empty description="暂无数据"。然后给所有 5 个核心页面（LoginView/HomeView/ContractUpload/ContractList/ContractDetail）的 el-table 加 stripe + border 属性，el-button 统一 size="default"。在 ContractList.vue 中做响应式：@media (max-width: 768px) 时表格转 el-card 列表展示。

**C — Docker 完善 + API 文档：**
> 项目根目录已有 docker-compose.yml（ChromaDB 单服务），今天完善为双服务版本：增加 backend 服务定义（build: ./backend，端口 8080:8080，volumes 挂载 uploads/ 和 chroma_data/，environment 注入 DEEPSEEK_API_KEY 和 DIFY_API_KEY，depends_on chromadb）。确保 docker compose up 后两个服务都启动成功。给 backend/Dockerfile 增加健康检查：HEALTHCHECK --interval=30s CMD curl -f http://localhost:8080/api/health || exit 1。在 backend/main.py 中给所有 API 端点补全 docstring 和 response_model 注解。

**D + E — 知识库填充 + RAG 效果测试：**
> 请在 backend/ai/knowledge/cases/risk_cases.json 中将风险案例从当前数量扩充到 80 条（12 类风险各约 7 条），每条包含至少 3 种不同的合同表述变体。在 backend/ai/knowledge/laws/contract_law.md 中补充至约 150 条法律法规条文。写一个 RAG 测试脚本 backend/ai/rag/test_retrieval.py：准备 20 个测试 query（4 类主要风险 × 5 种不同表述方式），统计 Top-5 检索相关率，打印每类的准确率。

---

## Day 8（7.26 周六）— Phase 2 交付冻结

> 今日目标：**代码冻结，全功能验收，截图留档。不写新功能，只修 blocker。**

### 全员（上午 9:00-12:00 — 验收走查）

组长 C 主持，逐项检查 Phase 2 交付清单：

| # | 验收项 | 标准 | 状态 |
|---|-------|------|:--:|
| 1 | 注册登录 | 注册→登录→JWT 认证→路由守卫 | ☐ |
| 2 | 合同上传 | 上传 docx/pdf → 自动解析 → 自动分类 | ☐ |
| 3 | 合同管理 | 列表/详情/删除 → 分页/筛选 | ☐ |
| 4 | 审核执行 | 点"开始审核"→ 完整管道 → 返回风险列表 | ☐ |
| 5 | 审核结果 | 风险列表展示 + 等级颜色 + 置信度筛选 + 原文跳转 | ☐ |
| 6 | 审核报告 | 六章结构 + 热力图 + 饼图 + 条款比对 + HTML 导出 | ☐ |
| 7 | 反馈标注 | 确认/修正/误报/补充 → 存入 feedback_log | ☐ |
| 8 | API 文档 | Swagger 完整可用，10+ 个端点 | ☐ |
| 9 | 数据库 | 8 张表完整，ORM 模型对应 | ☐ |

### 下午（13:00-17:00 — Bug 修复 + 留档）

1. 优先修复验收中发现的 blocker
2. A+B 截全部页面截图（登录/首页/上传/列表/详情/审核结果/报告）
3. C 截 Swagger 全部 API 截图
4. D+E 整理 AI 管道测试结果（3 份合同的审核输出 + RAG 检索效果数据）
5. 将截图和测试数据打包存到 docs/screenshots/ 目录

### 晚上（18:00-20:00 — 缓冲时间）

- 未完成项的兜底修复
- 如果一切顺利：D+E 开始看 Corex 代码结构，为下周做准备

---

## Day 9（7.27 周日）— 缓冲日 + Phase 3 预研

> 今日目标：追进度 or 为 Corex 开发做技术准备。不排硬性任务。

### 如果有未完成的 Phase 2 任务
- 今天是最后补漏机会
- A+B：补充差异页面（如有未开发完的页面组件）
- C：补充遗漏的 API 端点
- D+E：补充 AI 模块的边界处理

### 如果 Phase 2 全部完成
- D+E：搭建 Corex Review 的 4 个 Agent System Prompt 模板，在单条款上手动跑一遍法务→合规→财务→Self-QA 流程
- C：设计 Corex 的 audit_service 集成方案（异步编排、超时处理、结果融合）
- A：设计"审核模式选择器"的 UI（轻量/标准/深度三档 radio）
- B：设计"Agent 日志"展示组件（展示法务/合规/财务各自说了什么）

### 晚上 21:00
- 全队在群里过一遍 Phase 2 验收结果
- C 发一份"Phase 3 开发计划"（基于今天的预研结果调整）

---

## Day 10（7.29 周一）— Corex Review 模式开发 ★

> 今日目标：Corex Review 四步流水线跑通（法务→合规→财务→Self-QA），单合同能输出带 Agent 日志的审核结果。

### D + E（AI — Corex 编排器，今天最核心的任务）

1. 创建 `backend/ai/corex/orchestrator.py`：
   - `async def run_review(full_text, risks_from_retrieve)` → 4 步流水线
   - 步骤 1: 法务 Agent（System Prompt: 15 年合同法律师，逐条审核法律风险）
   - 步骤 2: 合规 Agent（System Prompt: 企业合规官，检查行业监管要求）
   - 步骤 3: 财务 Agent（System Prompt: 财务总监，检查付款/违约金/税务）
   - 步骤 4: Self-QA Agent（综合前三步，逐条自问 + 终审裁定）
   - 超时处理：15s 硬超时 → 跳过该 Agent → 标注缺失
2. 创建 `backend/ai/corex/agents.py`（三个 Agent 的 System Prompt 常量定义）
3. 创建 `backend/ai/corex/__init__.py`

**✅ Day 10 D+E 验收：** 给一份测试合同 → run_review 输出带 4 个 Agent 日志的审核结果列表。

### C（后端 — Corex 集成 + 调度增强）

1. 修改 `backend/services/audit_service.py`：
   - 增加 mode 参数：light（仅 Retrieve）/ standard（Retrieve + Review）/ deep（全量）
   - standard 模式在 RAG+LLM 审核后 → 调 Corex run_review → 结果融合
   - 审核结果中增加 agent_logs 字段（记录每个 Agent 的判断和置信度）
2. 修改 `POST /api/audit/start`：接受 mode 参数，传给 audit_service
3. 测试：Swagger 里分别调 light / standard / deep 三种模式

**✅ Day 10 C 验收：** 三种模式都能正常启动审核，standard 模式返回带 Agent 日志的结果。

### A + B（前端 — 模式选择 + Agent 日志展示）

1. A: 在 ContractDetail.vue 的"开始审核"按钮旁加模式选择器：
   - el-radio-group：轻量审核 / 标准审核 / 深度审核（默认标准）
   - 每个模式旁有 Tooltip 说明（耗时 + 成本 + 适用场景）
2. B: 在 AuditResult.vue 中每条风险右侧加"Agent 日志"展开面板：
   - 展示 4 个 Agent（法务/合规/财务/Self-QA）各自对该风险的判断
   - 用不同颜色的 el-tag 区分 Agent 身份
   - 显示每个 Agent 的置信度分数

**✅ Day 10 A+B 验收：** 选择标准模式审核 → 审核结果中每条风险可展开看到 Agent 日志。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**D + E — Corex Review 编排器（核心）：**
> 在 backend/ai/corex/agents.py 中定义 4 个 Agent 的 System Prompt 常量。LEGAL_AGENT_PROMPT: "你是一位有 15 年经验的合同法律师。请逐条审核以下合同条款，识别法律风险。风险等级标准：高风险=潜在损失>10万元或违反强制性法律；中风险=可能引发合同争议；低风险=表述不精确但可控。请以 JSON 格式输出：{"risks":[{"risk_type":"R01-R12","level":"high/medium/low","clause_text":"...","reason":"...","confidence":0.0-1.0}]}"。COMPLIANCE_AGENT_PROMPT: "你是一位企业合规官..."。FINANCE_AGENT_PROMPT: "你是一位财务总监..."。SELF_QA_PROMPT: "你是一位资深法务审核员，请综合以下三位专家的审核意见，逐条验证并给出终审裁定..."。在 backend/ai/corex/orchestrator.py 中实现 async def run_review(full_text, risks_from_retrieve)，依次调用 4 个 Agent（每个 15s 超时），任何 Agent 超时则跳过并标注，最终融合输出带 agent_logs 的风险列表。

**C — Corex 集成：**
> 在 backend/services/audit_service.py 中修改 audit_contract 函数，增加 mode 参数处理：mode='light' 只走 Retrieve → LLM；mode='standard' 走 Retrieve → LLM → Corex Review（调 run_review）；mode='deep' 走全管道（先空着 deep 的逻辑，下周实现 Discuss）。审核结果中增加 agent_logs 字段（JSON 数组，每条风险记录每个 Agent 的判断和置信度）。在 backend/api/audit_router.py 的 POST /start 请求体中增加 mode 字段（默认 standard）。

**A + B — 模式选择器 + Agent 日志：**
> 在 ContractDetail.vue 的"开始审核"按钮旁加一个审核模式选择器：el-radio-group（轻量/标准/深度），默认标准。每个选项旁放一个 el-tooltip 说明（轻量"~5s，适合快速筛查"；标准"~20s，多角色交叉验证"；深度"~30s，含辩论裁决"）。在 AuditResult.vue 的每条风险行下方加一个可展开区域（el-table 的 expand 插槽），展示 4 个 Agent 的日志：用 el-descriptions 展示（Agent 名称/判断/置信度/是否一致），如果某个 Agent 未参与（超时）则显示灰色"未参与"标签。

---

## Day 11（7.30 周二）— Corex 降级切换 + Dify 深度集成 + RAG 增强

> 今日目标：Dify 从独立测试升级为 Corex 降级备选，审核管道有兜底方案。RAG 检索准确率提升。

### E（AI — Dify 升级为 Corex 降级备选）

1. **Dify 工作流已在 Day 2 搭建完成，今天做升级：**
   - 优化 Dify 工作流的 System Prompt，使其输出格式与 Corex standard 模式一致（含 agent_logs 字段）
   - 增加知识库内容：补充 Day 4-7 积累的审核经验案例到 Dify 知识库
2. C 配合：在 audit_service 中增加降级开关——当 Corex 连续 2 个 Agent 超时 → 自动回退到 Dify 工作流
   - 降级结果标记 `detection_method='dify_fallback'` → 前端无感知切换
   - 降级触发时在 audit_batch 中记录 `fallback_reason` 字段
3. 在 `backend/services/dify_client.py` 中增加 `health_check()` 方法——审核启动前先检测 Dify 可用性

**✅ Day 11 E 验收：** 模拟 Corex 超时场景 → 审核自动回退到 Dify → 前端无感知。Dify 输出格式与 Corex 一致。

### D + E（AI — RAG 增强）

1. Cross-Encoder Rerank 集成到审核管道：检索后 → Rerank → 精排 Top-5 → 注入 LLM Prompt
2. 规则引擎 + RAG 联动：规则命中的条款自动作为 RAG 检索 query，提高检索命中率
3. 检索缓存：同一合同的重复检索（如不同 Agent 检索同一 query）走缓存
4. 知识库增量更新脚本：支持新增案例/法条后热更新 ChromaDB

**✅ Day 11 D+E 验收：** RAG 增强后 3 份测试合同的审核质量对比增强前有提升（风险检出数增加 + 误报减少）。

### A + B（前端 — 报告页完善 + 比对结果优化）

1. B: AuditReport.vue 增加"审核引擎"标签（标注本次使用了轻量/标准/深度模式 + 使用了哪些 Agent）
2. B: 条款比对结果增加"相似度分数"展示 + "偏离原因"说明
3. A: ContractDetail.vue 增加"审核历史"面板（展示该合同的所有历史审核记录）
4. A: 全局增加"审核中"状态提示（顶部横幅 notification-bar）

**✅ Day 11 A+B 验收：** 报告页能看到审核引擎标签，比对结果有分数和原因。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**E — Dify 升级为 Corex 降级备选：**
> 在 backend/services/dify_client.py 中增加 health_check() 方法——GET 请求 Dify API 的基础端点，返回 True/False。在 audit_service.py 的 standard 模式审核管道中增加降级逻辑：执行 Corex run_review 时，如果连续 2 个 Agent 超时（每个 Agent 15s 硬超时）→ 自动调用 DifyClient.run_workflow 作为备用审核 → 将 Dify 返回结果转换为与 Corex 一致的格式（每条风险带 risk_type/level/clause_text/reason/confidence，detection_method 设为 'dify_fallback'）→ 降级触发时在 audit_batch 表记录 fallback_reason 字段。前端无感知——审核结果格式不变。

**D + E — RAG 增强：**
> 在 backend/ai/rag/vector_store.py 的 search 函数中，在 ChromaDB 余弦检索后增加 Cross-Encoder Rerank 步骤——用 sentence-transformers 的 cross-encoder/stsb-distilroberta-base 模型对 Top-20 候选重排序。增加检索缓存：用 Python dict 或 functools.lru_cache，同一 query+collection 组合 5 分钟内不重复检索。在 backend/ai/auditor/llm_auditor.py 中，把规则引擎检测到的条款文本自动作为额外的 RAG 检索 query（与合同全文检索结果合并去重）。

**A + B — 报告页增强 + 审核历史：**
> 在 AuditReport.vue 顶部增加"审核引擎"标签栏：显示本次使用的模式（轻量/标准/深度）+ 参与 Agent 列表（法务✓/合规✓/财务✓/Self-QA✓）+ 总耗时 + 预估成本。在条款比对展示中，每条比对结果增加相似度分数（如"相似度 78% — 建议复核"）和偏离原因说明。在 ContractDetail.vue 中增加"审核历史"面板（el-timeline 时间线组件）：展示该合同所有历史审核记录（每次审核的模式/时间/风险数/操作人），从 audit_batch 表查询。

---

## Day 12（7.31 周三）— 边界场景 + 错误降级 + OCR 基础

> 今日目标：各种异常情况有合理提示，系统不崩溃。OCR 能识别清晰扫描件。

### 全员（上午 9:00-11:00 — 边界场景集中测试）

每人负责一类边界测试，记录结果：

| 边界场景 | 负责人 | 预期行为 |
|---------|:-----:|---------|
| 上传非合同文件（.jpg 照片/空文件/病毒文件.exe）| A | 前端拦截 + 提示"不支持的文件格式" |
| 超大文件（>50MB .pdf）| A | 前端限制 20MB + 后端限制 30MB |
| 后端数据库断开 | C | API 返回 503 + "服务暂时不可用" |
| DeepSeek API Key 失效 | C | 审核失败 + 明确告知"AI 服务异常，请检查 API Key" |
| ChromaDB 服务不可用 | E | 降级为关键词检索（jieba + TF-IDF）|
| LLM 返回非法 JSON | D | 正则提取 + 重试 1 次 + 仍失败则标注"AI 输出格式异常" |
| 合同文本为空（扫描件全图片）| D | 提示"未检测到可提取文本，请确认文件质量" |
| 同时上传 5 份合同 | C | 队列排队处理，不崩溃 |

### C + D（下午 — 错误降级完善）

1. audit_service 增加全局异常捕获——任何步骤抛异常不阻塞后续步骤
2. 增加审核状态通知机制（审核完成/失败 → 前端轮询可获取）
3. D 完善分类器/抽取器的降级输出（LLM 不可用时返回默认值 + 标注"降级结果"）

### D + E（下午 — OCR 基础集成）

1. 在 `backend/ai/parser/` 下创建 `ocr_parser.py`：
   - 用 PaddleOCR 识别扫描件/图片中的文字
   - 输出格式与 docx/pdf 解析器统一
2. parser `__init__.py` 增加文件类型检测：图片文件（.jpg/.png/.tiff）→ 走 OCR
3. 基础质量检测：图片 DPI < 150 时提示

**✅ Day 12 D+E 验收：** 上传一张清晰的合同扫描件 → OCR 提取出全文 → 后续审核正常。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**全员 — 边界场景处理：**
> 在 frontend/src/views/ContractUpload.vue 中，el-upload 增加 before-upload 钩子：检查文件类型（只允许 .docx/.pdf/.jpg/.png/.tiff），检查文件大小（限制 20MB），不符合的在控制台 warn 并用 ElMessage.error 提示用户。在 backend/api/contract_router.py 的 upload 端点中增加文件大小二次检查（限制 30MB）和 MIME 类型校验。在 backend/services/audit_service.py 中给整个 audit_contract 函数包 try/except，任何未捕获异常 → 设置 audit_batch status='failed' + 写入错误详情 → 返回友好错误信息（不暴露内部调用栈）。

**D — LLM 容错：**
> 在 backend/ai/classifier/classifier.py 和 backend/ai/extractor/extractor.py 中增加降级逻辑：如果 LLM API 调用超时（10s）或返回非 JSON → 分类器返回 {"contract_type":"未识别","confidence":0.0,"fallback":true}，抽取器返回 {"parties":{"甲方":"未知","乙方":"未知"},"amount":"未识别",...,"fallback":true}。降级结果在审核报告中明确标注"AI 自动分类失败，请手动选择合同类型"。

**D + E — OCR 解析器：**
> 在 backend/ai/parser/ocr_parser.py 中实现 parse_image(filepath) -> dict，用 PaddleOCR（from paddleocr import PaddleOCR，ocr = PaddleOCR(lang='ch')）识别图片中的文本。先用 PIL 检查图片 DPI（<150 时在结果中标记 low_quality:true）。输出格式与 docx/pdf 解析器保持一致：{'paragraphs':[...], 'full_text':'...', 'ocr_quality': 'normal'/'low'}。在 parser/__init__.py 的 detect_and_parse 中增加图片格式检测（.jpg/.png/.tiff → 走 ocr_parser）。

---

## Day 13（8.1 周四）— 全系统集成 + Demo 流程走查

> 今日目标：**端到端全流程走 3 遍，每一遍都不出错。确定 Demo 用的合同和演示流程。**

### 全员（上午 9:00-12:00 — 集成测试）

1. 准备 5 份不同类型的测试合同（采购/销售/保密协议/服务外包/劳动）
2. 每份合同走完整流程：上传 → 自动分类 → 三种审核模式各跑 1 次
3. 对比三种模式的输出差异：
   - 轻量：风险数、耗时、成本
   - 标准：风险数、Agent 日志、耗时、成本
   - 深度（如果 Discuss 还没做完，先标记为"开发中"）
4. 记录所有异常和体验问题

### 下午（13:00-17:00 — Demo 流程确定 + 修复）

1. 选定用于 Demo 展示的合同（1 份采购合同 + 1 份技术外包合同，要求风险点分布合理、热力图好看）
2. 确定 Demo 演示流程（5 分钟内）：
   - 登录 → 上传合同（10 秒）
   - 选择标准模式 → 开始审核（等待时讲解技术架构，60 秒）
   - 审核结果展示 → 展开 Agent 日志 → 点击跳转原文（90 秒）
   - 切换到报告页 → 热力图 + 条款比对（60 秒）
   - 反馈标注演示（30 秒）
   - 总结亮点（30 秒）
3. 按 Demo 流程走 3 遍，每遍计时，确保 5 分钟内完成
4. 修复 Demo 中出现的任何问题

**✅ Day 13 验收：** Demo 流程 3 遍都不出错，每遍 5 分钟内完成。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**全流程测试脚本：**
> 在 backend/tests/ 下创建 test_full_pipeline.py，用 pytest + httpx.AsyncClient 写端到端集成测试：
> 1. test_register_login: POST /api/auth/register → POST /api/auth/login → 验证返回 token
> 2. test_upload_and_audit: 用 token 上传 fixtures/test_contract.docx → 获取 contract_id → POST /api/audit/start mode=standard → 轮询 GET /api/audit/status/{batch_id} 直到 completed → GET /api/audit/result/{batch_id} → 验证返回风险列表不为空
> 3. test_report: GET /api/reports/{batch_id} → 验证返回 HTML 含六个章节标题
> 4. test_feedback: POST /api/feedback → GET /api/feedback/{contract_id} → 验证反馈存储成功
> 5. test_boundary_empty_file: 上传 0 字节文件 → 验证返回 400
> 6. test_boundary_wrong_type: 上传 .exe 文件 → 验证返回 400

**A + B — Demo 流程优化：**
> 在 AuditResult.vue 中优化风险列表加载动画：从顶部依次滑入（用 Vue 的 <TransitionGroup> 或 CSS animation，每条风险延迟 50ms 依次出现）。在 AuditReport.vue 中优化热力图渲染：加 loading 骨架 → 数据加载完成后从透明渐显（transition opacity 0.5s）。在 ContractDetail.vue 的审核进度展示中，用 el-steps 步骤条代替 el-progress 百分比：解析中 → 分类中 → 审核中 → 比对中 → 生成报告中 → 完成（当前步骤高亮，已完成步骤打勾）。

---

## Day 14（8.2 周五）— Bug 清零 + UI 最终打磨

> 今日目标：**所有已知 bug 修复，页面视觉统一、专业。**

### 全员（全天 — Bug 清零）

1. 整理 bug 清单（来源：Day 12 边界测试 + Day 13 集成测试 + 个人开发中记录的问题）
2. 按优先级排序：P0（Demo 中会出现）> P1（功能正确性）> P2（体验优化）
3. P0 和 P1 今天必须修完

### A + B（UI 最终打磨）

1. 统一全部页面视觉风格：
   - 主色 #409EFF（Element Plus 默认蓝）
   - 所有 el-card 加 shadow="hover"
   - 所有表格加 stripe + border
   - 所有按钮统一大小（default / small 二选一）
2. 页面标题统一（每个页面顶部 h3 标题 + el-divider）
3. 移动端基本可用（不要求完美，但布局不崩塌）

### C（API 最终检查）

1. 检查全部 API 的请求/响应格式是否与前端一致
2. 检查全部 API 的错误返回格式是否统一
3. 检查 JWT 过期后的刷新逻辑（如果没做，至少在过期时给明确提示）

### D + E（AI 输出一致性）

1. 同一合同用相同模式审核 3 次，对比 3 次结果的差异：
   - 风险检出数量差 ≤ 2 条
   - 同一风险的等级一致率 ≥ 90%
   - 如果不一致，检查 temperature 是否 = 0.1、Prompt 是否有随机性

**✅ Day 14 验收：** P0/P1 bug 全部清零。同一合同 3 次审核结果一致。


### 💡 今日 Cowork 提示词速查（直接复制到 Cowork）

**审核一致性测试：**
> 在 backend/tests/ 下创建 test_consistency.py，用 pytest 实现：同一份合同（fixtures/test_contract.docx）用相同模式（standard）审核 3 次。验证：（1）三次审核的风险检出数量差 ≤ 2 条；（2）同一条款（按 clause_text 匹配）在三次审核中的风险等级相同率 ≥ 90%；（3）如果不一致，打印差异详情供排查。用 subprocess 调 curl 或直接用 httpx 依次调 POST /api/audit/start 三次，等全部完成后比对结果。

**UI 最终检查清单脚本：**
> 在 frontend/src/ 下创建一个配置检查对象（放在 utils/theme.js 中导出），定义全局视觉规范：（1）主色 #409EFF（保持 Element Plus 默认蓝）；（2）所有 el-button 统一 type="primary" 或默认，不混用多种 type；（3）所有 el-table 统一 stripe + border + empty slot（显示 el-empty）；（4）所有页面顶部统一 h3 标题 + el-divider；（5）所有数字超过 999 的用 toLocaleString() 加千分位分隔。然后在 App.vue 中全局引入。

**Swagger 文档完善：**
> 在 backend/ 下所有 router 文件中，给每个端点加上完整的 FastAPI docstring（summary + description + response_description），给所有请求体加上 Pydantic model 定义（包括 field 的 description 和 example），给所有响应体加上 response_model。确保 /docs 页面中每个端点的"Try it out"功能可用（不需要手动填复杂参数）。

---

## Day 15（8.3 周六）— Demo 冻结 + 录制 + Phase 3 交付

> 今日目标：**代码冻结，录制 Demo 视频，截图留档，Phase 3 正式交付。**

### 上午 9:00-11:00 — 最终验收走查

组长 C 主持逐项检查（不开新功能、不改 UI、只确认现有功能正常）：

| # | 验收项 | 标准 | 状态 |
|---|-------|------|:--:|
| 1 | 注册登录完整闭环 | 注册→登录→首页 | ☐ |
| 2 | 合同上传+自动分类 | 上传→解析→分类→列表 | ☐ |
| 3 | 三种审核模式 | 轻量/标准/深度都能启动并返回结果 | ☐ |
| 4 | 审核结果展示 | 风险列表+等级+置信度+Agent日志 | ☐ |
| 5 | 审核报告展示 | 六章+热力图+饼图+条款比对 | ☐ |
| 6 | 反馈标注 | 确认/修正/误报/补充→存储 | ☐ |
| 7 | 边界场景 | 异常输入有合理提示 | ☐ |
| 8 | 全流程 Demo | 5 分钟内完整走完 | ☐ |

### 上午 11:00-12:00 — Demo 视频录制

1. 录屏工具：OBS Studio（免费）/ 系统自带录屏
2. 录制 3 遍，选最好的 1 遍
3. 视频要求：1080p、有语音解说、5 分钟内、展示核心功能全流程
4. 如果录屏一直卡——先截图，截图够 20 张也能交

### 下午 13:00-16:00 — 材料整理

1. 全部页面截图（PNG，1920×1080）
2. Swagger API 文档截图
3. 数据库 ER 图（用 DBeaver 自动生成）
4. 系统架构图（draw.io 或 Excalidraw 画）
5. 打包：代码（GitHub 仓库）+ 文档 + 截图 + Demo 视频

### 下午 16:00-17:00 — 最终缓冲

- 任何最后一刻发现的问题紧急修复
- 如果一切顺利：提前收工庆祝

### 💡 今日 Cowork 提示词速查 — 今天不写代码，只做下面这些：

**打包脚本：**
> 在项目根目录创建 scripts/package.sh（Windows 用 package.ps1），执行以下操作：（1）复制 docs/ 目录下所有文档到 dist/package/docs/；（2）复制 screenshots/ 下所有截图到 dist/package/screenshots/；（3）用 git archive 导出当前代码到 dist/package/source.zip；（4）打印"打包完成，文件位于 dist/package/"。如果有 Demo 视频文件，手动复制到 dist/package/。

**快速检查脚本：**
> 在项目根目录创建 scripts/check.sh，检查以下项并输出 ✓ 或 ✗：（1）backend/ 下所有 .py 文件无语法错误（python -m py_compile）；（2）frontend/ 下 npm run build 不报错；（3）docker-compose.yml 语法正确（docker compose config）；（4）.gitignore 包含 venv/、node_modules/、*.db、uploads/、.env。

---

## Phase 3 交付后：8.4 - 9.3 深度冲刺规划

Phase 3 Demo 交付（8.3）后，到 9 月初还有约 4 周时间。这段时间用于将 Demo 打磨为比赛级作品：

### 第 4 周（8.4-8.10）：BERT 训练 + Corex Discuss

| 任务 | 负责人 | 产出 |
|------|:-----:|------|
| BERT 微调训练（5 分类）| D | ONNX 模型 + 准确率 ≥ 88% |
| Corex Discuss 辩论裁决 | D+E | 分歧检测器 + 红蓝辩论 3 轮 + 裁判裁决 |
| 深度模式全流程打通 | C | Retrieve+Review+Discuss 完整管道 |
| 前端深度模式展示 | B | Discuss 辩论过程可视化 |

### 第 5 周（8.11-8.17）：知识库扩充 + 性能优化

| 任务 | 负责人 | 产出 |
|------|:-----:|------|
| 知识库扩充至目标规模 | E | 80 案例 + 150 法条 + 150 模板 + 审核经验库积累 |
| 检索 pipeline 性能优化 | E | 检索耗时 < 500ms，Rerank < 200ms |
| 前端性能优化 | A+B | 页面加载 < 2s，图表渲染 < 500ms |
| 批量审核功能 | C | 一次上传多个合同，队列处理 |

### 第 6 周（8.18-8.24）：答辩素材 + 实验数据

| 任务 | 负责人 | 产出 |
|------|:-----:|------|
| 答辩 PPT（20 页）| 组长 C + A | 问题→方案→架构→亮点→Demo→数据 |
| 消融实验 | D+E | 附录 D 全部实验数据（含图表） |
| 性能测试报告 | C | 三种模式耗时/成本/准确率对比 |
| 用户手册 | A+B | 5 页操作指南（截图+文字） |

### 第 7 周（8.25-8.31）：答辩排练 + Bug 清零

| 任务 | 负责人 | 产出 |
|------|:-----:|------|
| 答辩排练（≥5 遍）| 全员 | 15 分钟答辩 + 5 分钟 QA |
| 全系统回归测试 | 全员 | 50 份合同测试，零 P0 bug |
| 文档终稿 | C | 设计文档 + 开工指南最终版 |
| 材料提交包 | 组长 | 代码 + 文档 + PPT + 视频 + 截图 |

### 第 8 周（9.1-9.3）：最终缓冲

- 完全空白，什么都不排
- 用于应对突发问题 or 提前交付
- 如果一切顺利：答辩排练加练、文档润色

---

## 附录 A：每日检查清单汇总

| Day | 日期 | 核心目标 | 交付物 |
|:---:|------|---------|--------|
| 0 | 7.17 周五 | 环境搭建 | 5 人环境就绪 |
| 1 | 7.18 周六 | 骨架代码 | 页面骨架 + API 占位 + 模块接口 |
| 2 | 7.19 周日 | 数据库 + 解析器 + 登录注册 + Dify 工作流 | 表建完 + docx/pdf 解析 + 登录页 + Dify 可调用 |
| 3 | 7.20 周一 | 上传+列表+分类器+RAG+前端调用 Dify | 前后端联调 + 分类+检索可用 + AI 测试按钮 |
| 4 | 7.22 周二 | 审核引擎核心 + 审核结果页 | 审核管道跑通 + 风险列表展示 |
| 5 | 7.23 周三 | 审核报告 + 条款比对 + 前端闭环 | HTML 报告可用 + 比对结果 |
| 6 | 7.24 周四 | 全管道联调 + 反馈面板 | 端到端走通 + 反馈标注 |
| 7 | 7.25 周五 | 前端打磨 + Docker + 知识库完善 | Phase 2 交付准备 |
| 8 | 7.26 周六 | Phase 2 交付冻结 | 全功能验收 + 截图留档 |
| 9 | 7.27 周日 | 缓冲日 + Phase 3 预研 | 追进度 or Corex 技术准备 |
| 10 | 7.29 周一 | Corex Review 模式 | 多 Agent 交叉验证跑通 |
| 11 | 7.30 周二 | Dify 降级集成 + RAG 增强 | Corex 超时→Dify 回退 + RAG 优化 |
| 12 | 7.31 周三 | 边界场景 + OCR | 异常不崩溃 + OCR 可用 |
| 13 | 8.1 周四 | 全系统集成 + Demo 走查 | Demo 流程 5 分钟走完 |
| 14 | 8.2 周五 | Bug 清零 + UI 打磨 | P0/P1 bug 清零 |
| 15 | 8.3 周六 | Demo 冻结 + 录制 + 交付 | 视频 + 截图 + 打包 |

---

## 附录 B：紧急联系方式

| 谁能帮你 | 擅长什么 | 什么时候找 |
|:-------:|---------|----------|
| C（后端） | API 调不通、数据库错、Token 问题 | 前端调接口报错 |
| D（AI） | LLM 调用失败、分类不准、Corex 报错 | 审核结果为空或质量差 |
| E（AI） | RAG 检索、ChromaDB、条款比对、报告生成 | 知识库问题、比对不准、报告乱码 |
| A（前端） | 页面白屏、路由跳转、Vue 报错 | 前端页面问题 |
| B（前端） | ECharts 图表、pdf.js、样式布局 | 热力图不显示、PDF 问题 |

---

> **指南到此结束。每天 21:00 群内发当日进度，格式：`[日期] 角色: 完成项 ✓ | 待解决问题 ⚠️ | 明天计划 →`。**
> 
> **最重要的原则：宁可少做一个功能，也要保证演示不崩。3 周 5 个人，跑通 > 完美。**
