# A24 基于大模型的企业合同智能审核与风险预警系统

# 详细设计文档

> 版本：v3.0
> 日期：2026年7月16日
> 团队：海底汪汪队

---

## 目录

1. [项目概述与商业价值](#1)
2. [核心方案一：合同智能分类与要素抽取](#2)
3. [核心方案二：多层递进式风险条款识别](#3)
4. [核心方案三：标准条款语义比对与缺失检测](#4)
5. [核心方案四：用户反馈闭环与审核报告生成](#5)
6. [系统架构总览](#6)
7. [创新亮点总结](#7)
8. [开发计划与里程碑](#8)
9. [数据库设计](#9)
10. [API接口设计](#10)
11. [部署与运维](#11)
12. [安全与企业适配](#12)
13. [V2.0 未来规划](#13)
14. [附录A：项目目录结构](#14)
15. [附录B：技术风险与应对](#15)
16. [附录C：鼓励创新方向（赛题第7节）](#16)
17. [附录D：量化实验指标与验证方案](#17)
18. [附录E：数据集与模型训练方案](#18)
19. [附录F：赛题提交材料对应体系](#19)

---

# 第一部分：方案设计篇

---

## <a id="1"></a>一、项目概述与商业价值

### 1.1 项目定位

**SmartContract Auditor** 是一款面向企业法务与风控部门的 AI 原生合同智能审核平台，深度适配网新恒天等 IT 服务与金融科技企业的合同管理场景。它以"人机协同审核"为核心理念，融合大语言模型的知识推理能力、多 Agent 协作的交叉验证机制以及专业法务知识库，实现从"合同上传 → 智能审核 → 风险预警 → 报告生成"的全流程闭环。

### 1.2 赛题要求覆盖与达标方案

> 比赛文档的第一要务：先证明基础指标达标，再展示创新加分。以下逐项对照赛题强制要求，说明本系统的达标方案和对应的量化指标验证位置。

| 赛题要求 | 达标方案 | 预期指标 | 验证 |
|---------|---------|:------:|------|
| 合同类型分类（≥5 种，准确率 ≥85%）| 双模型协同：BERT 微调（主力）+ LLM 兜底；BERT 不可用时降级为纯 LLM 分类 | 预期 ≥ 90% | 附录 D.2 |
| 关键要素抽取（F1 ≥ 80%）| JSON Schema 约束 + Few-shot Prompting + Pydantic 校验 | 预期 ≥ 82% | 附录 D.2 |
| 风险条款识别（≥10 类，召回率 ≥75%）| 三层递进：规则引擎（12 类）+ RAG 语义增强 + Corex Review 多 Agent 交叉验证 | 预期 fast ≥ 85%；precise ≥ 92% | 附录 D.2 |
| 标准条款比对与缺失检测 | 双层语义对齐（关键词匹配结构对齐 + 向量语义对齐）| 预期 条款级匹配 F1 ≥ 85% | 第四章 + 附录 D.2 |
| 自动生成结构化审核报告 | 模板引擎 + AI 生成摘要 + ECharts 风险热力图 | 六章结构报告 | 第五章 |
| Web 可视化界面 | Vue 3 SPA + pdf.js 点击跳转（P0）/ 字符级高亮（P1）+ ECharts 热力图 | 五个核心页面 | 第六章 |
| 用户反馈标注 | 交互式标注（确认/修正/补充）+ 反馈数据结构化存储支持导出 | 闭环机制 | 第五章 |

### 1.3 功能范围与交付优先级

系统明确区分三个交付优先级，确保 3 周开发周期内核心功能可完整演示：

**P0（必须交付 — 赛题强制要求 + 演示闭环基础）：**

合同上传与多格式文档解析（含 OCR）、合同类型分类（LLM 分类 + BERT 可选优化）、关键要素结构化抽取、规则引擎 + RAG 语义审核（12 类风险）、标准条款比对与缺失检测、审核报告生成（含 ECharts 风险热力图）、用户反馈标注、Web 可视化界面。

**P1（冲刺交付 — 差异化加分项，P0 完成后再做）：**

Corex Review 多 Agent 交叉验证（核心差异化，P0 完成后优先冲刺）、JWT 认证 + 基础角色区分、Docker Compose 一键部署。

**P2（未来规划 — 文档设计但不开发，答辩 PPT 标注"V2.0 规划中"）：**

Corex Discuss 辩论裁决模式、Corex Retrieve 多源检测融合、自定义审查规则引擎、合同版本差异对比、企业知识库管理、RBAC 三级权限体系、完整操作审计日志、数据加密体系、多轮对话合同修改、知识图谱跨条款关联分析、印章/签名区域定位。详见第十三章"未来规划"。

### 1.4 评分要点对应与核心指标达成

为方便评委按评分维度快速定位，以下表格逐项对应初赛评分标准：

| 评分维度 | 本系统对应章节 | 关键内容 |
|---------|-------------|---------|
| 需求分析 | 第一章（1.1-1.5）| 商业价值故事 + 赛题覆盖矩阵 + 功能优先级矩阵 |
| 方案设计 | 第二至五章 | 四个核心技术方案的问题分析 → 方案设计 → 创新 → 指标 |
| 技术创新 | 第三章（Corex）+ 第七章 | Corex 多 Agent 辩论式审核 + 横向对比表 + 五重精准率保障 |
| 工程可行性 | 第九至十二章（工程实现篇）| 数据库设计 + API 设计 + Docker 部署 + 降级方案 |
| 商业价值 | 第一章（1.5）| 量化价值对比（审核时间 80%↓、风险遗漏 5%↓） |
| 演示效果 | 第八章（开发计划）| 分阶段里程碑 + MVP 降级路径 |

**核心指标达成总览（对标赛题强制要求）：**

以下指标均为基于技术方案论证的**预期值**，最终实测数据将在开发完成后以附录 D 实验方案验证并更新。所有预期值均远超赛题强制要求。

| 赛题强制指标 | 赛题要求 | 本系统预期值 | 状态 |
|------------|:------:|:--------:|:---:|
| 合同分类准确率 | ≥ 85% | 预期 ≥ 90%（双模型）/ 预期 ≥ 85%（纯 LLM 降级）| ✅ 远超 |
| 要素抽取 F1 | ≥ 80% | 预期 ≥ 82% | ✅ 远超 |
| 风险识别召回率 | ≥ 75% | 预期 ≥ 85%（fast）/ 预期 ≥ 92%（precise）| ✅ 远超 |
| 风险识别精准率 | ≥ 75% | 预期 ≥ 82%（fast）/ 预期 ≥ 92%（precise）| ✅ 远超 |
| 风险类型覆盖 | ≥ 10 类 | 12 类（R01-R12）| ✅ 远超 |
| 审核时间 | — | 预期 fast ~8s / precise ~20s | ✅ |
| API 成本 | — | 预期 fast < 0.2 元/份 / precise < 0.5 元/份 | ✅ |

### 1.5 一句话价值主张

> **"把法务从逐条比对中解放出来，5 分钟完成一份百页合同的全面审核——不只是找风险，更是沉淀企业法务知识资产。"**

### 1.6 商业价值故事

网新恒天（Insigma Hengtian）作为国内领先的 IT 服务与金融科技企业，法务部每天面对大量技术服务合同、软件开发外包合同、SaaS 服务协议和金融客户合作协议。李律师是法务部的主办律师，每份合同需要逐条审核几十页的条款——不仅关注常规的违约金、保密、知识产权，还需要特别关注 IT 服务行业的特有风险：**SLA 服务等级条款是否合理、数据导出与销毁条款是否完备、知识产权交付物归属是否清晰、源代码托管是否约定**。

**一份典型的技术外包合同完整审核通常需要 2-4 小时。** 更棘手的是，李律师休年假时，同事接手完全不熟悉他之前与客户的特殊约定——法务知识沉淀在个人脑中，人走了经验就没了。

**引入 SmartContract Auditor 后：**

- 李律师将百页技术外包合同一键上传，系统在 30 秒内完成全文解析、类型识别、要素提取
- AI 自动标注了 15 处风险条款（其中 3 处高风险、7 处中风险、5 处低风险），涵盖常规风险（违约金、保密）和行业特有风险（SLA 罚则过高、数据导出条款缺失、IP 归属模糊），每处附带原文定位、风险等级、修改建议和参考条款
- Corex 多 Agent 协作引擎自动调用了法务、合规、财务三个专业视角交叉审核，并对其中 2 处争议条款（"源代码托管是否必须""验收标准是否合理"）启动了辩论裁决模式——模拟了企业法务部"重大条款上会讨论"的真实流程
- 系统自动比对网新恒天的标准合同模板，发现缺失了"不可抗力条款""数据隐私保护条款"和"源代码托管条款"，并给出了补全建议
- 李律师用 5 分钟审核了所有 AI 标注，修正了 2 处误报，补充了 1 处 AI 未发现的行业特定风险（金融客户的监管合规要求）
- 点击"生成报告"，一份结构化的审核报告（含风险分布热力图、条款对比表、具体修改建议）导出为 PDF，发给业务部门
- 上周的审核记录、风险标注、修改决定全部存档在系统中，新同事可以直接搜索历史案例

**量化价值对比：**

| 指标 | 传统模式 | AI 辅助模式 |
|------|---------|-----------|
| 单份合同审核时间 | 2-4 小时 | 5-15 分钟 |
| 风险遗漏率 | ~15%（人工疲劳） | <5%（AI 全覆盖） |
| 法务知识可传承性 | 依赖个人 | 系统沉淀，可搜索 |
| 跨合同关联风险发现 | 几乎不可能 | 自动关联提醒 |
| API 调用成本 | — | 预期 < 0.5 元/份（精准模式） |

### 1.7 本文档阅读指引

本文档分为三部分：

- **方案设计篇（第一至第八章）：** 围绕四个核心技术问题展开，遵循"问题分析 → 方案设计 → 创新亮点 → 预期指标"的叙事逻辑。评委和答辩老师可重点阅读。其中 1.2 节为赛题达标总览，1.3 节为功能优先级矩阵。
- **工程实现篇（第九至第十二章）：** 数据库设计、API 接口、部署方案、安全方案等工程落地细节。证明方案的可行性和完整性。
- **附录（A-E）与未来规划（第十三章）：** 目录结构、风险应对、创新方向、实验指标、数据集方案；V2.0 规划功能清单。作为支撑材料，答辩时可选择性引用。

---

## <a id="2"></a>二、核心方案一：合同智能分类与要素抽取

> **赛题指标：** 合同类型分类 ≥ 5 种，准确率 ≥ 85%；关键要素抽取 F1 ≥ 80%

### 2.1 问题分析

合同审核的第一步是"搞清楚这是什么合同、里面有什么"。传统做法是法务人员手工翻阅，判断合同类型、提取甲乙双方、金额、日期等关键信息——这类机械性工作占审核总时间的 30% 以上。

**技术挑战：** 合同类型多样（采购、销售、保密、外包、劳动等），表述方式差异大（"买方/卖方""甲方/乙方""需方/供方"），单一模型难以同时兼顾速度和准确率。

### 2.2 双模型协同分类（BERT + LLM）

系统采用"本地小模型 + 云端大模型"双模型协同策略，兼顾速度、成本和准确率：

**第一级 — BERT 本地分类（覆盖 90% 合同，推理 < 100ms，零成本）：** 使用 bert-base-chinese 在自建中文合同数据集上微调，支持 5 种合同类型分类（采购、销售、保密协议、服务外包、劳动）。模型以 ONNX 格式导出部署，CPU 推理即可。

**第二级 — LLM 兜底验证（覆盖 10% 低置信度合同）：** 当 BERT 分类置信度 < 0.85 时（如格式特殊或类型模糊的合同），自动调用 DeepSeek-V3 做二次确认并输出可解释的分类理由。

**效果预期（对应赛题指标 1）：** 双模型协同下分类准确率预期 ≥ 90%，其中 BERT 独立准确率 ~88%，LLM 兜底将剩余 12% 中的 2/3 修正正确。LLM 调用次数相比纯 LLM 方案降低 70%，API 成本大幅下降。

**BERT 训练失败降级方案：** 若标注数据不足或微调后验证准确率未达 85%，系统默认降级为纯 LLM 分类方案（DeepSeek-V3 Few-shot Prompting）。此时分类准确率预期 ~85%（仍满足赛题要求），但每份合同增加约 0.03 元 API 成本（约 500 input tokens）。BERT 微调作为可选优化，在数据准备好后补充训练并热替换，不影响系统主线功能。第一周优先跑通纯 LLM 分类管道，BERT 训练并行推进。

数据集构建与 BERT 微调方案详见附录 E。

### 2.3 关键要素结构化抽取

分类完成后，系统从合同中抽取结构化关键要素。采用 JSON Schema 约束 + Few-shot Prompting 方案：在 Prompt 中预定义 JSON Schema（parties / amount / date / dispute_resolution / governing_law 等字段），配合 3-5 个标注示例引导 LLM 输出规范 JSON。后端通过 Pydantic 验证层做格式校验和容错。

**预期抽取字段：** 甲方/乙方（含角色：买方/卖方）、合同金额（数值 + 币种）、签署日期、履行期限（起止日期）、争议解决方式、适用法律。

**效果预期（对应赛题指标 2）：** 字段级 F1 预期 ≥ 82%，其中双方信息抽取 F1 ~90%、金额抽取 F1 ~95%、日期抽取 F1 ~88%。要素抽取结果结构化存入 contracts 表的 extracted_elements 字段（JSON 格式，详见第九章数据库设计）。

### 2.4 文档解析支撑

分类和抽取的前提是高质量的文本提取。系统支持三种格式的解析：

| 格式 | 处理方案 | 核心库 |
|------|---------|--------|
| .docx | 直接提取文本 + 保留段落结构（标题/条款/表格/签名区）| python-docx |
| .pdf（文本型）| 直接提取文本 + 表格识别 | pdfplumber |
| .pdf（扫描型）/ 图片 | OCR → 版面分析 → 文本提取 | PaddleOCR |

**扫描质量预检与容错（P0 简化版）：** P0 阶段的核心目标是"可提取文本即可"——PaddleOCR 对清晰扫描件的识别准确率已足够支撑后续分类和审核。上传扫描件/图片时，系统执行基础质量预检（检测 DPI < 150 时提示可能影响识别效果），OCR 识别结果中置信度 < 0.6 的区域在风险标注时附带"OCR 识别质量低，建议人工核对原文"提示。高级质量检查（倾斜检测、缺页告警、法律术语字典纠错）和置信度分级标注体系列入 V2.0 优化。

解析过程中保留段落层级结构（章-条-款-项），这是后续风险定位和原文高亮的技术基础——每一条 AI 标注都能精确追溯到原文的页码和行号。

---

## <a id="3"></a>三、核心方案二：多层递进式风险条款识别（含 Corex ★）

> **赛题指标：** 风险识别 ≥ 10 类，召回率 ≥ 75%

### 3.1 问题分析

风险识别是合同审核的核心。单一方法的局限性明显：规则引擎覆盖率高但无法处理模糊语义（如"合理的违约金"是多少？）；LLM 语义理解强但存在幻觉风险（可能标注出不存在的条款）；RAG 检索能补充知识但无法交叉验证。

**我们的方案：** 三层递进式架构——规则引擎（快、准、零成本）→ RAG 语义增强（广、深、有据）→ Corex 多 Agent 协作（稳、全、可信）。三层各司其职，层层加码，确保不遗漏、不误报。

> **Corex 触发的前提：** Corex 中所有 Agent 共享统一的量化风险等级判别标准（高：> 10 万元潜在损失或违反强制性法律规定；中：可能引发合同争议或商业风险；低：表述不够精确但可控），并在各自的 System Prompt 中明确嵌入。此设计确保多 Agent 判断的一致性——Agent 之间的分歧来自视角差异而非标准不一致。

### 3.2 第一层：规则引擎（12 类风险，本地运行，< 0.1 秒）

系统预置 12 类合同风险检测规则，覆盖最常见的合同风险类型。每条规则由触发条件和风险等级组成，本地运行，零 API 成本：

| 编号 | 风险类型 | 检测方法 | 风险等级判别标准 |
|:---:|---------|---------|----------------|
| R01 | 违约金过高 | 规则 + 语义 | 超过合同金额 20% → 高风险；10-20% → 中风险 |
| R02 | 无限责任条款 | 语义识别 | "全部损失""一切责任"等表述 → 高风险 |
| R03 | 单方解约权 | 语义识别 | 仅一方有任意解除权 → 中风险 |
| R04 | 管辖条款不利 | 实体识别 | 约定对方法院管辖 → 中风险 |
| R05 | 保密期间不合理 | 规则 + 语义 | 永久/无限期保密 → 高风险；超 5 年 → 中风险 |
| R06 | 知识产权归属不清 | 语义识别 | 未明确归属或默认归对方 → 高风险 |
| R07 | 付款条件不公平 | 规则 + 语义 | 预付比例 > 50% → 中风险；验收后不付 → 高风险 |
| R08 | 验收标准缺失 | 关键短语检测 | 合同未定义验收标准 → 中风险 |
| R09 | 不可抗力条款缺失 | 关键词检索 | 未出现"不可抗力"相关表述 → 中风险 |
| R10 | 竞业限制过宽 | 语义识别 | 限制范围/期限明显不合理 → 中风险 |
| R11 | 自动续约陷阱 | 规则匹配 | "期满自动续约"且无提前解约通知机制 → 中风险 |
| R12 | 数据/隐私条款缺失 | 关键词 + 语义 | 涉及用户数据但未定义保护条款 → 高风险 |

> **行业适配说明：** 以上 12 类风险已覆盖 IT 服务与金融科技行业的高频风险场景——包括 SLA 罚则（R01/R07 覆盖）、数据导出与销毁条款（R12 覆盖）、知识产权交付物归属（R06 覆盖）、源代码托管义务（R03/R08 覆盖）。系统预置的采购合同和外包服务合同两类模板中已嵌入上述行业特有条款的标准文本，直接适配网新恒天的业务场景。

每条审核结果结构化存入 audit_records 表，保留 detection_method = 'rule' 标记。

> **注：** 自定义审查规则（可视化表单配置）属于 V2.0 规划功能，custom_rules 表已在第九章数据库设计中预留。P0 阶段仅使用上述 12 条系统预置规则，确保 3 周内可完整实现。

### 3.3 第二层：RAG 语义增强审核

规则引擎能覆盖确定性风险，但面对模糊条款（如"合理的违约金"到底是多少？）需要语义理解能力。

系统构建了四个专业向量知识库（ChromaDB），审核时自动检索最相关的 Top-5 条目注入 LLM Prompt 上下文：

| 知识库 | 内容 | 规模 | 用途 |
|-------|------|:---:|------|
| 风险案例库 | 12 类风险的典型案例 + 裁判结果 | ~80 条 | 辅助判断：类似条款历史上是否被认定为风险？|
| 法律法规库 | 《民法典》合同编条文 + 司法解释 | ~200 条 | 法条依据：当前条款违反了哪条法律？|
| 标准条款库 | 5 种合同类型的标准条款文本 | ~150 条 | 参考基准：标准条款通常怎么写？|
| 审核经验库 | 用户确认的历史审核记录 | 运行时积累 | 知识沉淀：本企业法务之前怎么判断？|

**检索与分块策略：**

分块方案：法律条款上下文关联性强，简单的固定长度切分会割裂条款语义。系统采用"按条款标题分块"策略——利用文档解析阶段保留的段落层级结构，将合同按"章-条-款-项"切分为独立条款块，确保每条条款完整不被截断。对于超过 1024 tokens 的长条款，采用滑动窗口（窗口 512 tokens，重叠 128 tokens）分块，检索时根据窗口的向量均值排序。

**无条款标题的降级方案：** 当合同文档无明确层级结构（如简单协议、非标准格式合同）导致无法按标题切分时，系统降级为固定长度滑动窗口分块（窗口 768 tokens，重叠 256 tokens），以保持语义连续性。此降级方案确保任意格式合同都能被 RAG 检索覆盖。

检索重排序（Rerank）：ChromaDB 余弦检索召回 Top-20 候选后，通过 Cross-Encoder Reranker（text2vec-large-chinese 的 cross-encoder 模式）对候选列表做精细重排序，将 Top-20 精排为 Top-5 注入 LLM Prompt。相比纯余弦检索，Rerank 后检索相关率从 ~70% 提升到 ~85%。

检索置信度过滤：Rerank 后 score < 0.5 的条目不注入 Prompt（避免不相关内容误导 LLM）；score 0.5-0.7 的条目附带"低相关性参考"标注注入；score > 0.7 的条目正常注入。检索零命中时（无任何 score > 0.5 的条目），跳过 RAG 步骤，仅用规则引擎结果 + LLM 自身知识做判断，避免空检索浪费时间。

RAG 增强后，模糊条款的判断准确率从纯 LLM 的 ~65% 提升到 ~72%。

### 3.4 第三层：Corex 多模型协作推理引擎 ★（核心创新）

**这是 A24 区别于所有已知合同审核系统的核心技术壁垒。** 规则引擎和 RAG 解决了"覆盖面"问题，但法律审核最致命的问题是"幻觉"——AI 告诉你"第 8 条存在违约金过高风险"，但原文根本没有违约金条款。Corex 通过多 Agent 交叉验证，将风险召回率从 78% 提升到 92%，误报率从 20% 降低到 8%。

> **P0/P1 边界说明：** P0 底线为规则引擎 + RAG + 单 LLM 审核（轻量模式），已满足赛题全部强制指标（召回率预期 ≥ 85%，远超赛题要求的 ≥ 75%）。Corex 多 Agent 协作（标准模式和深度模式）属于 P1 冲刺项——在 P0 全部跑通后再启动，作为差异化加分项。

我们借鉴 COLM'24 论文《Corex: Pushing the Boundaries of Complex Reasoning through Multi-Model Collaboration》提出的多模型协作范式，适配到合同审核场景。下表横向对比 Corex 与主流 LLM 优化方案的差异，说明"为什么 Corex 不是简单的多轮 Prompt 调用"：

| 对比维度 | 单一 LLM | Self-Reflection | 普通多 Agent | **Corex（本系统）** |
|---------|:------:|:------------:|:---------:|:---------------:|
| 验证视角数量 | 1 个 | 1 个（自问自答）| 2-3 个（同质 Prompt）| **3-5 个（法务/合规/财务异质视角）** |
| 争议处理能力 | 无 | 无（无法对抗自己）| 无（投票取平均）| **蓝红辩论 → 裁判裁决（对抗式）** |
| 知识源多样性 | 单源（LLM 训练数据）| 单源 | 单源（共享知识库）| **多源（法条/案例/模板，异质知识库）** |
| 可解释性 | 输出结论 | 输出结论 + 自我质疑 | 输出结论 | **逐 Agent 日志 + 置信度 + 审核路径追溯** |
| 幻觉抑制 | Prompt 约束 | 一轮自我检查 | 简单投票 | **存在性验证 + 合理性验证 + 交叉否决** |
| 学术支撑 | 无 | Self-Refine 论文 | Multi-Agent 论文 | **COLM'24 Corex 论文** |

#### 三个范式的本质关系：递进式纵深防御

三个范式**不是并列关系**，而是"基础层 → 核心层 → 仲裁层"的递进执行管线。我们清醒地认识到，全量运行三个范式在商业上是不可行的。因此，我们没有将三者简单堆砌，而是构建了**级联触发逻辑**——只有上一层无法确定结论时，才启动下一层。

| 范式 | 执行顺序 | 类比 | 触发条件 | 核心职责 |
|------|:---:|------|---------|---------|
| **范式一：Retrieve（多路召回）** | 第一阶段 | "智囊团分头查资料" | **全量执行**（必选项） | 多 Agent 用各自知识库同时检索，把"疑似风险"全挖出来——广撒网，确保高召回 |
| **范式二：Review（多角色复核）** | 第二阶段 | "法务部内部三级审批" | **默认执行**（标准/深度模式） | 法务→合规→财务→Self-QA，对 Retrieve 结果交叉验证——过三关，去伪存真 |
| **范式三：Discuss（辩论裁决）** | 第三阶段 | "疑难案件上审委会" | **条件触发**（仅深度模式） | 当 Review 出现争议条款时，红蓝辩论 + 裁判裁决——打官司，断悬案 |

**一句话总结：** Retrieve 负责"广撒网"找线索，Review 负责"过三关"核线索，Discuss 负责"打官司"断悬案。

> **Corex 触发的前提：** Corex 中所有 Agent 共享统一的量化风险等级判别标准（高风险：潜在损失超过 10 万元或违反强制性法律规定；中风险：可能引发合同争议或商业风险；低风险：表述不够精确但可控），并在各自的 System Prompt 中明确嵌入。此设计确保多 Agent 判断的一致性——Agent 之间的分歧来自视角差异而非标准不一致。

---

### 3.4.1 范式一：Retrieve — 多路召回（基础层，全量执行）

Retrieve 是审核管线的**第一道工序**，解决的核心问题是"单一知识来源有盲区"——只查民法典条文会脱离商业实践，只靠案例库会跟不上司法趋势。多个 Agent 同时用自己的专属知识源检索同一份合同，把"疑点"尽可能挖全。

| Agent | 知识来源 | 优势 | 盲区 |
|-------|---------|------|------|
| Agent 1 | 《民法典》合同编条文 | 法条依据权威、可引用 | 不反映商业实践 |
| Agent 2 | 内置风险案例库 | 贴近实务风险模式 | 覆盖范围有限 |

> **初赛阶段定位：** 2 个 Agent（民法典条文 + 风险案例库），调用同一个 DeepSeek-V3 模型——区别仅在于注入的知识来源不同，而非多模型融合。这有助于控制上下文长度和 API 成本。完整 4 Agent 方案（增加行业最佳实践 + 裁判文书网类案）列入 V2.0 规划。

两个 Agent 并行调用后，评分 Agent 对每条风险标注做三件事：核实原文确实存在该条款、判断风险等级是否合理、综合给出置信度分数（0-1）。取置信度最高的方案作为主输出，合并另一个方案中独有的高质量标注（评分 > 0.7）。

**效果：** 单次 Retrieve 耗时约 3-5 秒，API 成本约 0.1 元/份（2 次 LLM 调用）。召回率从单源检索的 ~72% 提升到 ~85%。

---

### 3.4.2 范式二：Review — 多角色顺序复核（核心层，默认执行）

Review 在 Retrieve 的输出之上运行，解决"一个角度可能看走眼"的问题。四个 Agent **顺序执行**（后一个 Agent 能看到前一个的输出），模拟企业法务部"初审 → 合规复核 → 财务复核 → 自检终审"的真实审批流：

| 步骤 | Agent | 职责 | 输入 | 输出 |
|:---:|-------|------|------|------|
| 1 | 法务 Agent | 逐条审核法律风险，参考《民法典》合同编；System Prompt："15 年合同法律师" | Retrieve 的风险标注 + 合同原文 | 逐条确认/修正/新增 + 修改建议 |
| 2 | 合规 Agent | 检查是否符合行业监管要求，补充遗漏法定义务；System Prompt："企业合规官" | 步骤 1 输出 + 原文 | 合规修正意见 |
| 3 | 财务 Agent | 检查付款条件、违约金比例、税务条款的商业合理性；System Prompt："财务总监" | 步骤 2 输出 + 原文 | 财务修正意见 |
| 4 | Self-QA Agent | 逐条自问：原文是否存在该条款？风险等级合理否？有无遗漏？综合前三步输出终审结果 | 前三步汇总 | 终审风险报告 + 置信度 |

三个专业 Agent（法务/合规/财务）使用不同的 System Prompt，从各自专业视角独立判断。Self-QA 步骤同时承担"自检验证"和"终审输出"双重职责——它不只是检查错误，而是综合前三步的所有意见，对每条标注做最终裁定，生成统一的风险报告。

与 CRA 的 Self-QA 相比（单 Agent 自问自答），Corex Review 在 Self-QA 之前已有三个不同视角的交叉审核，Self-QA 只是最后一道防线——多视角验证比单视角自检更可靠。

**效果：** Retrieve + Review 合计耗时约 15-20 秒，API 成本约 0.4-0.5 元/份（6-7 次 LLM 调用）。误报率从仅 Retrieve 的 ~20% 降至 ~10%，精准率提升到预期 ≥ 88%。

---

### 3.4.3 范式三：Discuss — 争议条款辩论裁决（仲裁层，条件触发）

某些条款处于法律上的"灰色地带"（如"付款后 60 日内验收"——是合理商业安排还是变相拖款？），多角色复核也可能出现分歧。Discuss 模式通过对抗式辩论解决此类争议。

**触发条件（分歧检测器）：** Review 阶段结束后，Orchestrator 对所有风险标注执行分歧检测。同时满足以下两个条件的条款自动触发 Discuss：

1. **等级分歧：** 任意两个 Agent 对同一风险条款的等级判断相差 ≥ 2 级（如法务判"高风险"、财务判"低风险"），或
2. **置信度不足：** Self-QA 终审后该条款的置信度 < 0.7

**辩论流程（3 轮）：**

- **蓝方 Agent（保守立场）：** 从"保护我方利益"出发，论证为何该条款构成风险，建议从严处理
- **红方 Agent（商业灵活立场）：** 从行业惯例和对等原则出发，论证该条款的商业合理性
- **裁判 Agent：** 听取双方各 3 轮辩论后，综合给出平衡裁决——不是简单选一方，而是给出有条件的折中方案（如"保留 60 天验收期，增加'逾期未验收视为验收合格'条款，补充分阶段验收机制"）

**资源控制：** Discuss 是稀缺资源——只对 Review 中置信度 < 0.7 或 Agent 意见冲突的 **Top 3 高风险条款**触发，而非对全合同逐条辩论。这确保了在控制算力成本的前提下，最关键的灰色问题得到最充分的论证。

> **模拟合议庭比喻（答辩记忆点）：** "就像法院的模拟合议庭——三位 AI 律师（法务/合规/财务）已经分别审理了合同，大部分条款结论一致。但对于少数'谁也说服不了谁'的争议条款，启动红蓝辩论——正方说风险，反方说合理，裁判 AI 做最终裁决。不是每条条款都需要开庭，只有争议条款才上审委会。"

---

### 3.4.4 三级火箭：预算感知调度器（Budget-Aware Scheduler）★

全量运行三个范式（Retrieve + Review + Discuss）固然准确率最高，但每次 6-10 次 LLM 调用在商业部署中成本不可忽视。本系统创新的**预算感知调度器**根据合同价值自动选择执行深度——这是工程可行性与算法先进性的最佳平衡点。

| 模式 | 执行范式 | 适用场景 | 触发条件 | 耗时 | API 成本 | 召回率 | 精准率 |
|------|:---:|---------|---------|:---:|:---:|:---:|:---:|
| **轻量模式** | 仅 Retrieve | 低价值合同（采购单 <5 万，内部试用协议）| 用户选择"快速筛查"或合同金额 <5 万 | ~3-5s | ~0.1 元/份 | 预期 ≥ 85% | 预期 ≥ 78% |
| **标准模式（默认）** | Retrieve + Review | 正式商业合同（销售、外包等）| 系统默认，覆盖赛题要求的大多数场景 | ~15-20s | ~0.5 元/份 | 预期 ≥ 90% | 预期 ≥ 88% |
| **深度模式** | Retrieve + Review + Discuss | 大额/高风险合同（>100 万，核心技术转让）| 用户选择"深度审核"或合同金额 >100 万 | ~25-30s | ~0.8 元/份 | 预期 ≥ 92% | 预期 ≥ 92% |

**路由决策规则：**

1. 用户在合同上传页选择审核模式（轻量/标准/深度），默认为标准
2. 若用户选择"智能推荐"，系统根据合同金额字段自动路由：< 5 万 → 轻量；5-100 万 → 标准；> 100 万 → 深度
3. 合同金额无法自动识别时，统一走标准模式

**答辩话术（Budget-Aware Scheduler）：**

> 评委问："你们把大模型调用这么多次，响应时间和 API 费用怎么平衡？"
>
> 答：我们设计了一套**预算感知调度器（Budget-Aware Scheduler）**。我们清醒地认识到，全量运行三个范式在商业上是不可行的。因此，我们没有将三者简单堆砌，而是构建了级联触发逻辑：
> - **必选层：** Retrieve（多路召回）是必选项，确保原始风险不漏网
> - **核心层：** Review（多角色复核）是默认模式，模拟真实法务审批流，拦截大部分误报
> - **按需层：** Discuss（辩论裁决）是稀缺资源，只对 Review 中置信度 < 0.7 或 Agent 意见冲突的 Top 3 高风险条款触发
>
> 这种设计，既保证了核心风险的严谨性，又避免了在无争议条款上浪费算力，是工程可行性（低成本）与算法先进性（高准确率）的最佳平衡点。

**Corex 效果预期表（三种模式对比）：**

| 对比维度 | 单一 LLM | 轻量模式（仅 Retrieve）| 标准模式（+ Review）| 深度模式（+ Discuss）|
|---------|:------:|:------:|:------:|:------:|
| 风险召回率 | ~78% | 预期 ≥ 85% | 预期 ≥ 90% | **预期 ≥ 92%** |
| 误报率 | ~20% | 预期 ~18% | 预期 ~10% | **预期 ~8%** |
| 模糊条款判断准确率 | ~65% | 预期 ~75% | 预期 ~82% | **预期 ~85%** |
| LLM 调用次数/份 | 2-3 次 | 3-4 次 | 6-7 次 | 8-10 次 |
| 审核时间 | 8-12s | 3-5s | 15-20s | 25-30s |
| API 成本/份 | ~0.15 元 | ~0.1 元 | ~0.5 元 | ~0.8 元 |
| 学术支撑 | 无 | RAG 论文 | 多 Agent 论文 | **COLM'24 Corex 论文** |

**LLM 调用一致性保障：** 所有审核相关的 LLM 调用（分类、要素抽取、规则辅助判断、RAG 增强审核、Corex 各 Agent）统一设置 temperature=0.1（确保输出稳定可复现），System Prompt 在代码中冻结为常量（不随用户输入动态拼接，避免 Prompt 注入风险），输出格式通过 Pydantic 严格校验（JSON Schema 约束 + 类型检查 + 必填字段检查）。此三项措施确保同一份合同在不同时间审核的结果一致，避免因 LLM 随机性导致审核结论漂移。

**Corex 工程实现关键规则：**

**（1）结果融合与冲突裁决：** 多 Agent 对同一风险给出矛盾判断时，按以下优先级裁决：（a）置信度 > 0.8 的 Agent 意见优先；（b）同置信度区间时，取多数 Agent 一致意见；（c）平局时法务 Agent 意见为最终裁定（法务 Agent 拥有对法律风险判断的最高权重）；（d）如果法务 Agent 置信度 < 0.5，则该风险标记为"需人工复核"。

**（2）容错降级行为：** 当任一 Agent 调用超时（15s 硬超时）：跳过该 Agent，剩余 Agent 结果正常融合。超时 Agent 的缺失在最终报告中标注（如"合规 Agent 未完成，本次审核不含合规视角"）。连续 2 个 Agent 超时：自动降级为单 LLM + RAG 审核（轻量模式），返回已有检测结果并提示用户"Corex 部分 Agent 不可用，已降级为轻量审核"。

**（3）Retrieve 模式初赛简化：** 原论文的"多源检索"在初赛阶段简化为双源——Agent 1 检索《民法典》条文 + Agent 2 检索内置风险案例库。完整四源方案（增加行业最佳实践 + 裁判文书网类案）列入 V2.0 规划。如需更多知识源，通过 Prompt 中的知识背景说明替代。

Corex 的完整工程实现（Orchestrator 调度、容错降级、并行策略）详见附录 B 的技术风险应对。

### 3.5 误报抑制与精准率保障机制

赛题要求风险识别"召回率与精准度均不低于 75%"。召回率通过三层递进审核保障，精准率通过以下五重误报抑制机制保障：

**第一重 — 规则白名单过滤：** 规则引擎不仅检测风险，同时维护一份"安全表述白名单"。例如：违约金条款中出现"不超过合同金额的 10%"的表述，虽然触发了 R01（违约金）规则，但白名单判定为安全表述，自动过滤不进入风险列表。白名单当前约 40 条规则，覆盖最常见的误报模式。白名单支持企业法务通过管理后台新增条目（如添加行业特定安全表述），新增条目即时生效。被白名单过滤的误报记录自动存入反例匹配库（见第二重），用于后续 RAG 反例匹配。

**第二重 — RAG 反例匹配：** 在 LLM 判定某条款为风险时，同时检索 ChromaDB 中的"被用户确认误报"的历史案例（通过审核经验库）。若当前条款与历史误报案例的语义相似度 > 0.85，系统降低该标注的置信度并添加"与历史误报相似，建议复核"的标记。

**第三重 — 风险判定的合理性验证（Self-QA 增强）：** Self-QA Agent 不只验证"原文是否存在该条款"（存在性验证），同时执行合理性验证——具体 Prompt 指令为："对于每条风险标注，请回答三个问题：(a) 原文是否确实存在被标注的条款？(b) 标注的风险等级是否合理？风险等级判别标准——高风险：潜在损失超过 10 万元或违反强制性法律规定；中风险：可能引发合同争议或商业风险；低风险：表述不够精确但不影响合同效力。如果原文表述不符合对应等级标准，请降级。(c) 是否存在对同一条款的多重风险标注（重复标注），如有请合并。" 量化风险等级标准同时嵌入法务、合规、财务三个 Agent 的 System Prompt，确保多 Agent 判断基准一致。

**第四重 — 置信度三阶过滤：** 所有 AI 标注输出前经过三阶置信度过滤：confidence < 0.5 → 直接丢弃不展示；0.5 ≤ confidence < 0.7 → 展示但标注"低置信度，建议人工复核"，risk_level 自动降一级；confidence ≥ 0.7 → 正常展示。过滤后的最终展示结果精准率显著提升。

**第五重 — Corex 多 Agent 交叉否决：** Review 模式中，若任一 Agent 对该风险标注给出 confidence < 0.4 且明确给出"不构成风险"的理由，该标注进入"争议状态"——不直接丢弃，但在报告中以特殊样式标注"Agent X 认为不构成风险"，供用户最终裁决。

**精准率预期：** 轻量模式（仅 Retrieve，前四重过滤）精准率预期 ≥ 78%；标准模式（Retrieve + Review + 第五重交叉否决）精准率预期 ≥ 88%；深度模式（全量 Retrieve + Review + Discuss）精准率预期 ≥ 92%。均远超赛题 ≥ 75% 的要求。消融实验中精准率的逐重提升数据见附录 D.3。

### 3.6 审核结果的数字化沉淀

每条风险审核记录以结构化形式存入 `audit_records` 表，包含：风险类型（R01-R12）、风险等级、原文片段 + 位置、判定理由 + 法条引用、修改建议、检测方法（rule/rag/corex）、Corex 多 Agent 协作日志（完整记录每个 Agent 的判断和置信度）。这使得每条 AI 判断都是可追溯、可审计的——评委和用户可以清楚看到"这条风险经过了哪些 Agent 的审核、每个 Agent 怎么说、置信度多少"。完整表结构见第九章。

### 3.7 预期指标与验证方法

| 指标 | 赛题要求 | 本系统预期值 | 验证方法 |
|------|:-----:|:--------:|---------|
| 风险识别召回率 | ≥ 75% | 预期 ≥ 85%（轻量）/ 预期 ≥ 90%（标准）/ 预期 ≥ 92%（深度）| 在 80 份标注测试集上计算检出风险 / 实际风险总数 |
| 风险类型覆盖 | ≥ 10 类 | 12 类（R01-R12）| 枚举检查 |
| 风险误报率 | — | 预期 ~18%（轻量）/ 预期 ~10%（标准）/ 预期 ~8%（深度）| 在标注测试集上统计误报数 / 总检出数 |
| Corex 终审一致性（深度模式）| — | 预期 ≥ 90% | 同一合同审核 3 次，终审结论一致的比例 |
| 审核耗时（预算感知调度）| — | 轻量 3-5s / 标准 15-20s / 深度 25-30s | 三类合同各测 10 次取 P95 |
| API 成本 | — | 轻量 ~0.1 元/份 / 标准 ~0.5 元/份 / 深度 ~0.8 元/份 | 统计 DeepSeek-V3 Token 用量 |

> **注：** 以上三项指标对应 3.4.4 节的三级火箭调度器——轻量模式仅跑 Retrieve，标准模式跑 Retrieve + Review（系统默认），深度模式全量跑 Retrieve + Review + Discuss。

完整实验方案、测试集构建计划、消融实验设计见附录 D。

### 3.8 数据获取与冷启动策略

AI 系统的核心瓶颈往往不在算法，而在数据。本系统依赖的数据类型多样——训练数据、测试数据、知识库数据、模板数据——每类数据的获取路径和确定性不同。以下从"确定性分层"角度逐项说明。

#### 3.8.1 确定性可获取（不依赖外部，团队自主完成）

**第一层：法律法规条文（构建 RAG 知识库的法律法规库，~200 条）**

来源：《民法典》合同编全文由全国人大官网公开，编委会发布的司法解释同理。团队成员手动提取关键条文（约 50-80 条），一人半天可完成。文本以 Markdown 格式存入 `backend/ai/knowledge/laws/` 目录，ChromaDB 向量化后即可检索。

**第二层：风险案例库（构建 RAG 知识库的风险案例库，~80 条）**

来源：基于 12 类风险类型，人工编写典型案例——每条案例包括"合同片段 + 风险类型 + 等级 + 判定理由 + 参考法条 + 修改建议"。编写原则：（a）每类风险至少编写 5 条案例以覆盖不同表述变体；（b）优先从 CUAD 和 LeCaRD 公开数据集中选取真实条款片段，改写为案例格式；（c）同学 D 和 E 各负责 6 类风险 × 5 条 = 30 条，2 天内完成，第 3 天交叉审核。案例以 JSON 格式存入 `backend/ai/knowledge/cases/` 目录。

**第三层：标准条款模板（5 类合同 × ~30 条/类 = ~150 条）**

来源：结合公开合同模板库、搜索引擎检索的合同范本、以及 DeepSeek/ChatGPT 辅助生成模板草案，团队交叉审阅后定稿。同学 D 和 E 各负责 2-3 类合同模板，1-2 天完成初稿，第 3 天合并审阅。模板以 JSON 格式存入 `backend/ai/knowledge/templates/` 目录，包含条款文本、条款标题、`priority`（required/recommended/prohibited）和 `strictness` 属性。

**第四层：自建合同数据集（用于训练测试，100+80 份）**

用于 BERT 分类微调和要素抽取 Prompt 调优的 100 份模板合同：基于上述标准条款模板，通过模板参数随机组合（不同的金额、日期、双方名称、条款变体）自动生成。工具选用 Python + Jinja2 模板引擎，一人 1 天可生成 100 份。每份合同标注类型标签（5 分类）和关键要素字段（parties/amount/date 等），标注信息嵌入生成逻辑中。

**第五层：12 类风险检测规则**

来源：从《民法典》合同编条文反推（如第 585 条违约金规则直接转化为 R01）+ 公开合同审查清单参考。同学 D 和 E 共同梳理，半天可完成初版 12 条规则的触发条件定义。后续在实际合同上测试迭代。

#### 3.8.2 需企业配合获取（有不确定性，已规划降级）

**第六层：网新恒天脱敏合同样例**

用途：测试集的真实合同来源，验证系统在真实企业合同上的表现；训练数据的补充，覆盖模板合同无法模拟的真实表述。

获取方式：联系命题企业（网新恒天）申请脱敏合同样例。正当性论证——命题企业作为赛题发布方，提供脱敏样例有助于验证参赛方案对企业实际场景的适配性，同时脱敏处理（去除公司名称、金额等敏感信息）后不涉及商业机密。

**降级方案（若获取不到）：** 使用自建模板合同的变体版本作为测试集（通过条款随机排序、金额日期替换、同义词替换等方式增加变体多样性）。虽然丧失了真实合同的部分复杂性，但足以验证系统核心功能。这直接影响附录 D 中"80 份测试集"的构成——最坏情况下 80 份全部为自建模板变体。

#### 3.8.3 标注工作量评估与可行性论证

风险审核测试集（80 份）的手工标注是最大的工程瓶颈。以下是对工作量的现实评估：

| 环节 | 工作量 | 人力 | 时间 |
|------|-------|------|:---:|
| 80 份合同生成（模板变体）| 脚本自动化 + 人工抽查 | 1 人 | 1 天 |
| 每份合同标注风险条款（约 5-10 条/份）| 按 8 条/份 × 80 份 = 640 条标注 | 4 人各标 20 份 | 2 天 |
| 两两交叉校验 | 每人审阅另一个人的 20 份标注 | 4 人 | 1 天 |
| 不一致标注由第 5 人裁决 | 预计不一致率 < 15%，约 96 条争议 | 1 人（组长）| 0.5 天 |
| Cohen's Kappa 计算 + 标注规范修订 | 统计分析 | 1 人 | 0.5 天 |
| **合计** | | | **5 天** |

> **现实性评估：** 5 天标注工作量在 3 周开发周期中占比约 24%，可行但紧张。若时间不足，可将标注量缩减至 40 份（4 人各标 10 份，交叉校验后 2 天完成），剩余 40 份用数据增强填充（同义词替换、条款位置打乱）——此时测试集规模仍保持 80 份但人工标注占比降至 50%，其余为算法生成标注的自动评估集。

#### 3.8.4 知识库冷启动策略

RAG 知识库的"审核经验库"（存储用户确认的历史审核记录）在系统首次部署时为空——这是 RAG 系统的典型冷启动问题。

**冷启动方案：** （a）初始阶段，审核经验库为空时，RAG 检索权重完全落在风险案例库和法律法规库上——这两个库在开发阶段已预先构建，不依赖运行时积累。（b）系统上线后，每份经用户确认（"采纳"/"修正"/"仅参考"）的审核记录自动存入审核经验库，ChromaDB 增量索引。（c）预期积累约 20 份合同后（约 2-3 周使用量），审核经验库开始对检索结果产生实质性影响——此时语义相似的历史案例可直接匹配，审核效率和质量进入正向循环。

> **关键保证：** 即使审核经验库全程为空，系统依赖规则引擎 + 预置案例库 + 法律法规库的能力，仍然满足赛题所有强制指标。审核经验库是加分项而非必需项。

---

## <a id="4"></a>四、核心方案三：标准条款语义比对与缺失检测

### 4.1 问题分析

企业法务通常有一套标准合同模板，逐条比对"对方版本和我们的标准版本差在哪里"是审核中最耗时的工作。传统方案基于关键词匹配，容易出现"标题匹配但内容完全偏离"的漏判。

**技术挑战：** 不同合同的条款标题表述各异（"违约责任""违约条款""违约处理"），条款顺序可能重排，内容可能拆分合并。纯关键词匹配无法应对这种表述多样性。

### 4.2 语义级双层对齐

系统采用句子级 Embedding 做语义匹配，分两阶段对齐：

**第 1 步 — 结构对齐：** 基于条款标题进行粗糙匹配。具体规则：（a）关键词归一化——将合同中的条款标题与标准模板标题进行关键词提取和同义词映射。（b）同义词映射采用两级策略：P0 阶段硬编码 20-30 组核心条款同义词对（如"违约责任""违约条款""违约处理"→"违约责任"；"争议解决""纠纷处理""管辖条款"→"争议解决"；"保密义务""保密责任""保密条款"→"保密义务"等），覆盖最常见的表述变体；硬编码映射未覆盖的标题，直接降级为向量相似度匹配（跳过结构对齐，进入第 2 步语义对齐），以 embedding 相似度作为唯一匹配依据。（c）Jaccard 相似度——计算标题关键词集合的 Jaccard 系数，≥ 0.5 视为候选配对。（d）位置权重——合同条款顺序与标准模板顺序相近时加分（相邻 ±3 位），利用合同条款通常按约定俗成顺序排列的特性。此步骤将每条合同条款的候选匹配范围从全部 20-30 条标准条款缩小到 2-5 条，显著降低语义对齐的计算量。

**第 2 步 — 语义对齐：** 用 text2vec-large-chinese 将每个条款编码为 1024 维向量，计算与标准条款的余弦相似度。相似度 > 0.85 视为"已覆盖"；< 0.5 视为"缺失"；介于 0.5-0.85 之间的标记为"可能存在偏离，建议人工复核"。

### 4.3 缺失检测与补全建议

- **缺失检测：** 标准模板中存在但语义相似度 < 0.5 的条款，自动列入缺失清单。常见缺失项包括：不可抗力条款、保密期限条款、验收标准条款、数据保护条款。
- **偏离分析：** 存在对应条款但语义差异较大（0.5-0.85），生成并排对比视图，差异部分高亮标注。
- **补全建议：** 对缺失条款，从模板库调取标准文本，结合合同上下文送入 LLM 生成定制化的补全建议。不仅告诉用户"少了什么"，还直接给可用的文本。

### 4.4 条款模板的可配置化设计

赛题要求"支持加载企业标准条款模板（用户可配置）"，系统在以下三个维度实现深度可配置。系统预置 5 种合同类型的标准条款模板（采购、销售、保密协议、服务外包、劳动），其中采购合同和外包服务合同模板已适配网新恒天 IT 服务业务场景（嵌入 SLA 条款、数据安全条款、IP 归属条款、源代码托管条款等行业特有条款标准文本）：

**条款分级配置：** 每条标准条款标注三个属性——"强制必选"（如违约责任条款、争议解决条款，缺失直接标高风险）、"推荐可选"（如保密条款、知识产权条款，缺失标中风险）、"禁止出现"（如无限责任、单方任意解约权，若出现标高风险）。分级属性在 templates 表的 clauses JSON 中以 `priority: required/recommended/prohibited` 字段存储，默认可按企业需求调整。

**比对严格度配置：** 提供三档严格度——严格模式（向量相似度 > 0.90 才视为覆盖，适用于核心法务条款）；标准模式（> 0.75 视为覆盖，默认推荐）；宽松模式（> 0.60 视为覆盖，适用于对表述细节容忍度高的场景）。严格度在审核请求中由用户选择，影响缺失检测和偏离分析的阈值。

**模板版本管理：** 每次模板修改自动递增版本号（version 字段），历史版本保留不删除。已审核合同记录其审核时使用的模板版本号，用户浏览历史报告时可查看当时使用的模板内容。模板更新后，提供"使用新模板重新审核"的主动提示，由用户决定是否触发重审——避免自动重审导致的审核结果漂移，同时保留重审的灵活性。

本方案借鉴 PAKTON 中"Researcher Agent"的混合检索思路（BM25 + Dense Embedding），简化为更适合三周开发周期的纯向量方案。标准条款模板数据管理详见第九章 templates 表设计。

---

## <a id="5"></a>五、核心方案四：用户反馈闭环与审核报告生成

### 5.1 用户反馈闭环（P0）

法务审核 AI 结果后，可对每条风险执行三种操作：
- **确认正确：** 作为正例存入 feedback_log 表，增强对该判断模式的信心
- **标记错误：** 选择错误类型（误报/漏报/等级不准），提交修正意见
- **手动补充：** 添加 AI 未检测到的风险

反馈不立即生效（法律审核要求结果稳定可追溯），而是以结构化形式存入 feedback_log 表，积累 ≥ 50 条后由管理员导出，用于后续模型迭代（更新 Prompt 示例、调整规则阈值、补充风险词典、更新 ChromaDB 向量）。每次更新记录版本号，支持回滚。

> **设计说明：** 反馈数据的用途是"结构化存储，支持导出用于后续模型迭代"——P0 阶段不实现自动触发模型更新，而是提供管理后台的数据导出功能（CSV/JSON），由团队在下一轮模型优化中手动使用。这一定位避免了过度承诺实时自适应能力，同时保留了反馈闭环的完整性。

> **注：** 自定义审查规则、合同版本差异对比、企业知识库管理等进阶功能属于 P2 未来规划，详见第十三章。

### 5.2 审核报告生成（P0）

审核完成后自动生成六章结构化报告：

| 章节 | 内容 | 亮点 |
|------|------|------|
| 一、合同基本信息 | 编号、类型、双方、金额、日期 | 自动提取，无需手动填写 |
| 二、风险评估总览 | 风险热力图 + 高/中/低分布 + 综合评分（0-100）| 30 秒了解全局风险 |
| 三、风险条款逐项分析 | 每条风险附原文定位 + 等级 + 修改建议 + 审核路径 | AI 判断可追溯 |
| 四、条款完整性检查 | 缺失条款清单 + 偏离条款对比 + 补全建议 | 从诊断到治疗闭环 |
| 五、修改建议汇总 | 按优先级排列的操作清单 + 可直接复用的参考条款 | 可执行、可落地 |
| 六、附录 | 审核时间线 + 标准说明 + 审核日志 | 透明可审计 |

报告输出格式：HTML（在线预览）+ PDF（下载）+ Markdown（内部归档）。其中风险热力图将合同每一页的风险密度映射为颜色（绿 → 黄 → 橙 → 红），是 A24 在产品体验层面区别于命令行/纯文本输出的核心优势。

---

## <a id="6"></a>六、系统架构总览

### 6.1 四层技术架构

系统采用经典四层架构：

**第一层 — 前端展示层（Vue 3 + Element Plus）：** 合同上传、审核看板、风险详情、报告展示、反馈标注五个核心页面。ECharts 渲染风险热力图。合同原文展示采用 pdf.js，P0 底线为点击页码跳转 + 侧栏文本对照展示，精确字符级高亮定位作为 P1 冲刺项。

**第二层 — 业务后端层（Python FastAPI）：** 文件服务、合同管理、审核调度、模板管理、统计服务。JWT 认证 + RBAC 权限控制。

**第三层 — AI 核心引擎层（同进程 Python 模块）：** 包含审核流水线（解析 → 分类 → 抽取 → 检测 → 报告）、Corex 多 Agent 协作引擎、双模型协同（BERT + LLM）和知识库 RAG。AI 模块作为普通 Python 包被业务层直接调用，不走 HTTP，省去序列化开销。

**第四层 — 数据持久层：** MySQL 8.0（结构化业务数据）+ ChromaDB（向量化知识库）+ 本地文件系统（原始合同 + 导出报告）。

（完整架构全景图见答辩 PPT）

### 6.2 技术栈选型

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 前端 | Vue 3 + Element Plus + ECharts + pdf.js | 响应式开发、中文文档完善、图表组件丰富 |
| 业务后端 | Python FastAPI + SQLAlchemy + PyMySQL | 赛题指定技术栈；与 AI 逻辑同语言，无需服务间通信 |
| 大模型 | DeepSeek-V3（主力）+ Qwen-Max（备选）| 性价比高、中文法律文本理解好 |
| 小模型 | BERT-base-chinese（微调分类）| 本地推理，速度快零成本 |
| 向量数据库 | ChromaDB | 轻量部署，Docker 一行命令启动 |
| OCR | PaddleOCR | 中文识别效果好，开源免费 |
| 文档解析 | python-docx + pdfplumber + PyPDF2 | Python 生态完善 |
| 部署 | Docker Compose（FastAPI + MySQL + ChromaDB）| 一键启动，评委 15 分钟可复现 |

### 6.3 架构关键决策：全栈 Python

赛题明确要求后端使用"Python + FastAPI/Django"，这是基础合规要求。全 Python 方案还带来额外好处：AI 逻辑（文档解析、分类、审核）和业务逻辑（合同管理、用户认证）在同一个 Python 进程中，直接用函数调用代替 HTTP 通信，省掉双服务之间的序列化开销和网络延迟。对 5 人团队 3 周开发的场景，统一技术栈意味着全部人只需要配置 Python 环境，代码审查无障碍，Bug 排查不需要跨越语言边界。

**5 人分工：**

| 角色 | 负责范围 | 技术栈 |
|------|---------|--------|
| 同学 A（前端）| 合同上传、列表、详情页 | Vue 3 + Element Plus |
| 同学 B（前端）| 审核结果、报告展示、风险热力图 | Vue 3 + Element Plus + ECharts |
| 同学 C（业务后端）| FastAPI 路由、数据库、认证、审核调度 | Python + FastAPI + SQLAlchemy |
| 同学 D（AI 引擎）| 文档解析、分类、要素抽取、Corex | Python + BERT + LLM |
| 同学 E（AI 引擎）| RAG 知识库、条款比对、报告生成 | Python + ChromaDB + Dify |

### 6.4 数据安全设计原则

合同是企业的核心商业机密。系统的设计原则是"数据本地处理，支持纯规则降级运行"——文档解析、规则引擎、BERT 推理全部本地完成；云端 LLM 调用仅传脱敏后的条款文本（公司名称替换为"甲方/乙方"，具体金额替换为"XX 万元"），不传整份合同。在无网络或用户拒绝调用 LLM 的场景下，系统可降级为纯规则引擎审核模式，功能受限但仍能检出 12 类基础风险。完整安全方案见第十二章。

### 6.5 企业集成接口预留

为适配网新恒天等企业的真实 IT 环境，系统在设计层面预留三项集成接口（V2.0 实现）：

- **企业 OA/合同管理系统对接：** 预留 RESTful API 供外部系统推送合同文件并获取审核结果，接口格式与现有 `/api/contracts/upload` 和 `/api/contracts/{id}/audit-result` 一致，仅增加 API Key 认证头。
- **单点登录（SSO）集成点：** 用户认证模块通过 FastAPI Depends 注入，预留 OAuth2/OIDC 接口，可对接企业统一身份认证系统（如 LDAP、CAS），无需修改业务逻辑代码。
- **企业知识库对接：** ChromaDB 向量检索层设计为可替换的 Provider 接口，V2.0 可对接企业现有知识库系统，仅需实现 `search(query) -> List[Document]` 接口即可。

---

## <a id="7"></a>七、创新亮点总结

A24 的亮点策略是"一硬一软"双核心 + 配套支撑——集中火力打透两个记忆点，其他能力作为配套。

### 7.1 硬核技术创新：Corex 递进式纵深防御 + 三级火箭调度 ★

A24 的核心技术壁垒包含两个层面——不是简单的"多 Agent 审核"，而是**递进式纵深防御管线**（Retrieve → Review → Discuss）配合**预算感知调度器**（轻量/标准/深度三级进档），两者协同实现"低成本高召回 + 高成本高精准"的动态平衡。

**为什么不是"多轮 Prompt 调用"？** Corex 与 Self-Reflection、普通多 Agent 的本质区别在于：三个范式是递进管线而非并列选项——Retrieve 先做多源召回（"广撒网"），Review 再做多角色复核（"过三关"），Discuss 仅对争议条款做辩论裁决（"打官司"）。Self-Reflection 是"自己质疑自己"，Corex 是"三个不同角色交叉审核，意见不统一时再上审委会"，后者在法律场景下更可靠。技术细节和横向对比表详见第三章 3.4 节。

**核心记忆点（答辩用）：** "就像法院的模拟合议庭——先分头查资料（Retrieve），再三个 AI 律师（法务/合规/财务）分别过审（Review），意见不统一时才启动红蓝辩论（Discuss），裁判 AI 一锤定音。并且不是所有合同都走全流程——小额采购单只查资料不演审批，只有百万级大单才上审委会。"

**关键技术指标（预期值）：** 召回率 78% → 92%，误报率 20% → 8%，精准率预期 ≥ 92%（详见 3.7 节和附录 D）。

### 7.2 产品体验创新：风险热力图 + 原文一键跳转

"上传合同 → 30 秒出热力图 → 红色区域一眼看到风险在哪 → 点击风险条目自动跳转原文高亮定位"——这是 A24 在产品体验层面区别于命令行/纯文本输出的核心优势。

热力图将合同每一页的风险密度映射为颜色（绿 → 黄 → 橙 → 红），配合风险一键跳转原文定位（点击任一条审核结果，合同原文面板自动滚动到对应条款并高亮显示）和 Corex Agent 协作日志的逐条追溯，形成"看到风险 → 一键跳转原文 → 理解 AI 判断逻辑"的三步交互闭环。

**pdf.js 降级方案：** P0 底线为点击页码跳转 + 侧栏文本展示，精确字符级高亮定位作为 P1 冲刺项——详见第六章架构设计。

### 7.3 配套技术支撑

| 能力 | 作用 | 对应章节 |
|------|------|---------|
| 双模型协同降本 | BERT 本地分类覆盖 90% 合同，API 调用量降低 70% | 第二章 |
| 五重精准率保障 | 白名单 + 反例匹配 + Self-QA 合理性验证 + 三阶过滤 + 交叉否决 | 第三章 |
| 语义级条款对齐 | Embedding 替代关键词匹配，三档比对严格度可配 | 第四章 |
| 条款补全建议 | 不仅告诉"少了什么"，还直接给可用标准文本 | 第四章 |

### 7.4 赛题鼓励创新方向落地

赛题第 7 节明确鼓励三个创新方向，本系统的实施方案：

**多轮对话式合同修改：** 初赛阶段在审核结果页嵌入对话面板，支持自然语言指令修改条款（如"将违约金改为合同金额的 20%"），调用 LLM + Function Calling 生成修改文本并展示修改前后对比。初赛可演示单条款修改 demo。

**知识图谱辅助跨条款关联风险：** 初赛阶段实现预定义关联规则检查（如"违约金条款金额不应超过合同总金额"），答辩 PPT 展示知识图谱概念设计；完整实体关系抽取 + Neo4j 可视化列入 V2.0。

**印章/签名区域定位：** 初赛阶段在文档解析模块集成 PaddleOCR 印章检测 API，实现"是否有印章"的二元判断；完整位置合规分析列入 V2.0。

（以上三个方向的详细 MVP 方案见附录 C。）

### 7.5 答辩叙事建议（三个场景）

**场景一（给评委看 — 技术深度）：** "一份 300 万的 IT 技术外包合同，系统自动识别合同金额后进入深度模式。首先 Retrieve 阶段，两个 Agent 从民法典条文和风险案例库中捞出 20 条疑似风险。然后 Review 阶段，法务 Agent 确认了 15 条，合规 Agent 补充了 2 条遗漏的数据合规条款，财务 Agent 对其中 3 条付款条款的等级判断与法务产生分歧。分歧检测器触发 Discuss，红蓝双方就'SLA 罚则金额是否合理'辩论 3 轮，裁判给出了'保留罚则但设上限'的折中裁决。整个流程 28 秒完成，最终报告标注为'经 Corex 深度审核（Retrieve + Review + Discuss）'。这个审核质量和可解释性，单一 LLM 和常规 RAG 方案都做不到。"

**场景二（给企业看 — 商业价值）：** "网新恒天的法务团队每年审核数千份 IT 服务合同。按每份平均 2 小时算，AI 辅助下缩短到 15 分钟（人工复核），相当于节省了 80% 以上的人力投入——更重要的是，所有审核知识沉淀在系统中，不再依赖个别法务的个人经验。"

**场景三（给用户看 — 产品体验）：** "网新恒天的李律师休年假的第二天，同事接手了一份紧急的技术服务合同。打开系统搜索上个月类似合同的审核记录，AI 的标注、李律师的修改意见、Corex 的协作日志、最终决策，一目了然。5 分钟后审核意见发出——客户甚至没发现李律师不在。"

---

## <a id="8"></a>八、开发计划与里程碑

### 8.1 总体时间规划（三周，含缓冲）

**第一阶段：线下集中设计（7.15 - 7.20，1 周）**

全体成员集中进行系统设计工作。完成系统原型设计、需求文档定稿、技术方案评审。后端搭建 FastAPI 骨架并完成用户注册登录前后端联调；前端完成 Vue 3 项目创建、路由配置、Element Plus 集成及首页布局。AI 团队在 Dify 平台搭建带 RAG 的 LLM 工作流并验证 API 调用可行性。**本阶段最后一天（7.20）预留为联调缓冲日。**

**第二阶段：线上开发第一周（7.22 - 7.27，1 周）**

完成 P0 功能的前后端联调。前端完成合同上传页、列表页、详情页、审核结果展示页。后端完成合同 CRUD 接口、审核记录接口、模板管理接口和 AI 服务调用链路。AI 团队优先跑通纯 LLM 分类管道（不依赖 BERT），并行推进文档解析器和要素抽取。数据库表结构全部建完。**进度检查点（7.24）：若分类管道未跑通，7.25-7.26 集中攻关；BERT 训练若 7.25 仍未达标，直接切换纯 LLM 方案。**

**第三阶段：线上开发第二周（7.29 - 8.3，1 周）**

冲刺构建可演示的完整 Demo。AI 团队按优先级排序：规则引擎 + RAG 语义审核（P0，7.29-7.30）→ 条款比对 + 报告生成（P0，7.31-8.1）→ Corex Review 多 Agent 交叉验证（P1，8.2-8.3，P0 全部完成后再启动）。后端完成审核调度 API 和 PDF 导出。前端完成审核报告页面和反馈标注。**8.3 全天为集成联调 + Bug 修复缓冲日，不做新功能开发。**

### 8.2 分阶段里程碑与缓冲

| 阶段 | 时间 | 关键交付物 | 缓冲日 | 验证方式 |
|------|------|-----------|:---:|---------|
| 线下设计周 | 7.15-7.20 | 设计文档定稿；登录注册调通；Dify 工作流可 API 调用 | 7.20 | 注册登录可走通 |
| 线上第一周 | 7.22-7.27 | P0：分类+抽取+文档解析；全数据流跑通 | 7.26-7.27 | 上传 → 分类 → 展示要素 → 入库 |
| 线上第二周 | 7.29-8.3 | P0：审核+比对+报告；P1：Corex Review | 8.3 | 上传 → 审核 → 报告下载全流程 |

### 8.3 Dify 快速原型与自研迁移方案

**Dify 在整个项目中的统一定位：第一周原型验证工具 → 第二周起自研为主 → Dify 作为紧急兜底。**

第一周（7.15-7.20）：AI 团队在 Dify 平台创建"合同风险初审"工作流（知识库节点 + LLM 节点 → 输出风险 JSON），快速验证合同审核 AI 管道的可行性。FastAPI 后端通过 DifyClient（httpx）调用工作流 API，实现前端可演示的审核效果。

第二周起（7.22-8.3）：将 Dify 工作流中的逻辑逐步迁移到自建 Python AI 服务（`backend/ai/` 模块），包括规则引擎、RAG 检索、LLM 调用、Corex 编排。迁移完成后，自研引擎作为主方案，Dify 工作流仅作为备用兜底——若自研引擎抛出未捕获异常，自动回退到 Dify 工作流，确保演示不中断。切换对前端透明。

### 8.4 MVP 降级路径（关键）

本系统的 MVP 设计遵循"底线思维"——每个模块都有明确的降级路径，确保任一环节卡壳都不会阻塞整体演示：

| 模块 | 理想方案 | 降级方案 | 切换条件 | 降级后影响 |
|------|---------|---------|---------|----------|
| 合同分类 | BERT + LLM 双模型 | 纯 LLM（DeepSeek-V3 Few-shot）| BERT 训练 7.27 前未达 85% 准确率 | 每份 +0.03 元成本，准确率 ~85%（仍满足赛题） |
| 风险审核 | 规则 + RAG + Corex Review（P1 冲刺）| 规则引擎 + RAG + 单 LLM 审核（P0 底线）| Corex 开发 8.2 前未完成 | 召回率从预期 ~92% 降至预期 ~85%（仍远超赛题要求的 ≥ 75%）|
| LLM API 调用 | DeepSeek-V3 | Dify 工作流 API（已预搭建）| API Key 故障或网络问题 | 审核效果基本一致 |
| 向量检索 | ChromaDB 语义检索 | 关键词检索兜底（jieba + TF-IDF）| ChromaDB 启动失败 | 检索精度下降，规则引擎仍可覆盖基础风险 |

**P0 最低交付底线（无论如何必须跑通的演示闭环）：**
1. 合同上传 + 文档解析 + OCR
2. LLM 分类 + 要素抽取（不依赖 BERT）
3. 规则引擎 + RAG 语义审核（不依赖 Corex）
4. 标准条款比对
5. 审核报告生成 + ECharts 风险热力图
6. Web 可视化界面 + 反馈标注

以上 6 项构成从"上传 → 分类 → 审核 → 报告"的完整演示闭环，满足赛题全部强制要求。Corex Review 作为 P1 加分项，在 P0 全部跑通后冲刺；Corex Discuss/Retrieve 列为 P2，答辩 PPT 标注"已完成设计、开发中"。

---

# 第二部分：工程实现篇

---

## <a id="9"></a>九、数据库设计

### 9.1 存储架构总览

系统采用"MySQL + ChromaDB + 文件系统"三层存储：

| 存储层 | 技术 | 存储内容 | 特点 |
|--------|------|---------|------|
| MySQL 8.0 | 关系型数据库 | 用户、合同、审核记录、报告、模板、反馈日志 | 结构化、事务支持、关联查询 |
| ChromaDB | 向量数据库 | 风险案例向量、标准条款向量、法律法规向量、审核经验向量 | 语义相似度检索 |
| 文件系统 | 本地磁盘 | 原始合同文件、上传图片、导出报告 | 大文件、二进制数据 |

### 9.2 核心表设计（8 张表）

**表 1：users（用户表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 用户 ID |
| username | VARCHAR(50) UNIQUE | 用户名 |
| password_hash | VARCHAR(255) | bcrypt 哈希 |
| role | VARCHAR(20) | 基础角色（P0 仅区分 admin/user，完整 RBAC 为 V2.0） |
| is_active | BOOLEAN | 账号启用状态 |

**表 2：contracts（合同主表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 合同 ID |
| user_id | BIGINT FK | 上传用户 |
| file_name | VARCHAR(500) | 原始文件名 |
| contract_type | VARCHAR(50) | 分类结果 |
| type_confidence | FLOAT | 分类置信度 |
| parsed_text | LONGTEXT | 解析全文 |
| extracted_elements | JSON | 要素抽取结果（JSON 设计理由：要素字段随合同类型动态变化，如采购合同有"付款条件"、劳动合有"试用期"，JSON 灵活适应异构结构） |
| status | VARCHAR(20) | uploaded / parsing / auditing / completed |
| audit_mode | VARCHAR(10) | fast / precise |
| template_version | INT | 审核时使用的模板版本号（用于审核追溯） |

**表 3：audit_records（风险审核记录）** — 核心业务表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 记录 ID |
| contract_id | BIGINT FK | 关联合同 |
| audit_batch | VARCHAR(36) | 审核批次号（UUID），同一合同多次审核按批次区分 |
| risk_type | VARCHAR(50) | R01-R12 |
| risk_level | VARCHAR(10) | high / medium / low |
| clause_text | TEXT | 被标记原文 |
| clause_position | JSON | 文档位置（JSON 设计理由：不同格式合同的位置描述异构——PDF 用页码、行号，DOCX 用段落序号，JSON 统一适配） |
| reason | TEXT | 判定理由（含法条引用）|
| suggestion | TEXT | 修改建议 |
| detection_method | VARCHAR(30) | rule / rag / corex_review / corex_discuss / corex_retrieve |
| confidence | FLOAT | 置信度 0-1 |
| corex_agent_log | JSON | 各 Agent 协作日志（JSON 设计理由：不同范式 Agent 数量不同——Review 5 个、Discuss 3 个、Retrieve 2 个，JSON 灵活适配异构日志结构） |
| feedback_status | VARCHAR(20) | pending / confirmed / corrected / disputed |

**表 4：audit_reports（审核报告）** — 与 audit_batch 一对一，同一合同可有多份报告

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 报告 ID |
| contract_id | BIGINT FK | 关联合同 |
| audit_batch | VARCHAR(36) UNIQUE | 关联审核批次（每次审核生成新报告，不覆盖历史） |
| report_html | LONGTEXT | HTML 报告 |
| risk_score | INT | 综合风险评分 0-100 |
| high_risk_count | INT | 高风险数 |
| mid_risk_count | INT | 中风险数 |
| low_risk_count | INT | 低风险数 |
| risk_heatmap_data | JSON | 热力图数据 |
| missing_clauses | JSON | 缺失条款列表 |
| created_at | DATETIME | 报告生成时间 |

**表 5：templates（标准条款模板）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 模板 ID |
| name | VARCHAR(200) | 模板名称 |
| contract_type | VARCHAR(50) | 适用合同类型 |
| clauses | JSON | 条款列表 [{title, content, priority: required/recommended/prohibited}] |
| is_builtin | BOOLEAN | 是否系统内置 |
| version | INT | 版本号 |
| previous_version_id | BIGINT | 上一版本 ID（版本链追溯） |

**表 6：feedback_log（用户反馈日志）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 日志 ID |
| record_id | BIGINT FK | 关联审核记录 |
| user_id | BIGINT FK | 反馈用户 |
| action_type | VARCHAR(20) | confirm / correct / miss / add / dispute |
| original_risk | JSON | AI 原始判断 |
| corrected_risk | JSON | 用户修正后 |
| comment | TEXT | 用户备注 |

**表 7：custom_rules（自定义审查规则）** — V2.0 规划，预留表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 规则 ID |
| name | VARCHAR(200) | 规则名称 |
| rule_type | VARCHAR(50) | 检查字段（penalty_ratio / contract_amount / court_location 等） |
| condition | JSON | 条件定义 {operator, threshold} |
| risk_level | VARCHAR(10) | 触发后的风险等级 |
| contract_type | VARCHAR(50) | 适用合同类型（NULL = 全部） |
| is_active | BOOLEAN | 启用状态 |
| created_by | BIGINT FK | 创建人 |

**表 8：knowledge_base（知识库元数据）** — V2.0 规划，预留表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 知识条目 ID |
| title | VARCHAR(500) | 条目标题 |
| content | TEXT | 原始文本 |
| source_type | VARCHAR(50) | law / case / template / custom |
| tags | JSON | 标签 |
| chroma_id | VARCHAR(100) | ChromaDB 中的向量 ID |
| uploaded_by | BIGINT FK | 上传人 |
| created_at | DATETIME | 创建时间 |

完整建表 SQL（含索引、外键、预置数据）见项目仓库 `backend/schema.sql`。

### 9.3 ChromaDB 向量数据库设计

| Collection | 内容 | 预置量 | 向量维度 | 检索参数 |
|:---|------|:---:|:---:|:---|
| risk_cases | 12 类风险典型案例 | ~80 条 | 1024 | Top-K=5, 阈值=0.70 |
| standard_clauses | 5 种合同标准条款 | ~150 条 | 1024 | Top-K=5, 阈值=0.70 |
| laws | 《民法典》合同编 + 司法解释 | ~200 条 | 1024 | Top-K=5, 阈值=0.70 |
| audit_history | 用户确认的历史审核记录 | 运行时积累 | 1024 | Top-K=5, 阈值=0.70 |

首次启动时自动从 `backend/ai/knowledge/` JSON 文件编码向量并写入 ChromaDB，编码耗时约 2-5 分钟。

### 9.4 数据安全等级

| 等级 | 内容 | 存储 | 加密 |
|:---:|------|------|------|
| L3 高敏 | 合同原文、用户密码 | MySQL（密码 bcrypt）| AES-256（合同可选）+ TLS 传输 |
| L2 中敏 | 审核记录、要素摘要 | MySQL | TLS 传输 |
| L1 低敏 | 模板、统计报表 | MySQL | TLS 传输 |

---

## <a id="10"></a>十、API 接口设计

### 10.1 接口总览

**认证：**
- `POST /api/auth/register` — 用户注册
- `POST /api/auth/login` — 用户登录（返回 JWT Token，24h 有效）

**合同管理：**
- `POST /api/contracts/upload` — 上传合同（multipart，支持批量）
- `GET /api/contracts` — 合同列表（分页、类型/状态筛选、搜索）
- `GET /api/contracts/{id}` — 合同详情（含要素、段落结构）
- `DELETE /api/contracts/{id}` — 删除合同（admin 权限）

**审核：**
- `POST /api/contracts/{id}/audit` — 触发审核（body: {mode: "fast"|"precise"}，异步执行）
- `GET /api/contracts/{id}/audit-status` — 审核进度查询
- `GET /api/contracts/{id}/audit-result` — 审核结果（含全部 risk 列表 + Corex 日志）

**报告：**
- `GET /api/contracts/{id}/report` — 获取报告（HTML）
- `GET /api/contracts/{id}/report/pdf` — 下载报告（PDF）
- `GET /api/contracts/{id}/heatmap` — 风险热力图数据

**反馈：**
- `POST /api/feedback` — 提交反馈标注
- `GET /api/feedback/stats` — 反馈统计

**模板：**
- `GET /api/templates` — 模板列表
- `POST /api/templates` — 创建/更新模板

### 10.2 核心接口示例

**触发审核：**
```
POST /api/contracts/42/audit
Body: {"mode": "precise"}

Response: {
  "contract_id": 42,
  "status": "auditing",
  "audit_mode": "precise",
  "pipeline_stage": "classification",
  "estimated_time": 25
}
```

**审核结果（精简版）：**
```
GET /api/contracts/42/audit-result

Response: {
  "contract_type": "采购合同",
  "type_confidence": 0.94,
  "audit_mode": "precise",
  "elements": {
    "party_a": {"name": "杭州科技有限公司", "role": "买方"},
    "party_b": {"name": "上海软件有限公司", "role": "卖方"},
    "amount": {"value": 500000, "currency": "CNY"}
  },
  "risks": [
    {
      "risk_type": "R01", "risk_level": "high",
      "clause_text": "任何一方违约需支付合同金额30%作为违约金",
      "reason": "违约金比例30%超过法定上限...",
      "detection_method": "corex_review",
      "confidence": 0.94,
      "corex_agents": ["legal✓", "compliance✓", "finance✓", "self_qa✓"]
    }
  ],
  "missing_clauses": ["不可抗力条款", "保密期限条款"],
  "risk_score": 72
}
```

完整 API 文档（含请求/响应 JSON Schema）由 FastAPI 自动生成 Swagger 页面（`/docs`），开发期间可实时查看和测试。

### 10.3 边界场景处理

系统在以下边界和异常场景下有明确的处理策略：

| 边界场景 | 处理策略 | 用户提示 |
|---------|---------|---------|
| 合同内容为空或无法解析 | 前端上传时校验文件大小 > 0；后端解析失败返回错误码 PARSING_ERROR | "文件解析失败（文件可能为空或格式不支持），请检查文件后重试" |
| 合同分类置信度 < 0.5 | 默认归类为"其他合同"类型，不阻塞后续流程；审核报告中标注"类型识别置信度低，建议人工确认" | "合同类型自动识别为'其他合同'（置信度较低），请在详情页手动选择类型以获得更精准的审核" |
| 审核未检出任何风险 | 审核报告显示"本次审核未发现风险条款"，同时标注"AI 审核无法替代专业法律判断，建议人工复核"——避免用户误以为"零风险 = 绝对安全" | "本次审核未发现风险条款。请注意：AI 审核结果仅供参考，建议进行人工复核" |
| 超大合同（> 200 页）超出 LLM 上下文 | 按章切分合同，分批送入 LLM 审核，每章审核结果汇总后统一生成报告。切分边界以"第X章"或空行分隔符为准 | "检测到合同篇幅较长，系统将分章节审核（共 X 章），审核时间可能延长" |
| LLM API 全部不可用 | 降级为纯规则引擎模式，仅执行 12 类规则检测，RAG 和 Corex 暂不可用。审核报告中标注"快速检测模式（规则引擎），建议在网络恢复后重新进行完整审核" | "云端 AI 服务暂时不可用，系统已切换为本地快速检测模式。检测范围有限，建议稍后重新进行完整审核" |
| Dify 工作流与自研引擎的切换 | 自研引擎优先（第二周起）；当自研引擎抛出未捕获异常时，自动回退到第一周搭建的 Dify 工作流（作为兜底方案）。第一周 Dify 验证可行性 → 第二周自研为主 → Dify 保留为紧急兜底。切换对用户透明，后端日志记录切换原因 | 无感知切换，前端仅展示审核状态，不区分底层引擎 |

---

## <a id="11"></a>十一、部署与运维

### 11.1 Docker Compose 一键部署

系统包含 4 个容器：MySQL 8.0 + ChromaDB + FastAPI 后端 + Vue 前端（Nginx 静态服务）。评委从零到可演示约 15 分钟：

```bash
export DEEPSEEK_API_KEY=sk-your-key
docker-compose up -d
# 前端: http://localhost:3000
# 后端文档: http://localhost:8080/docs
```

### 11.2 低配置运行方案

| 组件 | CPU 方案 | 影响 |
|------|--------|------|
| BERT 分类 | ONNX Runtime 量化模型，CPU 推理 ~500ms | 分类速度略降，仍可接受 |
| 文本向量化 | text2vec-base-chinese 轻量模型（~100MB）| 精度略降 |
| OCR | PaddleOCR mobile 配置 | 速度稍慢 |
| LLM | 使用云端 API，不占本地算力 | 无影响 |

最低配置：4 核 CPU + 8GB RAM + 20GB 磁盘，可运行完整功能。

### 11.3 开源模型降级方案

当 DeepSeek API 不可用时，可切换为本地开源模型：

| 组件 | 默认 | 开源降级 | 切换方式 |
|------|:---:|------|------|
| LLM 分类/抽取/审核 | DeepSeek-V3 | Qwen2.5-7B-Instruct（vLLM 本地部署）| 修改 config.py 模型名 |
| Corex 多 Agent | DeepSeek-V3 | 4 Agent 共用同一 Qwen2.5-7B 实例（不同 Prompt）| 同上 |

### 11.4 统一开发环境

为确保 5 人团队开发环境一致、避免"我这能跑你那报错"，团队统一以下环境配置：

**基础环境：**

| 组件 | 版本 | 用途 | 安装方式 |
|------|:---:|------|---------|
| Python | 3.11.x | 后端 + AI 引擎 | pyenv 或官网安装（3.10+ 均可，3.11 推荐） |
| Node.js | 20.x LTS | 前端构建 | nvm 管理版本 |
| MySQL | 8.0.x | 结构化数据存储 | Docker 容器或本地安装 |
| Docker | 24.x+ | 容器化部署 | Docker Desktop 或 Engine |
| Git | 2.40+ | 版本管理 | 系统包管理器 |

**Python 依赖（requirements.txt）：**

后端核心：fastapi==0.110.0、uvicorn==0.29.0、sqlalchemy==2.0.30、pymysql==1.1.1、pydantic==2.7.0、python-jose==3.3.0（JWT）、passlib[bcrypt]==1.7.4、python-multipart==0.0.9、httpx==0.27.0（Dify API 调用）。

AI 引擎：torch==2.3.0、transformers==4.41.0、onnxruntime==1.18.0、chromadb==0.5.0、text2vec==1.2.0（向量编码 + Cross-Encoder Reranker）、pdfplumber==0.10.0、python-docx==1.1.0、paddleocr==2.8.0、paddlepaddle==3.0.0。

**前端依赖（package.json）：**

vue==3.4.0、element-plus==2.7.0、echarts==5.5.0、pdfjs-dist==4.0.0、axios==1.7.0、vite==5.2.0。

**环境搭建检查清单（新人入职 15 分钟完成）：**

1. `git clone` 项目仓库
2. `cp .env.example .env` 填入 DeepSeek API Key
3. `pip install -r requirements.txt --break-system-packages`（Linux）/ `pip install -r requirements.txt`（Windows）
4. `cd frontend && npm install`
5. `docker-compose up -d mysql chromadb`（仅启动数据库容器）
6. `python backend/main.py` 启动后端（开发模式）
7. `cd frontend && npm run dev` 启动前端（开发模式）
8. 访问 `http://localhost:5173` 确认前后端联通

以上检查清单与《A24-开工准备指南.md》配套使用，确保每位团队成员在 15 分钟内完成开发环境搭建。

---

## <a id="12"></a>十二、安全与企业适配

### 12.1 认证与基础权限（P0）

JWT Token 认证：用户登录后签发 24h 有效 Token，存前端内存（非 localStorage，防 XSS）。P0 阶段仅区分 admin（管理用户和模板）和 user（上传审核查看）两级角色。完整 RBAC 三级权限体系（admin/legal/viewer）列入 V2.0。

### 12.2 数据安全

- **合同原文：** 本地 MySQL 存储，云端 LLM 调用前自动脱敏（公司名 → 甲方/乙方，金额 → XX 万元），仅传条款片段，不传整份合同
- **用户密码：** bcrypt（cost=12）哈希，不可逆
- **API Key：** 环境变量注入，不入库、不入 Git
- **传输加密：** TLS 1.3（HTTPS）

### 12.3 审计日志（V2.0 规划）

关键操作（登录、上传、审核触发、报告导出、反馈提交）自动记录审计日志。完整审计日志功能（含操作人、时间、IP、操作详情、管理后台筛选查看）列入 V2.0。

### 12.4 企业部署模式

| 模式 | 适用场景 | 配置 |
|------|---------|------|
| 单机 Docker | 中小企业 1-5 人法务团队 | docker-compose up -d |
| 内网私有化 | 大型企业数据不出内网 | LLM 切换本地 Qwen2.5-7B |
| 云端 SaaS | 多租户按需付费 | 增加租户隔离 + 用量计费（V2.0）|

---



---

## <a id="13"></a>十三、V2.0 未来规划

以下功能已完成需求分析和概要设计，因三周开发周期限制，列入 V2.0 开发计划。答辩时可说明"已完成设计，列入下一阶段规划"，体现方案的前瞻性。

### 13.1 功能增强类

| 功能 | 当前状态 | V2.0 规划 | 优先级 |
|------|---------|----------|:---:|
| 自定义审查规则引擎 | 表结构已预留（custom_rules 表）| 可视化规则配置器 + 规则市场 | 高 |
| 合同版本差异对比 | 概要设计完成 | diff-match-patch + LLM 语义分析 | 高 |
| 企业知识库管理 | 表结构已预留（knowledge_base 表）| 知识库管理后台 + 自动向量化 | 中 |
| 多轮对话合同修改 | 初赛 demo 可演示单条款修改 | 完整自然语言指令理解 + 多条款联动 | 中 |
| RBAC 三级权限 | 概要设计完成 | 完整 admin/legal/viewer + 细粒度接口鉴权 | 中 |
| 完整操作审计日志 | 概要设计完成 | 审计日志存储 + 管理后台查询 + 导出 | 中 |

### 13.2 Corex 增强类

| 功能 | 当前状态 | V2.0 规划 | 优先级 |
|------|---------|----------|:---:|
| Corex Discuss 辩论裁决 | 概要设计完成（3.4.2 节）| 争议条款自动触发 + 三方辩论 + 裁判裁决 | 高 |
| Corex Retrieve 四源融合 | 简化为双源（法条+案例）| 完整四源（+ 行业实践 + 裁判文书）| 中 |
| Agent 专业模型微调 | 共用 DeepSeek-V3 | 各 Agent 独立 LoRA 微调（法务/合规/财务）| 低 |

### 13.3 创新探索类

| 功能 | 当前状态 | V2.0 规划 | 优先级 |
|------|---------|----------|:---:|
| 知识图谱跨条款关联分析 | 预定义关联规则检查 | 实体关系抽取 + Neo4j 可视化 + 跨条款推理 | 低 |
| 印章/签名区域定位与合规检查 | OCR 印章二元判断 | YOLO 印章检测 + 位置合规分析 + 视觉标注 | 低 |
| 企业 SSO 单点登录 | 接口已预留 | OAuth2/OIDC 对接企业统一认证 | 中 |

### 13.4 体验优化类

- 移动端适配（PWA）
- 合同模板市场（内置 20+ 行业标准模板）
- 批量合同审核（一次上传多份，并行审核）
- 风险趋势分析仪表盘（企业维度风险统计看板）

---

> **V2.0 规划的定位：** 以上功能说明 A24 具有清晰的演进路径，不是"做完就扔"的比赛项目。初赛阶段聚焦 P0/P1 功能实现完整演示闭环，V2.0 规划证明团队对产品的长期思考。



---

# 第三部分：附录

---

## <a id="14"></a>附录A：项目目录结构

### A.1 总体结构

项目仓库 `a24-contract-auditor/` 按前后端分离组织，AI 模块作为后端子包，部署通过 Docker Compose 统一编排。

### A.2 前端目录 (`frontend/`)

采用 Vue 3 + Element Plus 标准项目结构，Vite 构建。

`src/views/` 下包含五个核心页面：`Upload.vue`（合同上传）、`ContractList.vue`（合同列表，含筛选搜索分页）、`ContractDetail.vue`（合同详情，含要素展示和原文面板）、`AuditResult.vue`（审核结果，含风险列表和 Corex 审核路径展示）、`ReportView.vue`（审核报告，含 ECharts 热力图和 pdf.js 原文高亮）。`src/components/` 下抽取通用组件：`RiskBadge.vue`（风险等级徽章）、`ClauseHighlighter.vue`（条款高亮定位）、`FeedbackPanel.vue`（反馈标注面板）、`HeatmapChart.vue`（热力图封装）。`src/api/` 按模块封装 Axios 请求（auth / contract / audit / report / feedback / template），统一拦截 JWT Token 注入和 401 跳转。

### A.3 后端目录 (`backend/`)

FastAPI 应用根目录。`main.py` 为应用入口，注册路由和中间件。`config.py` 集中管理环境变量（数据库连接、LLM API Key、模型路径等）。

`routers/` 下按业务模块拆分路由文件：`auth.py`（注册登录）、`contracts.py`（CRUD + 上传）、`audit.py`（审核触发 + 进度 + 结果）、`reports.py`（报告获取 + PDF 导出）、`feedback.py`（标注提交）、`templates.py`（模板管理）。`models/` 下为 SQLAlchemy ORM 模型（user / contract / audit_record / audit_report / template / feedback_log），与第九章 6 张表一一对应。`schemas/` 下为 Pydantic 请求/响应模型，用于 FastAPI 自动文档生成和输入校验。

`services/` 为业务逻辑层：`auth_service.py`（JWT 签发 + bcrypt 校验）、`contract_service.py`（合同生命周期管理）、`audit_service.py`（审核流程编排，调度文档解析 → 分类 → 抽取 → 风险检测 → 报告生成流水线）、`report_service.py`（报告模板渲染）、`template_service.py`（模板 CRUD + 版本管理）。

### A.4 AI 模块目录 (`backend/ai/`)

AI 核心引擎作为 `backend/` 的子包：`parser.py`（多格式文档解析：docx / pdf / OCR）、`classifier.py`（BERT 分类器封装 + ONNX 推理）、`extractor.py`（LLM 要素抽取 + JSON Schema 约束）、`rule_engine.py`（12 类风险规则 + 自定义规则执行）、`rag.py`（ChromaDB 向量检索封装 + 知识库初始化和查询）、`corex/` 子包（`orchestrator.py` 编排器调度三种范式，`agents.py` 定义各 Agent Prompt 和行为，`review.py` / `discuss.py` / `retrieve.py` 分别实现三种协作范式）、`comparator.py`（条款语义比对 + 缺失检测）、`reporter.py`（报告数据组装 + Markdown 渲染）。`knowledge/` 子目录存放预置知识 JSON 文件（风险案例、法律法规、标准条款），首次启动时自动向量化写入 ChromaDB。

### A.5 部署目录 (`deploy/`)

`docker-compose.yml`（4 容器编排：MySQL + ChromaDB + FastAPI + Nginx）、`Dockerfile.backend` / `Dockerfile.frontend`、`init.sql`（建表 + 预置数据）、`.env.example`（配置模板）。

### A.6 文档目录 (`docs/`)

设计文档、API 文档、部署文档、用户手册。

---

## <a id="15"></a>附录B：技术风险与应对

### B.1 Corex 多 Agent 协作的工程风险

Corex 是 A24 的核心创新，也是最复杂的工程模块。以下是 5 个关键技术风险及应对方案：

| 风险 | 概率 | 影响 | 应对方案 | 降级策略 |
|------|:---:|:---:|---------|---------|
| LLM API 调用超时/限流 | 中 | 审核中断，用户体验差 | 每 Agent 调用设置 15s 超时；Review 模式 4 个 Agent 串行依赖（法务→合规→财务→Self-QA），Discuss 模式 2 Agent 并行辩论 → 1 裁判串行裁决，Retrieve 模式 2 Agent 全并行 | 超时 Agent 跳过，其余 Agent 结果正常融合；连续 2 次超时触发自动降级到轻量模式 |
| 多 Agent 并行时 API Key 耗尽 | 低 | 审核失败 | 实现 Token 用量计数器和配额预警（达 80% 时通知用户）；Discuss/Retrieve 并行调用前检查剩余配额 | 配额不足时仅运行 Review 模式（5 次串行调用） |
| Agent 输出格式不兼容 | 中 | 结果融合失败，审核报告缺字段 | 每个 Agent 的 LLM 调用使用严格的 JSON Schema 约束输出；Orchestrator 拿到各 Agent 输出后先做格式校验再融合 | 格式不合规的 Agent 输出丢弃重试 1 次，仍失败则跳过该 Agent |
| Corex 延迟过高（precise > 30s）| 中 | 用户等待焦虑 | Review 模式 Self-QA 步骤与前三步部分并行（合规 Agent 完成后 Self-QA 即可开始预校验）；前端展示审核进度条（"正在调用法务 Agent… → 合规复核中… → 财务分析中…"）| 30s 硬超时，返回已完成步骤的部分结果，标注"部分 Agent 未完成" |
| 模型输出幻觉（标注不存在的条款）| 中 | 审核结果可信度下降 | Corex 自带了缓解机制：Self-QA 步骤专做存在性验证（"原文是否确实存在该条款？"）；Retrieve 模式评分 Agent 同样执行存在性检查；置信度 < 0.6 的标注默认不展示 | 幻觉标注被 Self-QA 过滤后不进入最终报告 |

### B.2 其他技术风险

| 风险 | 概率 | 影响 | 应对方案 |
|------|:---:|:---:|---------|
| BERT 微调数据不足 | 中 | 分类准确率不达标（< 85%）| 数据增强（回译 + 同义词替换 + 模板生成）；优先确保 5 种类型各 ≥ 50 条标注样本 |
| ChromaDB 首次向量化耗时过长 | 中 | 首次启动等待 > 5 分钟 | 预置数据量控制在 500 条以内；Docker 镜像预构建向量索引 |
| PDF 扫描件 OCR 质量差 | 中 | 分类和审核结果不可靠 | 扫描质量预检（DPI < 150 或模糊度 > 阈值时提示用户重新上传）；OCR 结果置信度标注 |
| 测试合同数据不足 | 高 | 实验指标无法验证 | 提前联系命题企业获取脱敏样例；使用 CUAD / LeCaRD 公开数据集补充；自建 100 份模板生成合同 |
| 服务器性能不足（答辩现场网络差）| 中 | Demo 演示卡顿 | 预备离线 Demo 视频；LLM 响应预缓存若干典型合同的审核结果，演示时直接加载 |

---

## <a id="16"></a>附录C：鼓励创新方向与 MVP 分阶段实现

### C.1 赛题第 7 节鼓励创新的三个方向

赛题在第 7 节"其他"中明确鼓励三个创新方向。以下逐一分析实现方案和初赛/决赛阶段规划：

**方向一：多轮对话式合同修改建议**

实现方案：在审核结果页面嵌入对话面板，用户可通过自然语言指令修改合同条款（如"将第 8 条违约金上限改为合同金额的 20%"），系统解析指令 → 定位条款 → 调用 LLM 生成修改文本 → 展示修改前后对比。技术路线：指令解析用 LLM + Function Calling（定义 modify_clause / delete_clause / add_clause 三个 function），条款定位复用第四章的语义对齐能力。

MVP 分阶段：初赛阶段（7.15-8.3）完成对话面板 UI 框架 + 单条款修改 demo（指定条款编号 + 简单数值修改）；决赛阶段（如晋级）实现完整的自然语言指令理解 + 多条款联动修改 + 修改历史回溯。

**方向二：知识图谱辅助跨条款关联风险分析**

实现方案：从合同中抽取实体（甲方、乙方、金额、日期、管辖法院等）和关系（付款方、收款方、管辖地等），构建轻量级合同知识图谱。利用图谱推理发现跨条款的隐含风险——例如"争议解决条款约定北京仲裁，但管辖条款约定杭州法院"，或"保密期限超出合同履行期限 3 年以上"。技术路线：实体关系抽取用 LLM Few-shot Prompting，图谱存储用 Neo4j（轻量部署）或直接 JSON 表示，关联规则用预定义模板（跨条款冲突模式库）。

MVP 分阶段：初赛阶段仅做预定义关联规则检查（如"违约金条款 → 合同金额条款"金额一致性校验），答辩 PPT 中展示知识图谱概念设计；决赛阶段实现完整的实体关系抽取 + Neo4j 可视化图谱 + 跨条款推理。

**方向三：扫描件印章/签名区域定位与合规性检查**

实现方案：利用 PaddleOCR 的印章检测能力或训练轻量 YOLO 印章检测模型，定位合同扫描件中的印章和签名区域。检查项包括：是否缺少公章、签名是否置于正确位置（盖章处/签字处）、印章是否覆盖正文文字（合规性问题）。技术路线：印章检测用 PaddleOCR 内置印章识别 + OpenCV 红章颜色过滤；签名检测用目标检测模型定位手写体区域。

MVP 分阶段：初赛阶段在文档解析模块中集成 PaddleOCR 印章检测 API，实现"是否有印章"的二元判断，结果展示在合同详情页；决赛阶段实现印章位置合规分析 + 签名区域定位 + 视觉标注展示。

### C.2 MVP 最低交付标准（重申）

如果时间紧张，初赛阶段至少确保以下功能可演示，并在答辩 PPT 中标注其他功能为"已完成设计、开发中"：

- 合同上传 + 文档解析 + OCR（基础功能）
- BERT 分类 + 要素抽取（赛题指标 1、2）
- 规则引擎 + RAG 语义审核（赛题指标 3 基础要求）
- **Corex Review 模式（必须完成 — A24 核心差异化）**
- 标准条款比对与缺失检测（赛题要求）
- 审核报告生成 + ECharts 风险热力图
- 鼓励创新方向一的多轮对话基础 UI（展示创新能力）

---

## <a id="17"></a>附录D：量化实验指标与验证方案

### D.1 测试数据集构建计划

实验评估需要两组独立的标注数据集：

| 数据集 | 用途 | 规模 | 标注内容 | 来源 |
|-------|------|:---:|---------|------|
| 分类 + 抽取测试集 | 评估分类准确率和要素抽取 F1 | 100 份 | 合同类型标签 + 要素字段标注 | CUAD（英文，选取相关子集翻译）+ LeCaRD（中文）+ 自建模板生成 50 份 |
| 风险审核测试集 | 评估风险召回率/精确率和 Corex 效果 | 80 份 | 每条风险条款的位置 + 类型 + 等级 | 团队手工标注（4 人 × 20 份）+ 命题企业脱敏样例 |

标注流程：4 名团队成员各标注 20 份合同 → 两两交叉校验 → 不一致标注由第 5 人裁决 → 计算 Cohen's Kappa 标注一致性（目标 > 0.8）。

### D.2 核心指标验证方案

对应赛题要求的三项量化指标，逐项说明验证方法：

**指标 1 — 合同分类准确率（≥ 85%）**

验证方法：在 100 份分类测试集上，计算正确分类数 / 总数。分别测试 BERT 独立模式和 BERT + LLM 双模型模式，对比准确率差异和 LLM 调用比例。

**指标 2 — 关键要素抽取 F1（≥ 80%）**

验证方法：在 100 份分类测试集上，对每个要素字段（甲方、乙方、金额、日期、争议解决方式、适用法律）分别计算精确率 P、召回率 R、F1 = 2PR/(P+R)，取各字段 F1 的宏平均。字段匹配采用宽松匹配（如"2024 年 3 月 15 日"与"2024-03-15"视为匹配）。

**指标 3 — 风险条款识别召回率（≥ 75%）**

验证方法：在 80 份风险标注测试集上，计算召回率 = 正确检出的风险数 / 标注风险总数，精确率 = 正确检出的风险数 / 系统检出的风险总数。分别测试轻量模式（仅 Retrieve）、标准模式（Retrieve + Review）和深度模式（全量 Retrieve + Review + Discuss），对比三种模式的效果差异。

### D.3 消融实验设计

为验证各模块的独立贡献，设计以下消融实验：

| 消融条件 | 测试模块 | 预期影响 |
|---------|---------|---------|
| 基线：仅 LLM 直接审核 | 完整系统 | 预期召回率 ~78%，误报率 ~20% |
| 基线 + 规则引擎 | 规则引擎贡献 | 预期召回率 +5%，误报率 -2% |
| 基线 + 规则引擎 + RAG | RAG 贡献 | 预期召回率 +7%，误报率 -6% |
| 基线 + 规则引擎 + RAG + Corex Review | Corex Review 贡献 | 预期召回率 +10%，误报率 -10% |
| 完整系统（+ Corex Discuss + Retrieve）| Discuss + Retrieve 贡献 | 预期召回率 +14%，误报率 -12% |

消融实验的结果将作为答辩 PPT 的核心数据支撑，直观展示 Corex 多 Agent 协作的边际价值。

### D.4 非功能指标

| 指标 | 预期值 | 测试方法 |
|------|:-----:|---------|
| BERT 分类推理延迟 | 预期 < 500ms（CPU）| 100 次推理取 P95 |
| 合同上传到审核完成（轻量模式）| 预期 < 8 秒 | 计时 10 份不同长度合同 |
| 合同上传到审核完成（标准模式）| 预期 < 20 秒 | 同上 |
| 合同上传到审核完成（深度模式）| 预期 < 35 秒 | 同上 |
| API 调用成本（轻量模式）| 预期 < 0.2 元/份 | 统计 DeepSeek-V3 Token 用量 |
| API 调用成本（标准模式）| 预期 < 0.5 元/份 | 同上 |
| API 调用成本（深度模式）| 预期 < 1.0 元/份 | 同上 |
| 系统可用性（Docker 一键启动成功率）| 预期 > 95% | 5 台不同环境机器测试 |

### D.5 用户满意度与可用性测试

面向 3-5 名法学专业同学做 30 分钟可用性测试：完成"上传合同 → 查看审核结果 → 修正 AI 标注 → 导出报告"四个任务，记录任务完成率和操作时间，收集主观反馈（SUS 量表），为产品迭代提供方向。测试结果写入答辩 PPT"用户验证"页。

---

## <a id="18"></a>附录E：数据集与模型训练方案

### E.1 数据来源与获取

**公开数据集：**

CUAD（Contract Understanding Atticus Dataset）：包含 500+ 份英文商业合同，标注了 41 类关键条款。选取与赛题需求相关的子集进行翻译和适配（合同类型、双方信息、金额、日期、管辖、违约责任等），为 BERT 分类器提供基础训练数据。

LeCaRD（Chinese Legal Case Retrieval Dataset）：中文法律文本数据集，可用于辅助风险条款分类器和法律知识库构建。

**自建数据：**

5 种合同类型（采购、销售、保密协议、服务外包、劳动）各编写 20 份模板合同（共 100 份），覆盖不同类型合同的标准表述和常见变体，用于 BERT 分类微调和要素抽取 Prompt 调优。同时拟联系命题企业（网新恒天）获取脱敏合同样例补充训练数据。

**数据增强：** 对自建数据进行回译增强（中文 → 英文 → 中文，生成语义等价但表述多样的变体）、同义词替换（关键法律术语保持不变，通用词替换）、模板变异（修改合同金额、日期、公司名等实体值生成新样本）。

### E.2 BERT 分类器微调方案

**模型选择：** bert-base-chinese（HuggingFace），12 层 Transformer，768 维隐藏层，110M 参数。

**训练配置：**

| 配置项 | 值 | 说明 |
|--------|:---|------|
| 训练集 | 每种合同类型 50 条（共 250 条）| 自建模板 + 数据增强 |
| 验证集 | 每种合同类型 15 条（共 75 条）| 随机划分 |
| 测试集 | 每种合同类型 20 条（共 100 条）| 见附录 D.1 |
| 优化器 | AdamW，lr=2e-5 | 标准 BERT 微调学习率 |
| Batch Size | 16 | 适配 GPU 显存（T4 16GB） |
| Epochs | 5 | 配合 Early Stopping（patience=2） |
| 最大序列长度 | 512 tokens | 合同前 512 tokens 通常包含类型信号 |

**训练流程：**
1. 数据预处理：提取合同全文 → 按 512 tokens 截断（取前 512 + 后 128 拼接，保留结尾关键信息）→ 添加 [CLS] / [SEP] token → Tokenizer 编码
2. 训练：HuggingFace Trainer API，单卡 GPU（T4/V100），约 30 分钟完成
3. 评估：在验证集上计算 Accuracy + Macro F1，确认 ≥ 85% 后保存模型
4. 导出：转换为 ONNX 格式（torch.onnx.export），量化（dynamic quantization → INT8），模型文件从 ~400MB 压缩到 ~100MB，CPU 推理延迟从 ~800ms 降至 ~300ms

**预期效果：** 5 分类 Accuracy ≥ 88%（训练集），泛化到测试集 ≥ 85%；ONNX 量化后 CPU 推理 < 500ms。

### E.3 要素抽取 Prompt 调优

不微调模型（成本和时间限制），采用 Few-shot Prompting 方案。优化策略：

- 为每种合同类型设计 3-5 个标注示例（含完整的合同片段 + 结构化 JSON 输出），确保示例覆盖该类型的典型要素表述
- Prompt 中明确 JSON Schema 约束和字段说明（字段名、类型、必填/可选、示例值）
- 对所有 LLM 输出执行 Pydantic 校验（类型检查 + 必填字段检查），校验失败自动重试 1 次
- 记录抽取失败案例，分析常见失败模式（格式不规整 / 多值混淆 / 空值），迭代优化 Prompt 和示例

### E.4 ChromaDB 知识库初始化方案

四个 ChromaDB Collection 的预置数据以 JSON 文件存储在 `backend/ai/knowledge/` 目录下，首次启动时自动编码写入：

| Collection | JSON 文件 | 条数 | 单条示例 |
|:---|------|:---:|------|
| risk_cases | `risk_cases.json` | ~80 | {type: "R01", clause: "…", risk_level: "high", judgment: "…", source: "民法典第585条"} |
| standard_clauses | `standard_clauses.json` | ~150 | {type: "采购合同", title: "违约责任", content: "…", is_required: true} |
| laws | `laws.json` | ~200 | {law: "民法典", article: "第585条", content: "…", tags: ["违约金", "违约责任"]} |
| audit_history | 运行时写入 | 0 初始 | 由 feedback_log 中 confirmed=1 的记录自动向量化积累 |

编码流程：读取 JSON → text2vec-large-chinese 逐条编码为 1024 维向量 → ChromaDB Client API 批量写入。预置 430 条知识，编码耗时约 2 分钟（CPU）。

向量模型的降级方案：GPU 可用时使用 text2vec-large-chinese（精度高，~1.3GB），纯 CPU 环境自动切换 text2vec-base-chinese（~100MB，精度略降，速度更快）。切换对上层调用透明，仅在 config.py 中修改一行配置。

### E.5 模型版本管理

训练产出的 ONNX 模型文件、LLM Prompt 模板、规则配置文件均纳入 Git 版本管理。每次模型更新记录：更新日期、更新原因（如"补充 50 条采购合同训练数据"）、更新前后在测试集上的指标对比。模型的 ONNX 文件存储在 `backend/ai/models/` 目录，通过 Git LFS 管理大文件。



---

## <a id="19"></a>附录F：赛题提交材料对应体系

赛题明确要求提交五类材料，以下逐项说明体系化组织方式：

### F.1 用户使用手册框架（对应提交材料⑤-①）

用户手册计划在开发完成后按以下结构编写：

1. 系统概述与适用场景
2. 快速开始（注册登录 → 上传第一份合同 → 查看审核结果）
3. 功能操作指南：合同上传与管理、审核结果解读（风险等级/置信度/Agent 日志）、报告导出（HTML/PDF/Markdown）、反馈标注操作
4. 自定义配置：模板管理（上传/编辑/版本切换）、比对严格度设置
5. 常见问题 FAQ（10+ 条目）

### F.2 模型训练/微调过程记录框架（对应提交材料⑤-②）

训练过程将按以下模板逐项记录并提交：

| 记录项 | 内容 |
|-------|------|
| 训练日期 | [实际日期] |
| 模型版本 | bert-contract-classifier-v1.0 |
| 训练数据规模 | 250 条（5 类 × 50 条）+ 数据增强后 ~500 条 |
| 训练参数 | lr=2e-5, batch=16, epochs=5, early_stopping patience=2 |
| 训练时长 | ~30 min（单卡 T4） |
| 验证集准确率 | [实际值]% |
| 测试集准确率 | [实际值]% |
| 消融实验结果 | 见附录 D.3 消融实验执行台账 |
| 训练过程中的问题与解决 | [如实记录遇到的问题和调整方案] |

### F.3 项目分工与开发过程记录框架（对应提交材料⑤-③）

| 角色 | 姓名 | 负责模块 | 第一周目标 | 第二周目标 | 第三周目标 |
|------|:---:|---------|---------|---------|---------|
| 前端 A | — | 合同上传/列表/详情页 | 页面框架 + 路由 | 联调后端接口 | 完善交互 + 异常处理 |
| 前端 B | — | 审核结果/报告/热力图 | ECharts 集成 + pdf.js | 报告页面 + 反馈面板 | 联调 + 体验优化 |
| 后端 C | — | FastAPI 路由/数据库/认证 | 表结构建完 + 基础 CRUD | 审核调度 + 报告导出 | 联调 + 性能优化 |
| AI D | — | 文档解析/分类/Corex | 解析器 + LLM 分类管道 | 规则引擎 + RAG | Corex Review + 联调 |
| AI E | — | RAG/条款比对/报告生成 | ChromaDB 初始化 + 知识库 | 条款比对 + 报告模板 | 联调 + 效果调优 |

每日进度同步机制：飞书/钉钉每日站会 + GitHub Issues 任务看板。开发过程记录将整理为每周汇总报告提交。

---

> **文档结束。** 本文档版本 v3.1，共 13 章正文 + 6 篇附录，完整覆盖赛题全部要求。方案设计篇面向答辩评审，工程实现篇面向开发落地，附录与未来规划篇面向细节支撑和长期演进。三篇各有侧重、互为补充，共同构成 A24 合同智能审核系统的完整设计方案。
