# A24 项目现状诊断报告

> 日期：2026-07-19  
> 阶段：设计周最后一天（7.15-7.20）  
> 下次开发：7.22 线上开发第一周开始

---

## 一、导航 Bug 修复（本次已修）

最新提交 `d447486`（"B:实现主界面的反馈标注"）引入了 3 个阻断级 Bug，导致整个 Vue 应用在模块加载阶段崩溃，所有跳转看似"失效"。

| Bug | 文件 | 问题 | 状态 |
|-----|------|------|:--:|
| 1 | `ContractDetail.vue:211` | 使用 `useFeedback()` 但从未 import | ✅ 已修复 |
| 2 | `useFeedback.js:3` | 导入 `getFeedbackList`，但 API 只导出 `getFeedback` | ✅ 已修复 |
| 3 | `ContractDetail.vue` | `feedbackRef` 在 composable 解构和本地 ref 双重定义，`onFeedback` 命名冲突 | ✅ 已修复 |

**建议**：团队在合入代码前必须跑 `npm run dev` 确认页面不崩。这三个 Bug 任何一个都会导致 Vue 编译失败——说明改完代码没有跑起来验证过。

---

## 二、实际完成度 vs 设计文档对比

### 2.1 前端（约 70% 完成）

| 页面 | 状态 | 问题 |
|------|:--:|------|
| 登录/注册 | ✅ 完成 | — |
| 合同上传 | ✅ 完成 | 上传完立即跳转，不等审核完成 |
| 合同列表 | ✅ 完成 | 分页+筛选功能完整 |
| 合同详情 | ⚠️ 80% | PDF 硬编码为 `/test.pdf`；条款比对 Tab 是占位符 |
| 审核结果列表 | ✅ 完成 | N+1 查询性能问题 |
| 审核结果详情 | ⚠️ 85% | 未传 `loadedFeedbacks` 给 FeedbackPanel |
| 审核报告列表 | ✅ 完成 | N+1 查询 |
| 审核报告详情 | ⚠️ 70% | PDF 硬编码；无风险明细表；图表代码与审核报告页重复 |
| 首页 Dashboard | ❌ 存根 | **所有数据为 mock**：4 个统计卡片、近期合同表格、ECharts 图表全部是写死的数据 |

**前端代码质量问题**：
- `typeLabel` / `statusLabel` / `levelMap` / `formatTime` 在 6+ 个 views 中重复定义
- ECharts 初始化逻辑在 `AuditReport.vue` 和 `AuditReportDetail.vue` 中重复
- `RiskBadge.vue` 组件只在一个地方使用，其他地方都用内联 `<el-tag>`

### 2.2 后端（约 60% 完成）

| 模块 | 设计期望 | 实际状态 |
|------|---------|---------|
| 文档解析 (DOCX/PDF/OCR) | P0 完整 | ✅ 基础实现（无表格提取，无标题检测） |
| 合同分类 — LLM | P0 | ✅ DeepSeek 5 分类 |
| 合同分类 — **BERT** | P0（可选优化） | ❌ **零代码**：无 torch/transformers/BERT |
| 要素抽取 | P0 | ✅ DeepSeek + Pydantic 校验 |
| 规则引擎（12 类风险） | P0 | ✅ 12 条规则 + 6 条白名单 |
| **RAG 语义审核** | **P0** | ❌ **`ai/rag/` 目录为空**：知识库 JSON 存在但无检索管道 |
| **Corex 多 Agent 审核** | **P1 冲刺** | ✅ 4 Agent 顺序管道已实现（超前完成） |
| **标准条款比对** | **P0** | ❌ **`ai/matcher/` 目录为空** |
| **审核报告生成** | **P0** | ❌ **`ai/reporter/` 目录为空**：报告是 `contracts.py` 里拼接的 HTML 字符串 |
| 用户反馈标注 | P0 | ✅ CRUD 完整 |
| 模板管理 API | P0 | ❌ Model 存在但无路由/服务 |
| ChromaDB 向量库 | P0 | Docker 启动了但无代码使用 |
| JWT 认证 | P1 | ✅ 已实现（超前） |
| Docker 部署 | P1 | ❌ 仅 ChromaDB 容器，无 FastAPI/Nginx |

### 2.3 关键结论

**三个 P0 核心模块完全未开工**：RAG 检索、条款比对、报告生成——这些都是赛题强制要求的 P0 模块，设计文档中分配给同学 E（RAG + 条款比对 + 报告生成），但目前 `ai/rag/`、`ai/matcher/`、`ai/reporter/` 三个目录全空。

**一个 P1 模块反而超前完成**：Corex 多 Agent Review 是 P1 差异化加分项，但已经做了 4 Agent 管道。这反映开发优先级与设计文档不一致——P0 还没跑通就做了 P1。

**BERT 分类已事实放弃**：设计文档中 BERT 是可选的 P0 优化（有纯 LLM 降级方案），但目前连一行 BERT 代码都没有，可以认为已经走了降级方案。

---

## 三、五个人的下一步安排（7.22-8.3 两周冲刺）

### 当前紧急优先级

1. **立即**（7.19-7.20）：修完阻断 Bug，补上首页真实数据（别让答辩评委打开看到一个 mock 页面）
2. **第一优先级**（7.22-7.27）：补齐三个 P0 空模块（RAG / 条款比对 / 报告生成）
3. **第二优先级**（7.29-8.3）：联调、修边界场景、写测试、准备演示

---

### 同学 A（前端 — 合同管理模块）

| 时间 | 任务 | 产出 |
|------|------|------|
| 7.22-7.23 | HomeView 接入真实后端 API（统计卡片 + 近期合同列表 + 图表），去掉所有 mock 数据 | 首页可展示真实审核数据 |
| 7.24-7.25 | ContractDetail PDF 从合同 API 获取真实文件路径，替换硬编码 `/test.pdf`；条款比对 Tab 对接后端 API | 合同原文可真实预览 |
| 7.26-7.27 | 抽取公共工具函数（`typeLabel`/`statusLabel`/`formatTime` 放到 `src/utils/`），RiskBadge 全面替换内联 el-tag | 代码去重、维护性提升 |
| 7.29-8.1 | 前端边界场景处理：空合同、审核中状态轮询、超大文件上传提示 | 健壮性 |
| 8.2-8.3 | 联调 + Bug 修复 + 体验优化 | 可演示 |

### 同学 B（前端 — 审核 + 报告模块）

| 时间 | 任务 | 产出 |
|------|------|------|
| 7.22-7.23 | AuditReportDetail 补完：去掉硬编码 PDF，添加风险明细表，修复 PDF 只能看第 1 页问题 | 报告详情页完整 |
| 7.24-7.25 | 提取共享 `useEcharts` composable，消除 AuditReport 和 AuditReportDetail 的图表代码重复 | 代码整洁 |
| 7.26-7.27 | AuditResultDetail 补完 FeedbackPanel 的 `loadedFeedbacks` 集成；修复 AuditResult/AuditReport 的 N+1 查询（后端加字段或前端合并请求） | 反馈功能完整 |
| 7.29-7.31 | 审核进度实时轮询、审核中状态的 UI 展示（设计稿里提到的"正在调用法务 Agent…→ 合规复核中…"进度条） | 审核体验 |
| 8.1-8.3 | 联调 + 热力图真实数据对接 + Bug 修复 | 可演示 |

### 同学 C（后端 — API + 基础设施）

| 时间 | 任务 | 产出 |
|------|------|------|
| 7.22-7.23 | 修关键 Bug：审核管道错误吞噬（`except Exception: pass` → 记录日志+返回错误信息）；合同状态机加 rollback（审核失败不回 `auditing` 僵死） | 后端稳定性 |
| 7.24-7.25 | 实现模板管理 CRUD API（`/api/templates`），Model 已有，只需路由+服务层；实现 `DELETE /api/feedback/{id}`（前端 FeedbackPanel 的 undo 在等这个） | 模板 + 反馈删除 |
| 7.26-7.27 | 审核改为异步（FastAPI BackgroundTasks），添加审核进度查询端点（`GET /audit-status` 返回 pipeline_stage）；合同列表 API 返回风险数量摘要字段（修复前端 N+1） | 异步审核 |
| 7.29-7.31 | Docker Compose 全容器化（FastAPI + Nginx + MySQL + ChromaDB 四容器）；修正 CORS 配置、SECRET_KEY 环境变量化 | 一键部署 |
| 8.1-8.3 | 联调 + 前后端集成测试 + 准备演示环境 | 可演示 |

### 同学 D（AI — 解析 + 分类 + Corex）

| 时间 | 任务 | 产出 |
|------|------|------|
| **7.22-7.24** | **实现 RAG 检索模块（最高优先级）**：连接 ChromaDB → 编码知识库 JSON → 实现 `search(query)` → 在 `llm_auditor.py` 中实际注入 RAG 上下文。这是 P0 强制模块，目前完全空白 | RAG 审核上线 |
| 7.25-7.26 | 文档解析器增强：PDF 添加表格提取（pdfplumber 支持表格），DOCX 添加表格+页眉页脚提取；修复 PDF 段落碎片化问题 | 解析质量提升 |
| 7.27-7.28 | Corex 优化：Review 管道加 15s 超时 + 降级逻辑（设计文档附录 B 已定义）；与同学 E 协作调通 Retrieve 双源模式 | Corex 健壮性 |
| 7.29-7.30 | 知识库数据扩充：laws.json 从 10 条→200 条，risk_cases.json 从 15 条→80 条（按设计文档 3.8.1 节规格） | 知识库数据 |
| 7.31-8.3 | 端到端联调 + 审核效果调优 + 消融实验数据收集 | 实验数据 |

### 同学 E（AI — RAG + 比对 + 报告）

| 时间 | 任务 | 产出 |
|------|------|------|
| **7.22-7.24** | **实现条款比对模块（最高优先级）**：text2vec 向量编码 + 余弦相似度匹配 + 缺失检测（threshold 0.5/0.85） + 偏离分析。这是 P0 强制模块，目前 `ai/matcher/` 为空 | 条款比对上线 |
| 7.25-7.26 | **实现报告生成模块**：Jinja2 模板引擎 → 六章结构化 HTML 报告 → 导出为 PDF（WeasyPrint）。替换当前 `contracts.py` 里的字符串拼接 HTML | 专业报告 |
| 7.27-7.28 | 标准条款数据扩充：standard_clauses.json 从 10 条→150 条（5 类合同 × ~30 条/类），包含 priority 分级 | 条款模板数据 |
| 7.29-7.31 | 测试合同数据生成：Jinja2 模板引擎生成 100 份模板合同（5 类 × 20 份），风险标注测试集 40 份手工标注 | 测试数据 |
| 8.1-8.3 | 联调 + 实验指标验证（分类准确率、要素 F1、风险召回率） | 实验指标 |

---

## 四、风险分析

### 4.1 已出现的代码风险（需要立即修复）

| # | 风险 | 严重度 | 位置 | 说明 |
|---|------|:----:|------|------|
| 1 | 审核管道静默吞错 | 🔴 高 | `api/contracts.py:214,224` | `except Exception: pass` — LLM 或 Corex 失败时用户完全不知道，只拿到规则引擎的部分结果 |
| 2 | 同步审核阻塞 HTTP | 🔴 高 | `api/contracts.py` 审核端点 | precise 模式 4 次 LLM 调用在请求线程内串行执行，30-40s 无响应，浏览器可能超时 |
| 3 | HTML 注入风险 | 🟡 中 | `api/contracts.py` 报告拼接 | 风险条款原文直接拼进 HTML 字符串，不做转义，可能导致报告渲染错乱 |
| 4 | 合同文本截断 | 🟡 中 | `classifier.py(2000)`, `extractor.py(4000)`, `rule_engine.py(10000)` | 长合同的尾部内容被丢弃，尾部的风险条款完全漏检 |
| 5 | CORS 硬编码 | 🟡 中 | `main.py` | `allow_origins=["http://localhost:5173"]` — 换端口或部署后直接失效 |
| 6 | 审核状态机无 rollback | 🟡 中 | `api/contracts.py` | 审核中途崩溃后合同永久卡在 `auditing` 状态，无法重新审核 |
| 7 | requirements.txt 缺依赖 | 🟡 中 | `backend/requirements.txt` | 缺少 `openai`（DeepSeek 客户端依赖），新环境 `pip install` 后直接 import 失败 |
| 8 | SQLite 搭配 MySQL 连接池 | 🟢 低 | `database.py` | `pool_size=10, pool_recycle=3600` 对 SQLite 无意义但不致命 |
| 9 | 401 拦截器清理不完整 | 🟢 低 | `utils/request.js` | 只清 `token` 不清 `username`，退出后导航栏仍显示旧用户名直到下一次路由跳转 |
| 10 | SECRET_KEY 明文硬编码 | 🟡 中 | `config.py` | `"change-me-to-random-string"` 出现在源码中 |

### 4.2 未来两周的进度风险

| # | 风险 | 概率 | 影响 | 应对 |
|---|------|:--:|------|------|
| 1 | **RAG + 条款比对 + 报告 三个模块全空** | 🔴 已发生 | P0 演示闭环缺失三个核心环节 | 同学 D/E 本周最优先补齐，7.24 前必须有最小可用版本 |
| 2 | Corex 延迟导致 precise 模式 >30s | 🟡 中 | 演示时审核卡住 | 实现进度条轮询；precise 模式加 30s 硬超时返回部分结果 |
| 3 | LLM API 不可用（Key 耗尽/网络故障） | 🟡 中 | 全功能瘫痪 | 规则引擎可独立运行；预备离线 Demo 录屏；预缓存 3-5 份典型合同的审核结果 |
| 4 | 知识库数据量远不达标 | 🔴 已发生 | RAG 效果差，答辩被质疑 | 设计需求 ~430 条，目前 ~35 条。本周必须由 D/E 分工补到至少 200 条 |
| 5 | 前后端联调未做 | 🟡 中 | 演示时接口对不上 | 7.27 必须完成首轮全流程联调，7.29 第二轮 |
| 6 | 无 BERT 分类降级 | 🟢 已接受 | 分类准确率 ~85%（仅满足赛题底线）| 已走纯 LLM 降级方案，答辩时如实说明即可 |
| 7 | 测试数据未生成 | 🟡 中 | 实验指标无法验证 | 自建 100 份模板合同 + 40 份手工标注，最晚 7.31 完成 |
| 8 | 演示现场无网络/服务器性能差 | 🟡 中 | Demo 卡顿或崩溃 | 预备离线录屏；low-config 模式下所有组件 CPU 可运行 |
| 9 | 代码合并冲突（5 人并行开发）| 🟡 中 | 反复修冲突浪费时间 | 明确模块边界；每日至少合并一次 dev；功能开关隔离未完成代码 |
| 10 | PDF 真实路径问题 | 🔴 已发生 | ContractDetail 和 AuditReportDetail 都加载 `/test.pdf`，看不到真实合同 | 同学 A/B 本周内改为从 API 获取文件路径 |

### 4.3 架构/设计风险

1. **RAG 冷启动问题**：审核经验库初始为空是设计已知问题，有降级方案（依赖风险案例库 + 法律法规库），但前提是 RAG 检索模块本身被实现。

2. **全栈 Python 的单一故障点**：Go 或 Java 后端的同学无法直接上手。5 人全 Python 的技术栈要求意味着任何一个人掉队都会拖慢对应模块。

3. **Corex 4 Agent 串行依赖**：法务→合规→财务→Self-QA 的顺序依赖意味着任一 Agent 超时都会阻塞后续步骤。设计文档已定义了超时跳过逻辑但代码中未实现。

4. **pdf.js v6 API 兼容性**：memory 中已记录了 `destroy()` 在 `PDFDocumentLoadingTask` 而非 `PDFDocumentProxy` 的坑。升级 pdfjs-dist 版本时可能再次踩坑。

---

## 五、最关键的三个行动项

按紧急程度排序：

1. **立即（7.19-7.20）**：同学 C 修 3 个编译 Bug + 审核管道错误吞噬；同学 A 给 HomeView 接真实数据；所有人本地 `npm run dev` + `python main.py` 验证系统不崩。

2. **7.22 第一天**：同学 D 写 `ai/rag/` 最小可用版本（ChromaDB 连接 + 检索 + 注入 llm_auditor）；同学 E 写 `ai/matcher/` 最小可用版本（向量编码 + 余弦匹配 + 缺失检测）。当天晚上必须能跑通一条"合同上传 → 分类 → 抽取 → 规则+RAG 审核 → 条款比对 → 报告"的链路。

3. **7.24 前**：三个 P0 空模块（RAG、matcher、reporter）都达到可联调状态。如果 7.24 傍晚还有模块跑不通，7.25-7.26 全员集中攻关。

---

> **总结**：系统的骨架是好的——FastAPI + Vue 3 + Corex + 规则引擎都已可用。但有三块关键的肉没长出来（RAG、条款比对、报告），以及前端有两个编译 Bug 需要立刻修。未来两周的最大风险不是某个具体 Bug，而是**三个 P0 空模块同时从零开发的时间压力**。
